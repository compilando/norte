[package]
name = "norte-encoding"
description = "Text-encoding detection and decoding (BOM → chardetng → NUL heuristic)"
license = "MIT OR Apache-2.0"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
chardetng.workspace = true
encoding_rs.workspace = true
# NFC normalization for name pairing (ADR 0051, #151): ALREADY in the
# workspace (norte-compare, norte-core, norte-vfs-local, norte-testkit), so
# hard rule 8 asks for no further justification.
unicode-normalization.workspace = true

[dev-dependencies]
norte-testkit.workspace = true

[lints]
workspace = true
