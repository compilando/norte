//! CLI de humo de norte (`norte ls`, `norte cp`): banco de pruebas manual del
//! core en modo embebido. No es un producto; el frontend real llega en M1.

fn main() {
    // Los subcomandos llegan en la fase 11 de M0 (ls --json, cp con progreso).
}
