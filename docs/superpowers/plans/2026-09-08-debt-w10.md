# Debt wave W10 — the second audit of 2026-09-08, on the real tree this time

> **For agentic workers:** single source of truth for the wave. On resume:
> `git fetch`, read this file, `git log --oneline origin/main~20..`, continue
> from the first task not `done`. Work lands directly on `main` (owner's
> call, 2026-09-08); no feature branches. Update a task's status in the
> commit that finishes it.

**Baseline:** `235bfa27` (main == origin/main, after W9 and `v0.3.0-alpha.3`).
Gate budget as in CLAUDE.md: `just t <crate>` freely, `just c` when the lint
surface moves, `lint`/`test`/`docs` once at the close.

## What the audit found

Healthy after W9: 0 real `TODO`, 0 `#[ignore]`, 3 `#[allow(clippy)]` kinds
left and all justified, `[Unreleased]` empty, ARCHITECTURE lists every crate,
spec touched today, 99 ADRs, 6 first-party plugins + a template under
`plugins/`, `unsafe` only in vfs-local (75 blocks).

| measure | value | note |
| --- | --- | --- |
| biggest test file | `norte-ui-host/tests/controller.rs` 22 088 lines | one binary, one file |
| second | `norte-core/tests/daemon.rs` 9 194 lines | |
| biggest src file | `norte-proto/src/methods.rs` 8 530 | wire types; grows with the protocol |
| longest fn | `norte-tui/src/keys.rs::on_key` 1 183 lines | 20-branch precedence chain, documented `expect` |
| webview | `ui/src/render.ts` 4 387 lines of 6 591 | one TS file paints everything |
| `sleep()` in tests | 66 lines, 12 files | mostly bounded polls |
| `deny.toml` | 8 ignores cargo-deny no longer matched | every `just lint` printed 8 warnings |
| duplicated deps | 92 crates with >1 version | third-party anchored (W9 4.3) |
| remote branches merged into main | 6 | |
| `target/` | 36 GB → 26 GB after `just prune` | 187 stale test exes |

## Tasks

### 10.1 — `deny.toml` stale ignores — `done`

Removed the eight gtk-rs «unmaintained» ignores: under `unmaintained =
"workspace"` (config v2 default) a transitive crate does not count, so they
only produced `advisory-not-detected` noise. A note in the file says where
the argument lives if they ever fire again. `cargo deny check advisories`
clean.

### 10.2 — `mcp/tests/transport.rs` fixed waits — `done`

Three «sleep 100 ms so the copy reaches its Ask» became `esperar_ask(&human)`,
a poll of `policy.pending` with a 5 s deadline — the same poll the sibling
test already had inline, now shared. One negative wait («let a wrong
execution surface») stays. 42/42.

### 10.3 — merged remote branches — `done`

Six branches already in `main` deleted on origin.

### 10.4 — local disk — `done`

`just prune`: 187 stale test executables, 36 → 26 GB.

### 10.5 — split `ui-host/tests/controller.rs` into one binary, many files — `done`

`tests/controller/main.rs` (363 lines: header, shared helpers, the seven
base tests, `mod`s and the glob re-exports) + 18 section files, cut at the
file's own `// ────` headers and grouped by domain (`sync.rs` 4 029,
`transferencias.rs` 2 915, `renombrar.rs` 2 570, `registro.rs` 2 286, …).
Helpers became `pub(super)`; the fake backend is reached through
`#[path = "../backend_falso/mod.rs"]` because three other test binaries
share it. Line count 22 088 → 22 182 (the plumbing), 509/509, clippy clean
— the integration-test crate does not trigger `wildcard_imports`.

22 088 lines in one integration test file. Target shape:
`tests/controller/main.rs` (shared helpers) + `tests/controller/<domain>.rs`
mirroring `src/controller/*`, each `use super::*;`. ONE test binary, not
one per file: every extra integration test file is a full link of the
crate and its 30 GB universe (CLAUDE.md disk budget). Mechanical; the split
script from W9 (methods → files, `pub(super)`) adapts to top-level
`#[tokio::test]` items. `just t norte-ui-host` 418/418 must not change.
Note: `tests/catalogo_del_host.rs` scans `src/controller/*.rs`, not tests —
unaffected.

### 10.6 — same for `core/tests/daemon.rs` (9 194 lines) — `deferred`

Not the same recipe: the file has ONE section header, 184 tests and 254
helpers with no duplicate names. A split needs a hand-made grouping of the
184 tests by method family, which is a reading job, not a script. Leave it
to whoever next adds to the file; the W10 script (`split_tests.py`, in the
session scratchpad, not the repo) does the mechanical part once the groups
are written down.

### 10.7 — `render.ts` — `deferred`

4 387 lines painting every view. Splitting by view is renderer work (TS,
vitest, ADR 0067 boundary), out of a Rust debt wave. Noted for the GUI
owner.

### 10.8 — `keys.rs::on_key` — `dropped`

The 20-branch precedence chain the W9 postmortem already declined to table:
reordering changes `await` order inside the loop. Documented `expect`.

## Log

- 2026-09-08 — audit on the real tree; 10.1–10.5 done on `main`; 10.6
  deferred, 10.7 deferred to the GUI owner, 10.8 dropped. Wave closed.
