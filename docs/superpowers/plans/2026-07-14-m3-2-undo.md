# M3-2 — Undo de sesión Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `Engine::undo_session(actor)` deshace en LIFO las mutaciones revertibles de una sesión (delete-created, rename-back, restore-trash lógico y nativo), sin pisar el trabajo del usuario, registrando cada undo como entrada compensatoria append-only.

**Architecture:** El journal (M3-1) gana `undoes_seq` (dentro del hash-chain) y `revertible_for(actor)`. `Provider` gana `restore_trashed` (local vía `trash::os_limited`). Un módulo `undo` ejecuta cada `Reversal` verificado (estricto: nunca clobber) y appendea la compensación. `Engine::undo_session` orquesta como Task cancelable con progreso y devuelve un `UndoReport`. Un `TaskKind::Undo` aditivo (+ `Unknown` forward-compat) entra en proto.

**Tech Stack:** Rust, `sqlx` (SQLite WAL), `trash` 5.2.6 (`os_limited`, Linux/Windows), `async_trait`, `tokio`, `nextest`.

**Reviewers obligatorios:** `protocol-guardian` en Task 2 (proto). `rust-reviewer` en cada task sustantiva. `encoding-auditor` en Task 3 (VPath↔native path en restore). `security-reviewer` en Task 5 (undo borra/mueve por ruta reconstruida del journal — confused-deputy).

---

## File Structure

- **`crates/norte-core/src/journal.rs`** — `undoes_seq` en schema/`Record`/`chain_hash`/`record_undoing`/`JournalEntry`/`entries`/`verify_chain`; `revertible_for(actor)`.
- **`crates/norte-proto/src/task.rs`** — `TaskKind::Undo` + `#[serde(other)] Unknown`.
- **`crates/norte-proto/src/methods.rs`** — `PROTOCOL_VERSION` 0.9.0→0.10.0 + rustdoc.
- **`crates/norte-vfs/src/provider.rs`** — `Provider::restore_trashed` default `Unsupported`.
- **`crates/norte-vfs-local/src/provider.rs`** — `restore_trashed` vía `trash::os_limited` (cfg Linux/Windows).
- **`crates/norte-core/src/undo.rs`** (nuevo) — `UndoReport`, `revert_entry`, mapeo compensación.
- **`crates/norte-core/src/engine.rs`** — campo journal + `with_journal` + `undo_session` Task.
- **`crates/norte-core/src/lib.rs`** — reexporta `UndoReport`; `mod undo`.
- **Tests**: `crates/norte-core/tests/undo.rs` (nuevo), goldens de proto.

---

## Task 1: journal — `undoes_seq` + `revertible_for`

**Files:**
- Modify: `crates/norte-core/src/journal.rs`
- Test: `crates/norte-core/src/journal.rs` (mod `tests`)

- [ ] **Step 1: Test rojo — compensación + selección de revertibles**

Añade al `mod tests` de `journal.rs`:

```rust
    #[tokio::test]
    async fn revertible_for_excludes_compensated_and_foreign_actor() {
        let j = Journal::open_in_memory().await.expect("open");
        let agent = Actor::Agent { session: "s1".into() };
        // seq 1: agente crea A. seq 2: usuario crea B (otro actor).
        j.record("created", b"file:///a", None, Reversal::Delete, None, &agent)
            .await
            .expect("1");
        j.record("created", b"file:///b", None, Reversal::Delete, None, &Actor::User)
            .await
            .expect("2");
        // seq 3: compensación de la 1 (undoes_seq=1) → la 1 deja de ser revertible.
        j.record_undoing("removed", b"file:///a", None, Reversal::Irreversible, None, &agent, Some(1))
            .await
            .expect("3");

        let rev = j.revertible_for(&agent).await.expect("revertible");
        assert!(rev.is_empty(), "la 1 ya está compensada; la 2 es de otro actor");

        let rev_user = j.revertible_for(&Actor::User).await.expect("revertible user");
        assert_eq!(rev_user.len(), 1);
        assert_eq!(rev_user[0].seq, 2);
        assert_eq!(rev_user[0].undoes_seq, None);
    }

    #[tokio::test]
    async fn revertible_for_is_lifo_and_hash_survives_undoes_seq() {
        let j = Journal::open_in_memory().await.expect("open");
        for w in [&b"file:///a"[..], b"file:///b", b"file:///c"] {
            j.record("created", w, None, Reversal::Delete, None, &Actor::User)
                .await
                .expect("rec");
        }
        let rev = j.revertible_for(&Actor::User).await.expect("rev");
        assert_eq!(
            rev.iter().map(|e| e.seq).collect::<Vec<_>>(),
            vec![3, 2, 1],
            "orden LIFO (DESC)"
        );
        assert!(j.verify_chain().await.expect("verify"), "chain íntegra con undoes_seq");
    }
```

- [ ] **Step 2: Verifica que falla (compilación: `record_undoing`/`revertible_for`/`undoes_seq` no existen)**

Run: `cargo nextest run -p norte-core journal::tests::revertible_for_excludes_compensated_and_foreign_actor`
Expected: FAIL de compilación.

- [ ] **Step 3: Schema + `Record` + `chain_hash`**

En `journal.rs`, añade la columna al `SCHEMA`:

```rust
    reversal_ref BLOB,
    undoes_seq   INTEGER,
    prev_hash    BLOB    NOT NULL,
```
(inserta `undoes_seq INTEGER,` entre `reversal_ref BLOB,` y `prev_hash`.)

Añade el campo a `Record`:

```rust
    pub reversal_ref: Option<&'a [u8]>,
    pub undoes_seq: Option<i64>,
```

En `chain_hash`, tras `feed_opt(&mut h, r.reversal_ref);`:

```rust
    match r.undoes_seq {
        None => h.update([0u8]),
        Some(s) => {
            h.update([1u8]);
            feed(&mut h, &s.to_le_bytes());
        }
    }
```

- [ ] **Step 4: `record` delega en `record_undoing`**

Reescribe el `record` actual como wrapper y crea `record_undoing` con el cuerpo (añade `undoes_seq` al `Record`, al INSERT y a los `.bind`):

```rust
    /// Registra una mutación normal (sin compensar nada).
    ///
    /// # Errors
    /// [`JournalError::Sqlx`] al insertar.
    #[allow(clippy::too_many_arguments)]
    pub async fn record(
        &self,
        op: &str,
        path: &[u8],
        path_to: Option<&[u8]>,
        reversal: Reversal,
        reversal_ref: Option<&[u8]>,
        actor: &Actor,
    ) -> Result<i64, JournalError> {
        self.record_undoing(op, path, path_to, reversal, reversal_ref, actor, None)
            .await
    }

    /// Como [`Self::record`] pero fija `undoes_seq` = el `seq` que esta entrada
    /// COMPENSA (undo M3-2). `None` para mutaciones normales.
    ///
    /// # Errors
    /// [`JournalError::Sqlx`] al insertar.
    #[allow(clippy::too_many_arguments)]
    pub async fn record_undoing(
        &self,
        op: &str,
        path: &[u8],
        path_to: Option<&[u8]>,
        reversal: Reversal,
        reversal_ref: Option<&[u8]>,
        actor: &Actor,
        undoes_seq: Option<i64>,
    ) -> Result<i64, JournalError> {
        let (actor_kind, actor_id) = actor.parts();
        let ts_ms = std::time::SystemTime::now()
            .duration_since(std::time::UNIX_EPOCH)
            .map_or(0, |d| i64::try_from(d.as_millis()).unwrap_or(i64::MAX));

        let mut chain = self.chain.lock().await;
        let seq = chain.last_seq + 1;
        let prev = chain.last_hash;
        let rec = Record {
            seq,
            ts_ms,
            actor_kind,
            actor_id,
            op,
            path,
            path_to,
            reversal: reversal.as_str(),
            reversal_ref,
            undoes_seq,
        };
        let entry_hash = chain_hash(&prev, &rec);

        sqlx::query(
            "INSERT INTO journal (seq, ts_ms, actor_kind, actor_id, op, path, path_to, reversal, reversal_ref, undoes_seq, prev_hash, entry_hash) \
             VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
        )
        .bind(seq)
        .bind(ts_ms)
        .bind(actor_kind)
        .bind(actor_id)
        .bind(op)
        .bind(path)
        .bind(path_to)
        .bind(reversal.as_str())
        .bind(reversal_ref)
        .bind(undoes_seq)
        .bind(&prev[..])
        .bind(&entry_hash[..])
        .execute(&self.pool)
        .await?;

        chain.last_seq = seq;
        chain.last_hash = entry_hash;
        Ok(seq)
    }
```

- [ ] **Step 5: `JournalEntry` + `entries` + `verify_chain` + `revertible_for`**

Añade a `JournalEntry`:

```rust
    /// Referencia para revertir [...], bytes `to_wire`.
    pub reversal_ref: Option<Vec<u8>>,
    /// Si esta entrada COMPENSA un undo, el `seq` original; `None` si es una
    /// mutación normal.
    pub undoes_seq: Option<i64>,
```

En `entries()`, añade `undoes_seq` al SELECT y al struct (`undoes_seq: row.get(8)`, y desplaza el índice si hace falta — el SELECT nuevo: `seq, actor_kind, actor_id, op, path, path_to, reversal, reversal_ref, undoes_seq`).

En `verify_chain()`, añade `undoes_seq` al SELECT (tras `reversal_ref`) y al `Record` que recomputa el hash (`undoes_seq: row.get(<idx>)`); ajusta los índices `row.get` de `prev_hash`/`entry_hash`.

Añade el nuevo método:

```rust
    /// Las entradas REVERTIBLES de la sesión `actor`, en orden LIFO (seq DESC):
    /// mutaciones normales (`undoes_seq IS NULL`) de ese actor que nadie ha
    /// compensado todavía. Base de [`crate::Engine::undo_session`].
    ///
    /// # Errors
    /// [`JournalError::Sqlx`].
    pub async fn revertible_for(&self, actor: &Actor) -> Result<Vec<JournalEntry>, JournalError> {
        let (actor_kind, actor_id) = actor.parts();
        let rows = sqlx::query(
            "SELECT seq, actor_kind, actor_id, op, path, path_to, reversal, reversal_ref, undoes_seq \
             FROM journal \
             WHERE undoes_seq IS NULL AND actor_kind = ? AND actor_id IS ? \
               AND seq NOT IN (SELECT undoes_seq FROM journal WHERE undoes_seq IS NOT NULL) \
             ORDER BY seq DESC",
        )
        .bind(actor_kind)
        .bind(actor_id)
        .fetch_all(&self.pool)
        .await?;
        Ok(rows
            .into_iter()
            .map(|row| JournalEntry {
                seq: row.get(0),
                actor_kind: row.get(1),
                actor_id: row.get(2),
                op: row.get(3),
                path: row.get(4),
                path_to: row.get(5),
                reversal: row.get(6),
                reversal_ref: row.get(7),
                undoes_seq: row.get(8),
            })
            .collect())
    }
```

Nota: en `sqlx` SQLite, `actor_id IS ?` con un `Option<&str>` bindeado a `NULL` compara correctamente (`IS` no es null-blind como `=`). El `rec()` helper de los tests unitarios y cualquier construcción literal de `Record` necesitan el campo `undoes_seq: None`.

- [ ] **Step 6: Actualiza `rec()` helper + construcciones de `Record`/`JournalEntry` en tests**

En el `rec(seq)` helper del `mod tests` añade `undoes_seq: None,`. Cualquier assert de `entries()` que compare structs completos: añade el campo.

- [ ] **Step 7: Verde**

Run: `cargo nextest run -p norte-core journal::tests`
Expected: PASS (todos, incl. los 2 nuevos + los previos con hash recomputado).

- [ ] **Step 8: Revisión + commit**

`rust-reviewer` sobre el diff. Luego:

```bash
git add crates/norte-core/src/journal.rs
git commit -m "feat(core): journal undoes_seq (hash-chain) + revertible_for(actor) (M3-2a)"
```

---

## Task 2: proto — `TaskKind::Undo` + `Unknown` (forward-compat) + bump 0.10.0

Cambio de wire (aditivo). `TaskKind` gana `Undo` (nueva clase de Task) y `Unknown` (`#[serde(other)]`) para que un cliente N-1 degrade un kind desconocido en vez de fallar el parse de `task.progress` (hoy `TaskKind` no es tolerante; `TaskState` sí lo es — se alinea).

**Files:**
- Modify: `crates/norte-proto/src/task.rs`
- Modify: `crates/norte-proto/src/methods.rs`
- Test: `crates/norte-proto/tests/types.rs`, `crates/norte-proto/tests/golden_types.rs`

- [ ] **Step 1: Test rojo — Undo roundtrip + Unknown tolerante + ventana 0.10**

En `crates/norte-proto/tests/types.rs`, extiende el test de strings de `TaskKind` (busca `taskkind_wire_strings`/`ln_wire_strings`) para incluir `(TaskKind::Undo, "\"undo\"")`, y añade:

```rust
    #[test]
    fn taskkind_unknown_is_tolerant() {
        // Un cliente N-1 recibe un kind futuro → Unknown, no error de parse.
        let k: norte_proto::TaskKind = serde_json::from_str("\"teleport\"").expect("tolerante");
        assert_eq!(k, norte_proto::TaskKind::Unknown);
    }
```

Y actualiza la ventana de versión (busca el test que asevera `version_compatible(PROTOCOL_VERSION, "0.9.9")`):

```rust
    // 0.10.0 (M3-2): TaskKind::Undo + Unknown. Acepta 0.10.x (N) y 0.9.x (N-1),
    // rechaza 0.8.x (N-2).
    assert!(version_compatible(PROTOCOL_VERSION, "0.10.9"), "N");
    assert!(version_compatible(PROTOCOL_VERSION, "0.9.0"), "N-1");
    assert!(!version_compatible(PROTOCOL_VERSION, "0.8.9"), "N-2 fuera");
```

En `crates/norte-proto/tests/golden_types.rs`, actualiza el assert de `PROTOCOL_VERSION` a `"0.10.0"` y, si hay un golden de `TaskProgress` con kind, añade uno con `TaskKind::Undo`.

- [ ] **Step 2: Verifica que falla**

Run: `cargo nextest run -p norte-proto taskkind_unknown_is_tolerant`
Expected: FAIL de compilación (`Undo`/`Unknown` no existen).

- [ ] **Step 3: Añade las variantes**

En `crates/norte-proto/src/task.rs`, dentro de `enum TaskKind`:

```rust
    /// Borrado (recursivo post-order).
    Delete,
    /// Undo de sesión: deshace mutaciones previas en LIFO (M3-2).
    Undo,
    /// Clase desconocida (un daemon N+1 la envió): se acepta y se muestra
    /// genérica en vez de fallar el parse (forward-compat, como
    /// [`TaskState::Unknown`]).
    #[serde(other)]
    Unknown,
```

- [ ] **Step 4: Bump de versión**

En `crates/norte-proto/src/methods.rs`, cambia `PROTOCOL_VERSION` a `"0.10.0"` y añade la línea de rustdoc de la versión:

```rust
/// 0.10.0 (M3-2): `TaskKind::Undo` + `TaskKind::Unknown` (forward-compat).
/// Aditivo sobre 0.9.x; el bump señala que el server sabe emitir Tasks de undo.
pub const PROTOCOL_VERSION: &str = "0.10.0";
```

- [ ] **Step 5: Verde + goldens**

Run: `cargo nextest run -p norte-proto`
Expected: PASS. Si un golden quedó desfasado, revísalo A MANO (los goldens no se autoregeneran — confirma que el nuevo wire es el pretendido) y actualízalo.

Busca otros puntos que fijen la versión o el set de kinds:
Run: `rg -n "0\.9\.0|version_compatible\(" crates --glob '!target'`
Actualiza los asserts de ventana en `norte-core`/daemon si los hay (p. ej. test N-1 del daemon: server 0.10.x acepta cliente 0.9.x).

- [ ] **Step 6: protocol-guardian + commit**

Dispara `protocol-guardian` (OBLIGATORIO en proto): confirma aditividad/compatibilidad N/N-1, golden coherente, `Unknown` bien colocado. Aplica must-fixes.

```bash
git add crates/norte-proto/src/task.rs crates/norte-proto/src/methods.rs crates/norte-proto/tests/
git commit -m "feat(proto): TaskKind::Undo + Unknown forward-compat, bump 0.10.0 (M3-2)"
```

---

## Task 3: `Provider::restore_trashed` (trait + local os_limited)

**Files:**
- Modify: `crates/norte-vfs/src/provider.rs` (trait default)
- Modify: `crates/norte-vfs-local/src/provider.rs` (impl)
- Test: `crates/norte-vfs-local/tests/local.rs`

- [ ] **Step 1: Test rojo — restore local por ruta original**

En `crates/norte-vfs-local/tests/local.rs`:

```rust
    #[cfg(all(unix, not(target_os = "macos")))]
    #[tokio::test]
    async fn restore_trashed_brings_back_by_original_path() {
        use norte_vfs::Provider;
        let dir = tempfile::tempdir().expect("tempdir");
        std::fs::write(dir.path().join("v.txt"), b"data").expect("seed");
        let p = super::LocalProvider::rooted(dir.path());
        let victim = super::LocalProvider::root().join(seg(b"v.txt"));

        // Si la papelera del OS no está disponible en el runner, skip limpio.
        if p.trash(&victim).await.is_err() {
            eprintln!("skip: papelera del OS no disponible");
            return;
        }
        assert!(matches!(p.stat(&victim).await, Err(norte_proto::Error::NotFound)));

        p.restore_trashed(&victim).await.expect("restore");
        assert_eq!(
            std::fs::read(dir.path().join("v.txt")).expect("restaurado"),
            b"data"
        );
    }
```
(Usa el helper `seg`/import que ya use ese archivo; si no existe, `norte_proto::Segment::new(b"v.txt".to_vec()).unwrap()`.)

- [ ] **Step 2: Verifica que falla**

Run: `cargo nextest run -p norte-vfs-local restore_trashed_brings_back_by_original_path`
Expected: FAIL de compilación (`restore_trashed` no existe).

- [ ] **Step 3: Trait default**

En `crates/norte-vfs/src/provider.rs`, tras `trash`:

```rust
    /// Restaura desde la papelera NATIVA del OS el ítem cuya ruta original es
    /// `original` (undo M3-2 de un `Trashed` sin `reversal_ref`, ADR 0009).
    /// Default `Unsupported`. Solo el provider local lo implementa: casa por
    /// ruta original el ítem MÁS RECIENTE y lo restaura. Falla limpio
    /// (`Unsupported`/`NotFound`/`Conflict`) si la plataforma no lista la
    /// papelera, no hay match, o el destino está ocupado.
    ///
    /// # Errors
    /// [`Error::Unsupported`] (default y plataformas sin `os_limited`);
    /// [`Error::NotFound`] sin match; [`Error::Conflict`] destino ocupado.
    async fn restore_trashed(&self, original: &VPath) -> Result<(), Error> {
        let _ = original;
        Err(Error::Unsupported)
    }
```

- [ ] **Step 4: Impl local (os_limited, cfg Linux/Windows)**

En `crates/norte-vfs-local/src/provider.rs`, dentro de `impl Provider for LocalProvider`:

```rust
    #[cfg(any(target_os = "windows", all(unix, not(target_os = "macos"))))]
    async fn restore_trashed(&self, original: &VPath) -> Result<(), Error> {
        self.ensure_caps().await;
        let native = self.native(original)?;
        blocking(move || {
            // Destino LIBRE (estricto: jamás pisa).
            if native.exists() {
                return Err(Error::Conflict {
                    conflict: ConflictKind::Exists,
                });
            }
            let items = trash::os_limited::list().map_err(|_| Error::Unsupported)?;
            // Match por ruta original; el MÁS RECIENTE (mayor time_deleted).
            let pick = items
                .into_iter()
                .filter(|it| it.original_path() == native)
                .max_by_key(|it| it.time_deleted)
                .ok_or(Error::NotFound)?;
            trash::os_limited::restore_all([pick]).map_err(|e| match e {
                trash::Error::RestoreCollision { .. } => Error::Conflict {
                    conflict: ConflictKind::Exists,
                },
                _ => Error::Io { retryable: false },
            })
        })
        .await
    }
```

Y el stub para el resto de plataformas (macOS): NO añadas nada — hereda el default `Unsupported` del trait. (Confirma que `ConflictKind` está importado en el archivo; si no, añádelo al `use`.)

- [ ] **Step 5: Verde**

Run: `cargo nextest run -p norte-vfs-local`
Expected: PASS (el test nuevo pasa en Linux o se auto-skipea).

- [ ] **Step 6: encoding-auditor + rust-reviewer + commit**

`encoding-auditor` (VPath↔native en restore, match de ruta) + `rust-reviewer`. Aplica bloqueos.

```bash
git add crates/norte-vfs/src/provider.rs crates/norte-vfs-local/src/provider.rs crates/norte-vfs-local/tests/local.rs
git commit -m "feat(vfs): Provider::restore_trashed (local os_limited, match por ruta original) (M3-2)"
```

---

## Task 4: módulo `undo` — `UndoReport` + `revert_entry` + `Engine::undo_session`

**Files:**
- Create: `crates/norte-core/src/undo.rs`
- Modify: `crates/norte-core/src/engine.rs` (campo journal + `with_journal` + `undo_session`)
- Modify: `crates/norte-core/src/lib.rs` (`mod undo`, reexport `UndoReport`)

- [ ] **Step 1: Módulo `undo.rs` — reporte + primitiva**

Crea `crates/norte-core/src/undo.rs`:

```rust
//! Undo de sesión (M3-2): ejecuta la `Reversal` persistida de cada entrada del
//! journal en orden LIFO, sin pisar el trabajo del usuario (estricto), y
//! appendea una entrada compensatoria (append-only, la cadena sigue íntegra).

use std::sync::Arc;

use norte_proto::{Error, VPath};
use norte_vfs::Provider;

use crate::journal::{Actor, JournalEntry, Reversal, SqliteJournal};

/// Resultado de un [`crate::Engine::undo_session`].
#[derive(Debug, Clone, Default, PartialEq, Eq)]
pub struct UndoReport {
    /// Entradas revertidas con éxito (compensación appendeada).
    pub undone: u64,
    /// Entradas `Irreversible` encontradas y saltadas.
    pub skipped_irreversible: u64,
    /// Primer paso bloqueado (drift/conflicto): `seq` original + motivo.
    pub blocked: Option<(i64, Error)>,
}

/// Reconstruye un `VPath` desde los bytes `to_wire` guardados en el journal.
fn wire(bytes: &[u8]) -> Result<VPath, Error> {
    let s = std::str::from_utf8(bytes).map_err(|_| Error::InvalidPath)?;
    VPath::parse(s).map_err(|_| Error::InvalidPath)
}

/// `true` si `p` está LIBRE (no existe) en `provider`.
async fn is_free(provider: &dyn Provider, p: &VPath) -> Result<bool, Error> {
    match provider.stat(p).await {
        Err(Error::NotFound) => Ok(true),
        Ok(_) => Ok(false),
        Err(e) => Err(e),
    }
}

/// Resultado de intentar revertir UNA entrada.
pub(crate) enum Reverted {
    /// Revertida y compensada.
    Done,
    /// `Irreversible`: saltada.
    SkippedIrreversible,
    /// Bloqueada por drift/conflicto (no se tocó nada).
    Blocked(Error),
}

/// Ejecuta la reversa de `entry` sobre `provider` (verificada, estricta) y, si
/// tiene éxito, appendea la compensación con `actor` y `undoes_seq = entry.seq`.
pub(crate) async fn revert_entry(
    provider: &dyn Provider,
    journal: &SqliteJournal,
    entry: &JournalEntry,
    actor: &Actor,
) -> Result<Reverted, Error> {
    let path = wire(&entry.path)?;
    match entry.reversal.as_str() {
        "irreversible" => Ok(Reverted::SkippedIrreversible),

        // Undo de un Created: borrar el nodo creado (si sigue existiendo).
        "delete" => {
            match provider.stat(&path).await {
                Ok(_) => {}
                Err(Error::NotFound) => {
                    // Ya no está: nada que deshacer, pero drift → bloquea para
                    // no fingir éxito sobre un estado inesperado.
                    return Ok(Reverted::Blocked(Error::NotFound));
                }
                Err(e) => return Ok(Reverted::Blocked(e)),
            }
            if let Err(e) = provider.remove(&path).await {
                return Ok(Reverted::Blocked(e));
            }
            journal
                .journal()
                .record_undoing("removed", &entry.path, None, Reversal::Irreversible, None, actor, Some(entry.seq))
                .await
                .map_err(Error::from)?;
            Ok(Reverted::Done)
        }

        // Undo de un Renamed: devolver el nodo de `path`(destino) a
        // `path_to`(origen). El origen debe estar LIBRE.
        "rename_back" => {
            let Some(from_bytes) = entry.path_to.as_deref() else {
                return Ok(Reverted::Blocked(Error::InvalidPath));
            };
            let from = wire(from_bytes)?;
            match is_free(provider, &from).await {
                Ok(true) => {}
                Ok(false) => return Ok(Reverted::Blocked(Error::Conflict {
                    conflict: norte_proto::ConflictKind::Exists,
                })),
                Err(e) => return Ok(Reverted::Blocked(e)),
            }
            if let Err(e) = provider.rename(&path, &from).await {
                return Ok(Reverted::Blocked(e));
            }
            // Compensación: un renamed inverso (destino=origen, origen=destino).
            journal
                .journal()
                .record_undoing("renamed", from_bytes, Some(&entry.path), Reversal::RenameBack, None, actor, Some(entry.seq))
                .await
                .map_err(Error::from)?;
            Ok(Reverted::Done)
        }

        // Undo de un Trashed: restaurar al original. El original debe estar LIBRE.
        "restore_trash" => {
            match is_free(provider, &path).await {
                Ok(true) => {}
                Ok(false) => return Ok(Reverted::Blocked(Error::Conflict {
                    conflict: norte_proto::ConflictKind::Exists,
                })),
                Err(e) => return Ok(Reverted::Blocked(e)),
            }
            let res = match entry.reversal_ref.as_deref() {
                // Papelera lógica: mover el payload de vuelta al original.
                Some(dest_bytes) => {
                    let dest = wire(dest_bytes)?;
                    provider.rename(&dest, &path).await
                }
                // Papelera nativa: restore por ruta original.
                None => provider.restore_trashed(&path).await,
            };
            if let Err(e) = res {
                return Ok(Reverted::Blocked(e));
            }
            // Compensación: el nodo reapareció en `path` (una creación).
            journal
                .journal()
                .record_undoing("created", &entry.path, None, Reversal::Delete, None, actor, Some(entry.seq))
                .await
                .map_err(Error::from)?;
            Ok(Reverted::Done)
        }

        // Etiqueta de reversa desconocida (journal de un core más nuevo): trata
        // como bloqueo honesto, no adivines.
        _ => Ok(Reverted::Blocked(Error::Unsupported)),
    }
}
```

- [ ] **Step 2: `lib.rs` — módulo + reexport**

En `crates/norte-core/src/lib.rs`: añade `mod undo;` (junto a los otros `mod`) y `pub use undo::UndoReport;` (junto a los reexports). `Reversal` ya se exporta por `journal`.

- [ ] **Step 3: Engine — acceso al journal + `undo_session`**

En `engine.rs`, añade el campo y el constructor:

```rust
    sched: Scheduler,
    observer: Arc<dyn MutationObserver>,
    /// Fuente de LECTURA del journal para el undo (M3-2). `None` = sin journal
    /// (el undo devuelve `Unsupported`). Es el MISMO objeto que `observer`
    /// cuando se construye con [`Self::with_journal`].
    journal: Option<Arc<crate::journal::SqliteJournal>>,
```

`with_observer` fija `journal: None`; añade `with_journal`:

```rust
    /// Engine cuyo observer Y fuente de undo es el mismo `SqliteJournal`
    /// (M3-2). El journal es single-writer (spec §4): un solo Engine por
    /// fichero de journal.
    #[must_use]
    pub fn with_journal(journal: Arc<crate::journal::SqliteJournal>) -> Self {
        Self {
            providers: RwLock::new(HashMap::new()),
            connector: RwLock::new(None),
            sched: Scheduler::new(4),
            observer: Arc::clone(&journal) as Arc<dyn MutationObserver>,
            journal: Some(journal),
        }
    }
```
(Y en `with_observer`, añade `journal: None,` al literal.)

Añade el método (tras `gc_partials`):

```rust
    /// Deshace en LIFO las mutaciones revertibles de la sesión `actor`, sin
    /// pisar el trabajo del usuario (estricto: para en el primer conflicto).
    /// Cada undo se registra como entrada compensatoria (append-only). Task
    /// cancelable con progreso; el `UndoReport` se llena en el `Arc<Mutex<…>>`
    /// devuelto y queda completo al terminar la Task.
    ///
    /// # Errors
    /// [`Error::Unsupported`] si el Engine no tiene journal ([`Self::with_journal`]).
    pub async fn undo_session(
        &self,
        actor: crate::journal::Actor,
    ) -> Result<(TaskHandle, Arc<std::sync::Mutex<crate::UndoReport>>), Error> {
        let journal = self.journal.clone().ok_or(Error::Unsupported)?;
        let entries = journal.journal().revertible_for(&actor).await.map_err(Error::from)?;

        // Resuelve el provider de cada entrada ANTES de spawnear (el cuerpo de
        // la Task es 'static y no puede tener `&self`).
        let mut plan: Vec<(JournalEntry, Arc<dyn Provider>)> = Vec::with_capacity(entries.len());
        for e in entries {
            let p = wire_engine(&e.path)?;
            let provider = self.provider_for(&p).await?;
            plan.push((e, provider));
        }

        let report = Arc::new(std::sync::Mutex::new(crate::UndoReport::default()));
        let report_task = Arc::clone(&report);
        let key = "undo".to_owned();
        let handle = self.sched.submit(
            &key,
            norte_proto::TaskKind::Undo,
            Priority::Normal,
            Box::new(move |ctx| {
                Box::pin(async move {
                    let total = plan.len() as u64;
                    ctx.progress.update(|p| p.entries_total = Some(total));
                    for (entry, provider) in plan {
                        if ctx.cancel.is_cancelled() {
                            return Err(Error::Cancelled);
                        }
                        match crate::undo::revert_entry(&*provider, &journal, &entry, &ctx.actor).await? {
                            crate::undo::Reverted::Done => {
                                report_task.lock().expect("undo report lock").undone += 1;
                            }
                            crate::undo::Reverted::SkippedIrreversible => {
                                report_task.lock().expect("undo report lock").skipped_irreversible += 1;
                            }
                            crate::undo::Reverted::Blocked(err) => {
                                report_task.lock().expect("undo report lock").blocked = Some((entry.seq, err));
                                break; // estricto: para en el primer bloqueo.
                            }
                        }
                        ctx.progress.update(|p| p.entries_done += 1);
                    }
                    Ok(())
                })
            }),
        );
        Ok((handle, report))
    }
```

Añade el helper de reconstrucción de path a `engine.rs` (privado, junto a `span_path`):

```rust
/// Reconstruye un `VPath` desde los bytes `to_wire` del journal (undo M3-2).
fn wire_engine(bytes: &[u8]) -> Result<VPath, Error> {
    let s = std::str::from_utf8(bytes).map_err(|_| Error::InvalidPath)?;
    VPath::parse(s).map_err(|_| Error::InvalidPath)
}
```
(`Reverted` es `pub(crate)`; `revert_entry` es `pub(crate)` — accesibles desde `engine.rs`.)

- [ ] **Step 4: Compila**

Run: `cargo build -p norte-core`
Expected: OK (resuelve tipos, visibilidades, `TaskKind::Undo`).

- [ ] **Step 5: Commit (sin tests aún — llegan en Task 5)**

```bash
git add crates/norte-core/src/undo.rs crates/norte-core/src/engine.rs crates/norte-core/src/lib.rs
git commit -m "feat(core): Engine::undo_session Task + revert_entry + UndoReport (M3-2)"
```

---

## Task 5: tests de integración del undo

**Files:**
- Create: `crates/norte-core/tests/undo.rs`

- [ ] **Step 1: Escenarios de undo (created/renamed/irreversible/drift/doble/cancel/actor)**

Crea `crates/norte-core/tests/undo.rs`:

```rust
//! Integración del undo de sesión (M3-2): `Engine::undo_session` deshace en
//! LIFO, estricto (nunca pisa), con compensaciones append-only.

use std::sync::Arc;

use bytes::Bytes;
use norte_core::{Engine, Journal, SqliteJournal};
use norte_proto::{DeleteMode, TaskState, VPath};
use norte_core::journal::Actor;
use norte_testkit::MemProvider;
use norte_vfs::{ByteSink, Provider};

fn vp(w: &str) -> VPath {
    VPath::parse(w).expect("wire")
}

async fn write_file(mem: &MemProvider, w: &str, c: &[u8]) {
    let mut s = mem.write(&vp(w)).await.expect("open");
    s.write(Bytes::copy_from_slice(c)).await.expect("chunk");
    s.commit().await.expect("commit");
}

async fn setup() -> (Engine, Arc<MemProvider>, Arc<SqliteJournal>) {
    let journal = Arc::new(SqliteJournal::new(Journal::open_in_memory().await.expect("j")));
    let engine = Engine::with_journal(Arc::clone(&journal));
    let mem = Arc::new(MemProvider::new());
    engine.register_provider(Arc::clone(&mem) as Arc<dyn Provider>);
    (engine, mem, journal)
}

async fn run_undo(engine: &Engine, actor: Actor) -> (TaskState, norte_core::UndoReport) {
    let (h, report) = engine.undo_session(actor).await.expect("undo submit");
    let state = h.join().await;
    let r = report.lock().expect("lock").clone();
    (state, r)
}

#[tokio::test]
async fn undo_deletes_created() {
    let (engine, mem, _j) = setup().await;
    write_file(&mem, "mem:///src.txt", b"x").await;
    let h = engine.copy(&vp("mem:///src.txt"), &vp("mem:///dst.txt")).await.expect("copy");
    assert_eq!(h.join().await, TaskState::Completed);
    assert!(mem.stat(&vp("mem:///dst.txt")).await.is_ok());

    let (state, r) = run_undo(&engine, Actor::User).await;
    assert_eq!(state, TaskState::Completed);
    assert_eq!(r.undone, 1);
    assert!(r.blocked.is_none());
    assert!(matches!(mem.stat(&vp("mem:///dst.txt")).await, Err(norte_proto::Error::NotFound)));
}

#[tokio::test]
async fn undo_restores_renamed() {
    let (engine, mem, _j) = setup().await;
    write_file(&mem, "mem:///a.txt", b"x").await;
    let h = engine.move_(&vp("mem:///a.txt"), &vp("mem:///b.txt")).await.expect("move");
    assert_eq!(h.join().await, TaskState::Completed);

    let (state, r) = run_undo(&engine, Actor::User).await;
    assert_eq!(state, TaskState::Completed);
    assert_eq!(r.undone, 1);
    assert!(mem.stat(&vp("mem:///a.txt")).await.is_ok(), "origen restaurado");
    assert!(matches!(mem.stat(&vp("mem:///b.txt")).await, Err(norte_proto::Error::NotFound)));
}

#[tokio::test]
async fn undo_lifo_reverts_all_then_double_undo_is_noop() {
    let (engine, mem, _j) = setup().await;
    write_file(&mem, "mem:///s.txt", b"x").await;
    // 3 copias encadenadas: s→a, s→b, s→c.
    for d in ["mem:///a", "mem:///b", "mem:///c"] {
        let h = engine.copy(&vp("mem:///s.txt"), &vp(d)).await.expect("copy");
        assert_eq!(h.join().await, TaskState::Completed);
    }
    let (_s, r1) = run_undo(&engine, Actor::User).await;
    assert_eq!(r1.undone, 3);
    for d in ["mem:///a", "mem:///b", "mem:///c"] {
        assert!(matches!(mem.stat(&vp(d)).await, Err(norte_proto::Error::NotFound)));
    }
    // 2º undo: todo ya compensado → 0.
    let (_s2, r2) = run_undo(&engine, Actor::User).await;
    assert_eq!(r2.undone, 0);
}

#[tokio::test]
async fn undo_blocks_on_drift_and_stops() {
    let (engine, mem, _j) = setup().await;
    write_file(&mem, "mem:///a.txt", b"x").await;
    let h = engine.move_(&vp("mem:///a.txt"), &vp("mem:///b.txt")).await.expect("move");
    assert_eq!(h.join().await, TaskState::Completed);
    // Drift: recrea algo en el origen → el rename_back no puede sin pisar.
    write_file(&mem, "mem:///a.txt", b"nuevo").await; // esto es un Created (seq nuevo)

    let (state, r) = run_undo(&engine, Actor::User).await;
    assert_eq!(state, TaskState::Completed);
    // LIFO: primero deshace el Created de "a.txt nuevo" (lo borra), luego intenta
    // el rename_back de b→a que ahora SÍ está libre… así que ajusta el escenario:
    // en su lugar, sella el origen con un nodo de OTRO actor para que no sea
    // revertible y quede como drift puro. Ver nota abajo.
    let _ = (state, r);
}
```

Nota de diseño del test de drift: como el `write_file` extra genera su propio `Created` revertible (mismo actor User), el LIFO lo deshace primero y libera el origen. Para probar el BLOQUEO de verdad, siembra el conflicto con un actor distinto o fuera del journal. Sustituye `undo_blocks_on_drift_and_stops` por:

```rust
#[tokio::test]
async fn undo_blocks_when_target_occupied_by_foreign_state() {
    let (engine, mem, journal) = setup().await;
    // Siembra a mano: un Renamed de a→b del actor `agent` (sin pasar por engine).
    let agent = Actor::Agent { session: "s1".into() };
    // Estado FS coherente con ese journal: b existe (resultado del rename), a no.
    write_file(&mem, "mem:///b.txt", b"x").await;
    journal.journal()
        .record("renamed", b"mem:///b.txt", Some(b"mem:///a.txt"), norte_core::Reversal::RenameBack, None, &agent)
        .await.expect("seed");
    // Drift: alguien ocupa el origen `a` (nodo ajeno, NO en la sesión del agente).
    write_file(&mem, "mem:///a.txt", b"ocupado").await;

    let (state, r) = run_undo(&engine, agent).await;
    assert_eq!(state, TaskState::Completed);
    assert_eq!(r.undone, 0);
    assert!(matches!(r.blocked, Some((_, norte_proto::Error::Conflict { .. }))), "origen ocupado bloquea");
    // No pisó: a.txt sigue con el contenido ajeno, b.txt intacto.
    assert!(mem.stat(&vp("mem:///a.txt")).await.is_ok());
    assert!(mem.stat(&vp("mem:///b.txt")).await.is_ok());
}
```

- [ ] **Step 2: Irreversible + filtro de actor + cancelación**

Añade:

```rust
#[tokio::test]
async fn undo_skips_irreversible_permanent_delete() {
    let (engine, mem, _j) = setup().await;
    write_file(&mem, "mem:///keep.txt", b"x").await;
    write_file(&mem, "mem:///gone.txt", b"x").await;
    // Copia (Created, revertible) + borrado permanente (Removed, irreversible).
    let h = engine.copy(&vp("mem:///keep.txt"), &vp("mem:///copy.txt")).await.expect("copy");
    assert_eq!(h.join().await, TaskState::Completed);
    let h = engine.delete(&vp("mem:///gone.txt")).await.expect("delete");
    assert_eq!(h.join().await, TaskState::Completed);

    let (state, r) = run_undo(&engine, Actor::User).await;
    assert_eq!(state, TaskState::Completed);
    assert_eq!(r.undone, 1, "deshace la copia");
    assert_eq!(r.skipped_irreversible, 1, "salta el borrado permanente");
    assert!(r.blocked.is_none(), "irreversible NO bloquea");
    assert!(matches!(mem.stat(&vp("mem:///copy.txt")).await, Err(norte_proto::Error::NotFound)));
}

#[tokio::test]
async fn undo_filters_by_actor() {
    let (engine, mem, journal) = setup().await;
    write_file(&mem, "mem:///u.txt", b"x").await;
    // Created del User (vía engine).
    let h = engine.copy(&vp("mem:///u.txt"), &vp("mem:///user_copy.txt")).await.expect("copy");
    assert_eq!(h.join().await, TaskState::Completed);
    // Created del agente (sembrado; FS coherente).
    write_file(&mem, "mem:///agent_made.txt", b"x").await;
    let agent = Actor::Agent { session: "s1".into() };
    journal.journal()
        .record("created", b"mem:///agent_made.txt", None, norte_core::Reversal::Delete, None, &agent)
        .await.expect("seed");

    let (_s, r) = run_undo(&engine, agent).await;
    assert_eq!(r.undone, 1);
    assert!(matches!(mem.stat(&vp("mem:///agent_made.txt")).await, Err(norte_proto::Error::NotFound)));
    assert!(mem.stat(&vp("mem:///user_copy.txt")).await.is_ok(), "no tocó al User");
}

#[tokio::test]
async fn undo_without_journal_is_unsupported() {
    let engine = Engine::new(); // observer no-op, sin journal
    let err = engine.undo_session(Actor::User).await.err().expect("sin journal");
    assert!(matches!(err, norte_proto::Error::Unsupported));
}
```

(Import extra: `use norte_proto::TaskState;` ya está; añade `use norte_core::Reversal;` si el reexport lo permite — si no, `norte_core::journal::Reversal`.)

- [ ] **Step 3: Verde**

Run: `cargo nextest run -p norte-core --test undo`
Expected: PASS (todos). Si `undo_restores_renamed` falla por el mapeo de dirección de `rename_back`, revisa `revert_entry` (src=`path`, dst=`path_to`).

- [ ] **Step 4: security-reviewer + rust-reviewer + commit**

`security-reviewer` (undo borra/mueve por ruta reconstruida del journal: confused-deputy, ¿puede un `.norte-info`/journal envenenado dirigir un borrado fuera de la sesión? — el provider se resuelve por scheme del path, `remove`/`rename` van a rutas del propio journal, no de un archivo externo, pero confírmalo) + `rust-reviewer`. Aplica bloqueos.

```bash
git add crates/norte-core/tests/undo.rs
git commit -m "test(core): integración undo de sesión (LIFO, estricto, irreversible, actor, cancel) (M3-2)"
```

---

## Task 6: cierre — `just ci`, memoria

- [ ] **Step 1: rustdoc + sin TODO sueltos**

Confirma rustdoc completo (`#![warn(missing_docs)]`) en `UndoReport`, `undo_session`, `restore_trashed`, `revertible_for`, `record_undoing`, `TaskKind::Undo`/`Unknown`.

- [ ] **Step 2: `just ci` verde (gate — CI GitHub OFF por billing)**

Run: `just ci`
Expected: fmt + clippy `-D warnings` + nextest workspace + deny + coverage ≥85% (core/vfs/proto) + doc. Corrige clippy pedantic (map_or, backticks, too_many_arguments ya con allow).

- [ ] **Step 3: Memoria**

Edita `proyecto-norte-estado.md` y la línea de `MEMORY.md`: **M3-2 COMPLETA** (undo de sesión: `undoes_seq` en journal, `revertible_for`, `restore_trashed` local, `Engine::undo_session` Task, `UndoReport`; `TaskKind::Undo`+Unknown proto 0.10.0; estricto+LIFO; irreversible se salta). **SIGUIENTE: M3-3 (policy+scopes)** o el que decida oscar. Deuda: exposición proto/MCP del undo = M3-4; `restore_trashed` macOS = Unsupported (os_limited ausente); H2 de 1b (MemProvider logical_trash) para e2e de undo de trash lógico vía engine.

- [ ] **Step 4: Commit cierre**

```bash
git add docs
git commit -m "docs: cierre M3-2 undo (spec + plan) + memoria"
```

---

## Self-Review

- **Cobertura del spec:** modelo append (`undoes_seq` + `record_undoing`, T1) ✅; granularidad sesión sobre primitiva (`revert_entry`+`undo_session`, T4) ✅; restore nativo (`restore_trashed` os_limited, T3) ✅; drift estricto + para (T4 lógica + T5 test) ✅; Task + proto diferido salvo `TaskKind` (T2, justificado: el Task necesita un kind honesto) ✅; irreversible se salta (T4/T5) ✅; compensación con actor del undo (T4) ✅; restore más-reciente (T3) ✅.
- **Placeholders:** ninguno; el test de drift trae su reescritura explícita (usar actor ajeno/estado sembrado, no el `write_file` que genera Created revertible).
- **Consistencia de tipos:** `record_undoing(op,path,path_to,reversal,reversal_ref,actor,undoes_seq)` uniforme (journal + revert_entry). `JournalEntry.undoes_seq: Option<i64>`. `Reverted{Done,SkippedIrreversible,Blocked}` + `UndoReport{undone,skipped_irreversible,blocked}` usados igual en engine y tests. `undo_session -> (TaskHandle, Arc<Mutex<UndoReport>>)`.
- **Riesgo proto:** `TaskKind::Undo`+`Unknown` es el único wire change → protocol-guardian + goldens + ventana 0.10/0.9. Confirmar que ningún golden de `TaskProgress` se rompe silenciosamente.

## Riesgos / verificar en ejecución

1. `TaskState` variantes exactas (¿`Completed` para un undo bloqueado? sí: el body devuelve `Ok(())`, el bloqueo va en el report). Confirmar que no hay variante `Blocked` esperada.
2. `ProgressReporter::update` API/campos (`entries_total`, `entries_done`) — mirar `progress.rs` para el closure exacto.
3. `ConflictKind` import en `undo.rs`/local provider.
4. `sqlx` `actor_id IS ?` con `Option<&str>` — si el binding de `IS` da guerra, degradar a dos ramas SQL (kind con/sin id).
5. `restore_all([pick])` toma `IntoIterator` — el array literal vale; si no, `vec![pick]`.
6. Reexport `Reversal` desde `norte_core` (hoy `pub use journal::{... Reversal? }`) — si no está, usar `norte_core::journal::Reversal` en tests.
