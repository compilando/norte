# T05 — files directly in `crates/norte-core/tests/`

Scope: every `.rs` file directly under `crates/norte-core/tests/` (NOT `daemon/`
or `origen_a_peticion/`, owned by T04). All in-scope files translated (comments,
rustdoc, tracing/panic/assert/error text, local identifiers, local test names).

## 1. Pub renames (found, NOT renamed — owned elsewhere)

- `spawn_raiz` — cited in `spans_en_spawn.rs`'s allowlist comment; defined in
  `norte-core/src/blocking.rs` (not mine). Candidate: `spawn_root`.
- `Lane::Cola`, `submit_en`, `mover_en_cola` — used in `scheduler.rs`; pub API
  of the scheduler (`norte-core/src/scheduler.rs`). Candidates: `Lane::Queue`,
  `submit_in_lane`, `move_in_queue`.
- `CATALOGO`, `norte_proto::catalog::buscar` — used in `catalogo_rpc.rs`; pub
  items in `crates/norte-proto/src/catalog.rs` (T06/T07 territory). Candidates:
  `CATALOG`, `find`.
- `norte_core::connect::Causa` (struct) and its `causa` field — used in
  `connect.rs`; pub in `crates/norte-core/src/connect.rs`. Candidate: `Cause`
  / `cause` (already partly English elsewhere; the struct name itself is the
  gap).
- `ColumnPool::reutilizadas()` — used in `columns_git_e2e.rs`; pub (behind
  `#[cfg(any(test, feature = "testing"))]`) in `norte-core/src/plugins.rs`
  around line 4802. Candidate: `reused()`.
- `TestDaemon::spawn_daemon_journal` — NOTE, discovered only via the final
  `cargo check`: `tests/daemon/policy.rs:3783` calls
  `spawn_daemon_con_anillo()`/`hacia_el_anillo()`, which no longer exist —
  T04's in-flight renaming (background agent still running at report time)
  left these two call sites stale. Not mine to fix (daemon/ is T04's), flagging
  so it lands on their list. Same for `esperar_id` (4 call sites) and the
  `hasta!` macro (`tests/daemon/policy.rs`, `tests/daemon/main.rs`) — visibly
  mid-rename by T04's agent.

## 2. Cross-file literals (kept as-is, Spanish bytes preserved)

- `"hola norte\n"`, `"vacio.txt"` — hardcoded in
  `crates/norte-plugin-host/examples-wasm/provider-mem/src/lib.rs` (and
  `provider-mem-rw/src/lib.rs`). Used in `plugin_provider_e2e.rs`,
  `plugin_provider_installed_e2e.rs`, `plugin_provider_rw_e2e.rs`.
- `"sin-extension"` — hardcoded in `plugins/by-extension/src/lib.rs`. Used in
  `organizer_by_extension_e2e.rs`.
- `"aguja "` (part of the `preview_bidi_ctrl_injection` fixture) — hardcoded in
  `crates/norte-testkit/src/corpus.rs`. Used in `engine_search.rs`.
- `"hola"` / `"adios"` — SHA-256 hardcoded in `backend_paridad.rs`
  (`sha256("hola") == "b221d9db…"`); translating the literal would silently
  break the assertion, so it stays with a NOTE comment.
- Citation to `un_symlink_intermedio_no_redirige_un_borrado_fuera_de_la_raiz`
  (still Spanish-named, lives in `crates/norte-vfs-local/tests/confined.rs`,
  outside T05) — referenced from a doc comment in `engine_leaf_confined.rs`.
- Citation to `resolve_provider_nunca_sirve_un_scheme_del_core` in a comment —
  checked via `rg`, this function does not exist anywhere in the tree; it is a
  stale/dead reference from before some refactor. Left translated as prose
  (it's just a name inside a comment, not a live symbol) — flagging in case
  someone wants to fix the comment itself.
- `mod origen_a_peticion; use origen_a_peticion::{Mando, OrigenAPeticion};` and
  the calls `mando.empezo()`, `mando.sigue()`, `OrigenAPeticion::nuevo(...)` —
  T04-owned shared test helper (`crates/norte-core/tests/origen_a_peticion/mod.rs`).
  Used in `engine_destino_que_desaparece.rs` and
  `engine_sync_destino_que_desaparece.rs`. Kept untouched.

## 3. UI strings outside Fluent

None. Everything in this scope is backend/core integration tests, not
UI-facing.

## 4. Spanish file/module names in this task's scope (reported, not renamed)

- `catalogo_rpc.rs`
- `engine_destino_que_desaparece.rs`
- `engine_sync_destino_que_desaparece.rs`
- `engine_m2_fase1.rs`
- `engine_undo_de_copia_fallida.rs`
- `backend_paridad.rs`
- `rename_solo_caja.rs`

## 5. Glossary

No new terms needed beyond `TRANSLATION_GLOSSARY.md`; the vocabulary here
(journal, lock, busy, brake/backoff, spool, plan, batch, anchor, taxonomy) was
already covered or translated compositionally (e.g. "freno de reintento" →
"retry brake", "veredicto" → "verdict", "ocupante de paso" → "passing
occupant").

## 6. `TODO(translation)` markers added

0. No comment in this scope was stale/wrong enough to need the marker; a few
   pre-existing structural oddities (e.g. a misplaced doc comment sitting above
   an unrelated test in `backend_remote.rs`, documenting the wrong function)
   were translated faithfully in place, without restructuring, since the rule
   is translate-as-is, not fix bugs.

## 7. `cargo check -p norte-core --all-targets --features testing`

Ran once, at the end (feature `testing` is required — it's what `just`
recipes use — a plain `--all-targets` without it fails on unrelated
`#[cfg(feature = "testing")]` gated items like `ColumnPool::reutilizadas`,
which is a pre-existing gate, not something translation touched).

Result: **zero errors attributable to T05's files.** All remaining errors at
check time were in territory owned by other in-flight agents:
- `crates/norte-core/src/sync/exec.rs:1807` (`cannot find value 'igual'`) — T03
  ("Translate T03 core rest"), still running.
- `crates/norte-core/tests/daemon/policy.rs` and `.../daemon/main.rs`
  (`esperar_id`, `hasta!`, `spawn_daemon_con_anillo`, `hacia_el_anillo`) — T04
  ("Translate T04 core daemon"), still running.

These are expected mid-flight breakage from concurrent background agents
editing files outside T05's scope, not regressions from this task. Every file
directly under `crates/norte-core/tests/` (excluding `daemon/` and
`origen_a_peticion/`) is now fully translated and, as far as compilation
touching only those files goes, clean.
