# T24 — crates/norte-connect, norte-testkit, norte-mcp, norte-term, norte-vfs-object, norte-vfs-rar, norte-ai, norte-encoding

## 1. `pub` renames — proposed, not applied (cross the file boundary)

- **`norte-term`: the entire public API is Spanish-named** and was deliberately left unrenamed (only doc comments translated), per the phase-1 rule that cross-file/public renames are reported, not applied. Candidates for a phase-2 pass:
  - `Pantalla` → `Screen` (struct, and its methods `nueva`→`new`, `alimentar`→`feed`, `tamano`→`size`, `cursor_visible`, `celda`→`cell`, `fila_tramos`→`row_spans`, `fila_texto`→`row_text`, `redimensionar`→`resize`; `cursor` stays).
  - `Celda` → `Cell`, `Estilo` → `Style`.
  - `ColorTerm` variants `PorDefecto`/`Indexado`/`Rgb` → `Default`/`Indexed`/`Rgb` (note: `Default` would collide with the `Default` trait's associated meaning in a `match`, so a phase-2 pass should check for a better name, e.g. `Plain`).
  - `pty::Arranque` → `Startup`/`Launch` (fields `programa`/`dir`/`tam`/`env` → `program`/`dir`/`size`/`env`).
  - `pty::Shell` methods `abrir`/`bombear`/`escribir`/`redimensionar`/`muerto`/`pantalla`/`matar` → `open`/`pump`/`write`/`resize`/`dead`/`screen`/`kill`.
  - `pty::Responder` type alias — name itself is already English-legible but worth a second look.
  - `TERM` const is already English.
  - These were checked with `rg -w` and are used across `norte-tui` (the renderer bridge) — a rename here is a cross-crate change, out of scope for phase 1.
- No other `pub`/`pub(crate)`/`pub(super)` items needing a rename were found in the other seven crates — their public surfaces were already English before this pass (confirmed via `rg` for each renamed private item to ensure it stayed file-local: `Delegate::discover`/`discover_in`/`pinned`/`program`/`list_argv`/`read_argv`/`run_capture`/`run_stream` in `norte-vfs-rar` are `pub` but their names were already English; only their doc comments needed translation).
- `norte-encoding::fold`: renamed the `pub(crate)` function `es_ignorable_por_defecto` → `is_default_ignorable_impl` and the private helper `en_rangos` → `in_ranges`, plus private consts `INVISIBLES_FUERA_DE_DI` → `INVISIBLES_OUTSIDE_DI` and `IGNORABLES_PERMITIDOS` → `ALLOWED_IGNORABLES`. Checked via `rg -w` across `crates` and `plugins`: all four are used only inside `norte-encoding` (across `lib.rs`/`fold.rs`, which are both mine), so this was a same-crate rename, not a cross-file report.
- `norte-ai`: renamed the private const `MODELOS_CON_SALIDA_ESTRUCTURADA` → `STRUCTURED_OUTPUT_MODELS`, private fn `admite_salida_estructurada` → `supports_structured_output` (both `anthropic.rs`-local), and the private method `OpenAiCompatProvider::enviar` → `send` (checked with `rg -w`, no cross-file references — all four are private/module-local).

## 2. Cross-file literals kept (asserted elsewhere / golden)

- `norte-connect::error::ConnectError::SecretEmpty`'s message: checked via `rg -F` against `norte-core/tests/daemon/policy.rs` and `norte-proto/tests/golden_types.rs`, which carry similarly-worded Spanish `SecretEmpty` messages with different connection names — confirmed non-identical, so translating this crate's copy did not break another crate's pinned string.
- No other cross-crate literal collisions were found. Test fixture strings using non-ASCII bytes on purpose — `café`/`año`/`CAFÉ`/NFD-NFC pairs, cp437/cp866/Shift-JIS bytes, the canonical hostile corpus's embedded Spanish words (`aguja`, `Página`, `Muchos`, `Valla`, `cuerpo`, `título`) inside `norte-testkit::corpus` — were left untouched everywhere across all eight crates: the accented/hostile bytes and the intentionally-Spanish fixture text are the actual point of those tests, not incidental prose. Verified via targeted `grep -nP '[áéíóúñÁÉÍÓÚÑ¿¡]'` on every remaining file at the end of this task; every hit left is one of these categories (see §7 for the full list).
- `norte-vfs-rar`: two test byte-content literals (`"hola norte\n"` → `"hello norte\n"` in `provider.rs`/`real_delegate.rs`/`contract_ro.rs`) and one deliberately-malformed test name (`"mal\nnombre.txt"` → `"bad\nname.txt"` in `listing.rs`) were translated rather than kept, because they are arbitrary local test data (not canonical-corpus, not golden, not shared with another crate) — translating them changes no test property (still an 11/12-byte string, still a name split by an embedded `\n`). Length-dependent assertions (`hello.size == 12`, byte-range offsets) were updated in lockstep in the same edits.

## 3. UI strings outside Fluent, left as-is

None identified. All eight crates in this task are backend/protocol/infrastructure crates (VFS providers, testkit fixtures, MCP bridge, terminal emulation, AI provider abstraction, encoding detection) with no `t!()`-eligible user-facing strings; the handful of `tracing::*!`/`AiError`/`RarError` messages are developer/log-facing and were translated in place as instructed.

## 4. Spanish file/module names found

None. All module and file names across the eight crates were already English: `norte-connect` (`ftp`, `known_hosts`, `secret`, `spec`, `ssh`, `error`), `norte-testkit` (`corpus`, `faults`, `mem`, `smith`), `norte-mcp` (`bridge`), `norte-term` (`pty`), `norte-vfs-object` (`provider`), `norte-vfs-rar` (`delegate`, `index`, `listing`, `provider`), `norte-ai` (`anthropic`, `fake`, `http`, `ollama`, `openai_compat`, `provider`), `norte-encoding` (`fold`).

## 5. Glossary — new terms added

No new terms needed adding to `TRANSLATION_GLOSSARY.md`. Domain vocabulary encountered in the final three crates of this task matched existing conventions: `Delegate`/`discover`/`sandbox` (norte-vfs-rar's external-program pattern, aligned with existing "delegate" usage elsewhere in the tree), `fold`/`fold_delta`/`default ignorable`/`canonical singleton` (norte-encoding, Unicode terms-of-art kept verbatim as Unicode itself names them), `AiProvider`/`AiCaps`/`ChatStream`/`JsonContract` (norte-ai, already-English public API, no translation needed).

## 6. TODO(translation) count

0 added in this task. No stale/mismatched comments requiring that annotation were found in any of the eight crates — every doc comment translated matched its code, and no comment appeared to describe behavior the code no longer has.

## 7. Remaining non-ASCII content, verified as intentional DATA

Final sweep (`grep -rlP '[áéíóúñÁÉÍÓÚÑ¿¡]'`) over all eight crates' `src`, `tests`, and `Cargo.toml` found hits in exactly these files, each confirmed by inspection to be fixture/example data rather than untranslated prose:

- `norte-testkit/src/corpus.rs`, `mem.rs`, `smith.rs`, `tests/corpus.rs`, `tests/mem.rs`, `src/corpus/names.json`: canonical hostile-corpus/encoding fixture bytes and doc-comment *examples quoting them* (`café`/`año`/`título`/`Página`/cp437/cp866/Shift-JIS bytes) — explicitly the DATA this task's instructions said to preserve.
- `norte-mcp/tests/bridge.rs`: `"hola ñ"`, `"año 2026\n"` — wire-content fixtures exercising byte-exact non-ASCII transport, not prose.
- `norte-vfs-object/tests/trash.rs`, `s3.rs`: hostile S3-key fixtures (`"año 名前 😀.txt"`, `"ñé—😀.txt"`) — encoding/hostile-name test data.
- `norte-encoding/src/lib.rs`, `fold.rs`, `tests/{detect,names,hazards,needle}.rs`: doctest/unit-test examples demonstrating actual encoding/Unicode behavior (`año`, `café`, cp437's golden table, `ñ`/`é` fold pairs) — the accented content IS the thing under test.

`norte-connect`, `norte-term`, `norte-vfs-rar`, `norte-ai` have zero remaining non-ASCII hits.

## 8. `cargo check` result

```
cargo check -p norte-connect -p norte-testkit -p norte-mcp -p norte-term -p norte-vfs-object -p norte-vfs-rar -p norte-ai -p norte-encoding --all-targets
   Finished `dev` profile [unoptimized + debuginfo] target(s) in 17.57s
```

Clean, no errors or warnings, for all eight crates together (also independently re-verified for `norte-vfs-rar`, `norte-ai`, and `norte-encoding` — the three crates finished in this continuation — each with `--all-targets` and appropriate feature flags: `norte-ai --features testutil`, `norte-vfs-object --features it-s3`, `norte-term --features pty`).

Additional spot checks on the three crates finished in this continuation:
- `cargo test -p norte-encoding --doc`: 18 passed.
- `cargo test -p norte-ai --doc --features testutil`: 4 passed.
- `cargo test -p norte-vfs-rar --doc`: 5 passed.

## Summary paragraph (for the calling agent)

T24 (norte-connect, norte-testkit, norte-mcp, norte-term, norte-vfs-object, norte-vfs-rar, norte-ai, norte-encoding — all 8 crates) is fully translated. Every comment, rustdoc, log/error/panic message, and file-local identifier is now English; the canonical hostile/encoding corpus in `norte-testkit` and the encoding-example literals in `norte-encoding`/`norte-vfs-object`/`norte-mcp` were preserved as DATA exactly as instructed, confirmed file-by-file in the final sweep (§7). `norte-mcp`'s MCP tool names/schemas/descriptions were already English — nothing needed changing there. The largest reported item is `norte-term`'s entire Spanish-named public API (`Pantalla`, `Celda`, `Estilo`, `ColorTerm`, `pty::Arranque`/`Shell`), left unrenamed per the phase-1 rule and listed in §1 as a phase-2 candidate, since it is used cross-crate by `norte-tui`. Four small same-crate renames were made without report (`norte-encoding::fold`'s `es_ignorable_por_defecto`/`en_rangos`/two ignorable-table consts; `norte-ai::anthropic`'s model-list const/fn; `norte-ai::openai_compat`'s `enviar`→`send`), all verified file-local via `rg -w` first. Zero `TODO(translation)` markers were needed. The final required combined check, `cargo check -p norte-connect -p norte-testkit -p norte-mcp -p norte-term -p norte-vfs-object -p norte-vfs-rar -p norte-ai -p norte-encoding --all-targets`, passed clean in 17.57s with no errors or warnings; doctests for the three crates translated in this continuation also pass in full (27 total). Nothing was left broken; no other task's files were touched (the one transient `norte-client`/`norte-vfs-rar` false-positive compile error hit mid-session, described in the earlier progress notes, was a feature-unification artifact from checking `--lib` alone rather than `--all-targets`, not a real bug, and resolved itself once `--all-targets` was used).
