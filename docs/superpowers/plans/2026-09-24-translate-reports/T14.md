# T14 — `tui/src/app/`, `tui/src/jobs/`

Resumed task: the previous agent was cut off right before its final check.
Scanned all 29 files (21 in `app/`, 8 in `jobs/`) for residual Spanish;
comments/rustdoc were already fully translated. What remained was mostly
`#[test]` fn names (~100) and a handful of private helpers/locals. All of it
is now translated. `cargo check`/`cargo test --no-run` clean for everything I
own.

## 1. pub renames (not done — cross file)

- `crates/norte-tui/src/app/pane.rs:232` — `pub fn senalar` on `Pane`. Used
  from `crates/norte-tui/src/viewer_open.rs` (T13's file:
  `app.focused_mut().senalar(fila)`). Proposed: `senalar` → `point_to`
  (matches `norte_frontend::PaneState::senalar`, a *different* type also
  pending rename — see `norte-frontend`'s own task).
- `crates/norte-tui/src/app/layout.rs:991` — `pub fn soltar_teclado`. Used
  from `crates/norte-tui/src/ui/geometry.rs` (T15's file:
  `app.soltar_teclado()`). Proposed: `soltar_teclado` → `release_keyboard`.
- `crates/norte-tui/src/app/profile.rs:60` — `pub fn no_aplicable_en_caliente`.
  Used from `crates/norte-tui/src/event_loop.rs` (T12's file). Proposed:
  `no_aplicable_en_caliente` → `not_hot_applicable`.
- `crates/norte-tui/src/jobs/goto.rs:36` — `pub fn pedir_al_indice`. Used from
  `crates/norte-tui/src/keys.rs` (T12's file, x2) and named in a comment in
  `jobs/mod.rs:28` (mine, left as is since the fn itself isn't renamed).
  Proposed: `pedir_al_indice` → `request_from_index`.
- `crates/norte-tui/src/app/plugins.rs` — `ExtFoco` (pub enum, variants
  `Lista`/`Boton`), `pub fn siguiente_foco`, `pub fn anterior_foco`, and the
  `pub foco: ExtFoco` field on `ExtensionManager`. Heavily cross-file:
  `crates/norte-tui/src/screens/extensions.rs` (T15), `src/mouse.rs` (T12),
  `src/dispatch.rs` (T13), `src/overlays.rs` (T13), `src/ui/overlays.rs`
  (T15), plus `tests/extensions.rs`, `tests/mouse.rs`, `tests/snapshots_ui.rs`
  (T16). Proposed: `ExtFoco` → `ExtFocus`, `Lista` → `List`, `Boton` →
  `Button`, `siguiente_foco` → `next_focus`, `anterior_foco` → `previous_focus`,
  field `foco` → `focus`. This one needs a single coordinated pass (phase 2)
  across all those files at once, not a partial rename.
- The test module name `siguiente_foco_tests` in `plugins.rs` was left as is
  (mod names are report-only per the rules) — pairs with the `ExtFoco` rename
  above.

Renamed THIS pass (confirmed via `rg -w` that every reference lives inside my
29 files, so no cross-task coordination needed):

- `app/testutil.rs`: `caps_de_test` → `test_caps`, `degradacion_de_test` →
  `test_degraded`, `fila_huerfana` → `orphan_row` (call sites updated in
  `caps.rs`, `banners.rs`, `compare.rs`).
- `app/testutil.rs`'s `pane_con`, `app_dos_panes`, `app_en` were **NOT**
  renamed — confirmed cross-task (`ui/panels.rs`, `ui/status.rs` = T15;
  `logview.rs`, `app.rs` = T12; `wizard.rs` = T13). Proposed:
  `pane_con` → `pane_with`, `app_dos_panes` → `app_two_panes`,
  `app_en` → `app_at`.
- `app/layout.rs`: private `podar_por_arbol` → `prune_by_tree`, `revelar` →
  `reveal`, `se_ve` → `is_visible`, `cambiar_disposicion` → `change_layout`,
  and the private test helpers `es_panel_de_git` → `is_git_panel`,
  `plugin_con_panel` → `plugin_with_panel`, `app_con_registro_escondido` →
  `app_with_hidden_log`.
- `app/focus.rs`: private `aterrizar` → `land`.

## 2. cross-file literals

None found. No Spanish string literals remain in these files; the one
hostile-corpus literal (`"está"` in `layout.rs:1691`, a hostile-kind test
fixture with characters outside the alphabet) is deliberately kept.

## 3. UI strings outside Fluent

None found.

## 4. Spanish file/module names

None in this task's directories.

## 5. glossary

- **foto → snapshot** (a frozen copy of facts/state used for rendering — e.g.
  `freeze_help_facts`, plugin name/id snapshots in `help_view.rs`). Not
  previously in `TRANSLATION_GLOSSARY.md`.

## 6. TODO(translation)

0 added — no comment in this task's files was stale or wrong; all needed a
plain translation.

## 7. cargo check

`cargo check -p norte-tui --lib --bins`: clean.
`cargo test -p norte-tui --lib --no-run`: clean (compiles every unit test in
`app/*.rs` and `jobs/*.rs`, including all the renamed ones).
`cargo check -p norte-tui --all-targets` fails, but only in
`crates/norte-tui/tests/snapshots_ui.rs` (T16's file, not mine): three calls
to `render_en` that don't exist — `render_at` is the only definition present.
That matches the plan's own status note that T16 was interrupted mid-file.
Not touched, per the rule to stop and report when the break is in someone
else's file.
