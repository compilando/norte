# T01 report (RESUMED)

Owns `crates/norte-core/src/{engine.rs,ops.rs,plugins.rs}`.

Starting state: `ops.rs` and `plugins.rs` were already fully translated by
the previous (interrupted) run — only one leftover Spanish assert message
was found in `plugins.rs`. `engine.rs` was translated up to line ~1721;
everything from there to EOF (line 5660) was still Spanish. That is where
this run spent nearly all its time.

## 1. pub renames (not renamed — cross-file; proposed for phase 2)

These are identifiers **defined outside my three files** (so not mine to
rename) that `engine.rs` calls by name. Left as-is; flagging them here so
phase 2 knows `engine.rs` is a call site when the owning file gets renamed.

| item | owning file (task) | proposed English name |
| --- | --- | --- |
| `crate::compare::degradada` | `core/src/compare.rs` (T03) | `degraded` |
| `crate::embedded::FRENO_TRAS_FALLO` | `core/src/embedded.rs` (T02) | `RETRY_BRAKE_AFTER_FAILURE` |
| `crate::pack::Destino` | `core/src/pack.rs` (T02) | `Destination` |
| `crate::pack::Empaquetado` | `core/src/pack.rs` (T02) | `Packing` |
| `crate::pack::formato_de_nombre` | `core/src/pack.rs` (T02) | `format_from_name` |
| `crate::pack::mide_el_reparto` | `core/src/pack.rs` (T02) | `measure_split` |
| `crate::pack::que_se_comprueba` | `core/src/pack.rs` (T02) | `what_is_checked` |
| `crate::undo::deshechas` | `core/src/undo.rs` (T02) | `already_undone` |
| `crate::undo::Vigencia` (+ variants `Entera`/`EnParte`/`Deshecha`/`Parada`) | `core/src/undo.rs` (T02) | `Validity` (`Whole`/`Partial`/`Undone`/`Stopped`) |
| `crate::undo::vigencia` (fn) | `core/src/undo.rs` (T02) | `validity` |
| `crate::index_embed::Puntuado` | `core/src/index_embed.rs` (T03) | `Scored` |
| `crate::index_embed::mejores_k` | `core/src/index_embed.rs` (T03) | `top_k` |
| `OrganizePlan::carpetas`/`::pasos`, step `.destino()` | `core/src/organize.rs` (T03) | `folders`/`steps`/`.destination()` |
| `crate::scheduler::Lane::Cola`/`::Paralelo`, `submit_en`, `mover_en_cola` | `core/src/scheduler.rs` (T03) | `Lane::Queued`/`Lane::Parallel`, `submit_on`, `move_in_queue` |

`mover_en_cola` is the widest-reaching one: besides `scheduler.rs` it is
re-exposed verbatim as a method name on `Engine` (in my `engine.rs`),
`Backend` (`core/src/backend.rs`), `norte-client`'s `RemoteConnection`/`Task`,
`norte-ui-host`'s backend trait, and called from `norte-tui/src/dispatch.rs`
and `core/src/daemon/server.rs`. Renaming it is a cross-crate job, not a
single-file one — flagging loudly for whoever does phase 2's serial pass.

I also renamed the **private** field `Engine::undo_en_curso` → left it
UNCHANGED, even though it's private and only used inside `engine.rs`,
because `core/src/undo.rs` (T02) cites it BY NAME in a doc comment
(`` `Engine::undo_en_curso` ``). Proposed: `undo_in_progress`.

## 2. Cross-file literals

None found. No Spanish string literal in these three files was seen
duplicated in a golden/snapshot file or asserted in another file.

## 3. UI strings outside Fluent

None. `engine.rs`/`ops.rs`/`plugins.rs` are core/backend code; no
user-visible strings outside `tracing::*!`/error messages were found.

## 4. Spanish file/module names

None in this task's scope.

## 5. Glossary — new terms used (not yet in `TRANSLATION_GLOSSARY.md`)

| español | English | note |
| --- | --- | --- |
| trozo (de un split) | part | `file.split`/`file.combine`; local var `dir_trozos` → `parts_dir` |
| vigencia (de una unidad de undo) | validity | proposed enum rename above |
| puntuado | scored | proposed rename above |
| empaquetado | packing | proposed rename above |
| lock sano / lock envenenado | lock is sound / poisoned lock | matches the style already used earlier in `engine.rs` (`"foo lock is sound"`) |

## 6. TODO(translation) added

0. Every comment found was either already correct or unambiguous; none
needed the "stale comment" marker.

## 7. cargo check

```
cargo check -p norte-core --all-targets --features norte-core/testing
```
→ **Finished** clean (no errors, no warnings). Note: `--features
norte-core/testing` is required — without it, `cargo check --all-targets`
alone fails with unrelated pre-existing errors (`run_column_values_for_test`,
`ColumnPool::column_values_for_test`, `ColumnPool::reutilizadas` are all
gated behind `cfg(any(test, feature = "testing"))`, which integration-test
binaries don't get for free). This is the same feature flag the `justfile`
gate uses (`norte-core/testing` in the `features` variable).

Also verified: the mid-rename mismatch the plan warned about
(`run_column_values_for_test` in `core/tests/columns_git_e2e.rs` and
`columns_size_bar_e2e.rs` vs. `plugins.rs`) was **already consistent** —
the definition in `plugins.rs` (line 4466) and all call sites in both test
files already agree on the name. No edit to either test file was needed.

## Local/private renames done (informational, no cross-file impact)

All checked with `rg -w` first; none appear outside `engine.rs`/`ops.rs`/
`plugins.rs`:

- `primera`→`first`, `informe`→`report`, `vivo`→`live`, `rutas`/`fuentes`→
  `paths`/`sources`, `provider_destino`→`dest_provider`, `nombre`→`name`,
  `raiz`→`root`, `dir_trozos`→`parts_dir`, `carpetas`/`pasos`/`de`/`a`
  (organize local bindings) → `folders`/`steps`/`from`/`to`, `arriba`→`up`,
  `denegada`→`denial`, `en_curso`→`in_progress` (local only; field kept, see
  §1), `deshechas`→`already_undone` (local only; the `crate::undo::deshechas`
  **function** was left untouched, see §1), `opciones`→`options`,
  `modo`→`mode`.
- Private fn `categoria_de_ai` → `ai_error_category` (all 4 call sites in
  `engine.rs` updated); its three Spanish leaf strings translated:
  `"cancelada"`→`"cancelled"`, `"no_soportado"`→`"unsupported"`,
  `"otro"`→`"other"` (checked: not asserted anywhere by exact string).
- Test fn names in `engine.rs`'s `#[cfg(test)] mod tests` translated (e.g.
  `el_desalojo_sacrifica_primero_el_informe_que_no_cuenta_nada` →
  `eviction_sacrifices_the_report_that_counts_for_nothing_first`).
- `plugins.rs`: one assert message translated (`"sin consentir, ninguna
  columna se resuelve"` → `"without consent, no column resolves"`).

## Summary

`ops.rs`: 0 changes needed (already done). `plugins.rs`: 1 string literal
fixed. `engine.rs`: full translation of ~3900 remaining lines (comments,
rustdoc, log/error/panic messages, and every private/local identifier),
going from 710 lines containing Spanish-only accented characters down to 1
(a deliberate `/Café` example inside an already-English NFC/NFD sentence).
`cargo check -p norte-core --all-targets --features norte-core/testing`
passes clean. Nothing else went wrong.
