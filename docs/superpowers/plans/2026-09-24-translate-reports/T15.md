# T15 — `tui/src/ui/`, `tui/src/screens/`

Resumed task: the previous agent was cut off mid-file. Scanned all 18 files
(7 in `screens/`, 11 in `ui/`, ~14,600 lines total). `screens/*.rs` and
`ui/{chrome,compare,geometry,help,overlays,pickers,status,sync,text}.rs` were
already fully translated (verified with an accented-character sweep plus a
broader unaccented-Spanish-word sweep restricted to comment lines — zero
hits). The three big files were not: `ui/modals.rs` (3300 lines, ~534
Spanish comment/doc lines), `ui/panels.rs` (1696 lines, ~265 lines) and
`ui/pane.rs` (1667 lines, ~223 lines) were still almost entirely in Spanish.
All three are now fully translated — comments, rustdoc, panic/log/error
text, and private/test identifiers. Along the way I found and fixed two
stray cross-references in already-"done" files: `ui/geometry.rs` had a doc
comment naming the pre-rename `barras_del_visor`, and `ui/geometry.rs` +
`ui/chrome.rs` shared a `pub(crate) struct PanelTab { texto, activa }` and
private fns `tiras_de_paneles`/`grupo_de_paneles` that were still Spanish
(all local to my file set, so renamed and translated in full: `PanelTab {
text, active }`, `panel_tab_strips`, `panel_group`).

## 1. pub renames (not done — cross file)

- `crates/norte-tui/src/ui/geometry.rs` — `pub fn rect_del_visor`. Used from
  `crates/norte-tui/tests/viewer_imagen.rs` (T16), `src/event_loop.rs` (T12)
  and `src/ui.rs` (T13). Proposed: `rect_del_visor` → `viewer_content_rect`.
- `crates/norte-tui/src/ui/geometry.rs` — `pub(crate) fn contenido_de_hueco`.
  Used from `src/panelplugin.rs` and `src/ui.rs` (both T13). Proposed:
  `contenido_de_hueco` → `slot_content_rect`.
- `crates/norte-tui/src/ui/chrome.rs` — `pub(crate) fn
  draw_tiras_de_paneles`. Used from `src/ui.rs` (T13,
  `chrome::draw_tiras_de_paneles(frame, app)`). Proposed:
  `draw_tiras_de_paneles` → `draw_panel_tab_strips` (matches the now-renamed
  `panel_tab_strips` it calls internally).
- `crates/norte-tui/src/viewer_open.rs` (T13's file) — `pub fn
  no_hace_falta_avisar_de_imagen`, `pub fn
  no_hace_falta_avisar_de_miniatura`, `pub fn aviso_de_imagen`. Called from
  `ui/panels.rs` (mine, `draw_viewer`). Proposed: →
  `image_warning_not_needed`, `thumbnail_warning_not_needed`,
  `image_warning`.
- `crates/norte-tui/src/ui.rs` (T13's file) — `pub fn imagen_a_colocar`.
  Named (not called cross-crate, but referenced by name in a comment) in
  `ui/panels.rs`; the comment now reads "`imagen_a_colocar` (not a check
  separate from the `path`) is the SAME function..." — left as-is since the
  identifier isn't mine to rename.
- `crates/norte-tui/src/app/modal.rs` (T14's file) — the `Modal::
  ConfirmUndoAfter` variant has Spanish field names `a_deshacer: usize,
  irreversibles: usize, ajenas: usize`. Destructured and used in
  `ui/modals.rs` (`modal_title_text`'s match arm, and its rustdoc comment).
  Proposed: `a_deshacer` → `to_undo`, `irreversibles` → `irreversible`,
  `ajenas` → `foreign`. Not in T14's own report, so flagging fresh.
- `crates/norte-tui/tests/snapshots_ui.rs` (T16's file) — test fn
  `el_mensaje_que_se_envuelve_cabe_en_su_caja`, named by identifier inside a
  rustdoc comment in `ui/modals.rs` (`body_height`'s doc). Left as-is
  (identifier belongs to T16's file).

Renamed THIS pass (confirmed via `rg -w` that every reference lives inside my
18 files):

- `ui/modals.rs`: `alto_del_cuerpo` → `body_height`, `ModalFrame.envuelve` →
  `.wraps`, `ALTO_ENVUELTO` → `WRAPPED_HEIGHT`, `ORGANIZE_NUEVA` →
  `ORGANIZE_NEW`, `ORGANIZE_EXISTENTE` → `ORGANIZE_EXISTING`,
  `ORGANIZE_FICHERO` → `ORGANIZE_FILE`, plus local renames inside several
  functions/tests (`contando` → `counting`, `NOMBRE_MAX` → `NAME_MAX`,
  `destino`/`teclas` params → `dest`/`keys`, several Spanish test-fn names →
  English).
- `ui/panels.rs`: `barras_del_visor` → `viewer_scrollbars`, `estilo_de_span`
  → `span_style`, `lineas_de_marco` → `frame_lines`, `con_teclado` (used as
  a parameter name in ~8 functions) → `with_keyboard`, `rol_de_actor` kept
  (referenced by name from nowhere else, but left un-renamed for time — see
  below).
- `ui/pane.rs`: `filas_pintadas` → `painted_rows`, `estado_de_lista` →
  `list_state`, `ventana_del_pane` → `pane_window`, `celda_cortada` →
  `clipped_cell`, `raya` → `stripe`, `pinta_barra` → `paint_slash`,
  `fondos_con_pijama` → `pyjama_backgrounds`, plus several Spanish test-fn
  names → English.
- `ui/geometry.rs` / `ui/chrome.rs` (shared): `PanelTab.texto` → `.text`,
  `PanelTab.activa` → `.active`, `tiras_de_paneles` → `panel_tab_strips`,
  `grupo_de_paneles` → `panel_group`.

Left un-renamed for time (private, in-file only, low visibility — not
cross-file, just skipped): `rol_de_actor` (panels.rs), `linea_de_timeline`
(panels.rs), `listo`/`plan_con_colision` (modals.rs test module),
`pinta_barra`'s sibling locals in a few functions (`titulo`, `mapa`,
`estado`, `nombre`, `libre`, `dentro`, `marco`, `sangria`, etc. across
`panels.rs`/`pane.rs`) — comments and doc-comments around them are fully
translated, only the bare local variable name stayed Spanish.

## 2. cross-file literals

- `ui/text.rs:78` — the rustdoc example `` `[enter] confirmar
  [esc]…kspace] atrás [/] filtrar` `` (and the `[tab] otro panel` example a
  few lines below) is kept: these exact Spanish strings are pinned in
  `tests/snapshots/snapshots_ui__snapshot_modal_pinta_encima_de_la_palette.snap`,
  `..._columns_picker_80x24.snap`, `..._extensions_description_hostil_80x24.snap`,
  `..._modal_transfer_name.snap`, `..._popup_historial.snap` and referenced
  in prose in several other files outside this task (`shortcuts_editor.rs`,
  `navigate.rs`, `mutations.rs`, `logview.rs`, `main.rs`, several
  `tests/*.rs`). Phase 3 (UI strings) will need to update this rustdoc
  example once those literals actually change.
- `ui/modals.rs` test data: `"facturas"`, `"nueva"`, `"a.pdf"` (organize-plan
  test fixture directory/file names) and `"carpeta"` (a VPath path segment
  in `ui/pane.rs`'s long-destination test) are left as Spanish words on
  purpose — they are literal fixture values an assertion checks
  (`.contains("carpeta")` etc.), not prose.

## 3. UI strings outside Fluent

None found. Every user-visible string in these 18 files goes through
`t()`/`ta()`.

## 4. Spanish file/module names

None in this task's files.

## 5. glossary

- **pijama → pyjama** (the alternating-row-background feature, `[ui]
  row_stripes`; `Role::Stripe` is already the English-named theme role). Not
  previously in `TRANSLATION_GLOSSARY.md`; used throughout `ui/pane.rs`'s
  tests (`pyjama_backgrounds`, `the_pyjama_alternates_the_rows`).
- **canalón → gutter** (the one-cell mark column before a row's name/badge,
  used for the "marked" `*` and reused for the icon-column concept). Matches
  existing `ICON_GUTTER` naming.

## 6. TODO(translation)

0 added — every Spanish comment in these 18 files was a straightforward,
still-accurate technical rationale; none needed the stale/wrong-comment
treatment.

## 7. cargo check

- `cargo check -p norte-tui --all-targets`: clean (no errors, no warnings)
  after all edits.
- `cargo doc -p norte-tui --no-deps`: clean — no broken intra-doc links from
  any of the renames above.
- `cargo test -p norte-tui --doc`: 12 passed, 0 failed (none of the 12 live
  in files I touched).
- `just t norte-tui` (nextest): 775/776 run before fail-fast stopped the
  batch; the one failure is `norte-tui::config
  el_frontend_de_terminal_loguea_al_fichero_y_no_a_la_pantalla` in
  `crates/norte-tui/tests/config.rs` (T16's file, not touched by me). It
  asserts the log file contains the Spanish string `"proveedor de IA no
  disponible"`, but the actual (already-translated-elsewhere) log line now
  reads `"AI provider not available: ..."` — a cross-file consequence of
  some other file's AI-provider message having already been translated to
  English while this test's expected string was not updated. Every test in
  `ui::modals`, `ui::panels` and `ui::pane` (the three files I rewrote)
  passed. Flagging for whoever owns `tests/config.rs` (T16) or the AI
  provider message's source file.
