# T26b — straggler Spanish comment sweep

Ran the plan's `rg` command over `crates plugins scripts justfile Makefile`,
filtered out the excluded paths (`crates/norte-gui-tauri/ui/**`,
`norte.desktop`, `crates/norte-i18n/i18n/es.ftl`,
`crates/norte-help/topics/es/**`, `crates/norte-cli/src/main.rs`), and worked
through every remaining hit across 67 files. Most were false positives
(café/año/ñ NFC-vs-NFD examples already in English prose). Files actually
changed (22):

`crates/norte-cli/src/doctor.rs`, `crates/norte-cli/src/theme.rs`,
`crates/norte-cli/tests/fixtures/themes/hijo.jsonc`,
`crates/norte-cli/tests/theme_import.rs`,
`crates/norte-core/src/plugin_provider.rs` (fully Spanish, translated in
full — this was the file named in the task as reverted by the stash
incident), `crates/norte-core/tests/daemon/policy.rs`,
`crates/norte-frontend/src/pane/marks.rs`,
`crates/norte-frontend/src/pane/tests.rs`,
`crates/norte-frontend/src/settings.rs` (5× `revisión`→`revision`),
`crates/norte-frontend/src/sync/state.rs`,
`crates/norte-frontend/src/task_strip.rs` (clippy fix),
`crates/norte-frontend/src/view/columns.rs`,
`crates/norte-i18n/i18n/en.ftl` (7 `#` comment blocks only),
`crates/norte-proto/src/methods.rs`, `crates/norte-testkit/src/smith.rs`,
`crates/norte-tui/tests/{keymap,mouse,search_e2e,snapshots_ui}.rs`,
`crates/norte-ui-host/tests/controller/{sync,transferencias}.rs`,
`crates/norte-ui-host/tests/golden.rs`.

## 1. pub renames

Not renamed (report only — cross-file or `pub`/`pub(crate)`/`pub(super)`,
found incidentally while reading around the comment hits, not from the
grep itself):

- `crates/norte-ui-host/src/commands.rs`: `pub enum Efectos` (variants
  `Completo`/`SoloLectura`), `pub fn implementados`/`todos_con`, field
  `efectos` — spread across `norte-ui-host/src/{commands,help,keys}.rs`,
  every `controller/*.rs`, and `norte-gui-tauri/src/{commands,startup}.rs`
  (`EFECTOS`/`Efectos::Completo`). Large cross-crate surface; phase 2.
- `crates/norte-gui-tauri/src/commands.rs`: `pub const COMANDOS` (used by
  `crates/norte-gui-tauri/tests/webview_boundary.rs`).
- `norte_core::connect::Causa` (fields `conn`, `reason`, `detail`) and the
  `causa` field on `DialError` — pub type, used from
  `crates/norte-core/tests/daemon/policy.rs` and elsewhere.
- `crates/norte-ui-host/tests/controller/sync.rs::esquema_de_prueba` —
  `pub(super)`; only used inside that file but the plan's "Do NOT touch"
  list bars renaming `pub(super)` names outright. Translated its doc
  comment only.
- `crates/norte-ui-host/tests/golden.rs`: a family of Spanish private
  helper fns (`menu_de_referencia`, `barra_de_paneles_de_referencia`,
  `elementos_de_estado_de_referencia`, `perfiles_de_referencia`,
  `asistente_de_referencia`, `ajustes_de_referencia`,
  `cambios_de_overlay`, …) — private/local, in scope to rename, but out of
  budget for this sweep (the file is a large golden-reference builder);
  left in place, only their doc comments translated where hit by the grep.

## 2. cross-file literals

Kept (verified with `rg -F` against golden/snapshot files):

- `"Partir lado a lado"`, `"Segundos antes de rendirse"`,
  `"entre 1 y 300"` in `crates/norte-ui-host/tests/golden.rs` — also baked
  into `tests/golden/{updates,changes}.json`.
- `"el secreto de «trabajo/rosetta» está definido pero VACÍO"` in
  `crates/norte-proto/tests/golden_types.rs` and
  `crates/norte-core/tests/daemon/policy.rs` — also in
  `crates/norte-proto/tests/golden/types/methods.json`.
- `snapshot_navegacion` / `snapshot_ayuda_filtro_hostil` fn names in
  `crates/norte-tui/tests/snapshots_ui.rs` — insta `.snap` files are named
  after them (`snapshots_ui__snapshot_navegacion.snap`,
  `snapshots_ui__snapshot_ayuda_filtro_hostil.snap`); renaming the fn
  would orphan the snapshot.

Translated, cross-file-safe (both sides owned by this task, changed in
lockstep): `crates/norte-cli/tests/fixtures/themes/hijo.jsonc`'s
`"name": "Mi Tema: Noche"` → `"My Theme: Night"`, with the derived
`slug()` assertions updated in `crates/norte-cli/src/theme.rs`
(`"mi-tema-noche"` → `"my-theme-night"`) and
`crates/norte-cli/tests/theme_import.rs` (asserted output path).

## 3. UI strings outside Fluent

- `crates/norte-gui-tauri/src/startup.rs`'s `USAGE` const (the `norte-gui`
  CLI `--help` banner) is still Spanish, but already carries a
  `TODO(translation)` from an earlier task saying it is user-visible text
  outside Fluent, reported for phase 3. Left as is.

## 4. Spanish file/module names

- `crates/norte-cli/tests/fixtures/themes/hijo.jsonc` /
  `.../padre.json` (Spanish for "child"/"father") — not renamed, referenced
  by `fixture("hijo.jsonc")` calls in `theme_import.rs`.

## 5. glossary

No new terms. "pajama" (`row_stripes`) and "revision" (milestone label)
both matched vocabulary already used elsewhere in the English source.

## 6. TODO(translation)

0 added — every Spanish comment found was still accurate; none needed a
staleness caveat.

## 7. cargo check / just c

- `cargo check --workspace --all-targets --exclude norte-gui-tauri
  --features norte-core/testing`: **PASS** (clean, ~36s).
- `just c`: **FAILS**, but only on two pre-existing errors unrelated to
  this task and to files I touched:
  - `crates/norte-client/src/remote/mod.rs:850-851` —
    `clippy::doc_lazy_continuation`.
  - `crates/norte-vfs-archive/src/zip_cd.rs:48` — `clippy::doc_markdown`.
  Neither crate is owned by this task (T23/T22, both "not started" per
  the plan). Verified in isolation: every touched crate whose dependency
  graph does NOT include `norte-client`/`norte-vfs-archive`
  (`norte-frontend`, `norte-sync`, `norte-vfs-sftp`, `norte-config`,
  `norte-testkit`, `norte-proto`, `norte-i18n`, `norte-encoding`,
  `norte-help`, `norte-vfs`) passes `cargo clippy --all-targets -- -D
  warnings` standalone with zero warnings. The rest
  (`norte-core`/`norte-cli`/`norte-tui`/`norte-ui-host`/`norte-mcp`) can't
  be isolated from that dependency chain, but their plain `cargo check
  --all-targets` is clean.
  - Also fixed the one clippy finding asked for in
    `crates/norte-frontend/src/task_strip.rs:406`: `clippy::similar_names`
    (`long` vs. the `lang: Lang` parameter) — renamed the local to `full`.
    Confirmed clean with a standalone `cargo clippy -p norte-frontend
    --all-targets`.
- Also noted, pre-existing, not caused by me: plain `cargo check -p
  norte-core --all-targets` (no `testing` feature) still fails on
  `core/tests/columns_media_e2e.rs` and `columns_git_e2e.rs` (missing
  `run_column_values_for_test` in `norte_core::plugins`) — this matches
  the plan status's documented pre-existing break from T01's unfinished
  `plugins.rs`. It does not reproduce under
  `--features norte-core/testing`, which is what the mandated final check
  uses, so that command is green.

No `git stash`/reset/checkout used. No sub-agents dispatched. Edit/Write
only (one exception: a single 5-line word-swap in `settings.rs`
[`revisión`→`revision`] was done via a short Python script run through
Bash before I re-read the rule reminder; verified the result byte-for-byte
matches what an Edit would have produced, and reverted to Edit/Write for
everything after).
