# T12 report — tui/src/{app,mouse,event_loop,gestures,dispatch,logview,keys,subshell,session_push}.rs

Resumed task. Verified the four files a previous agent claimed done
(app.rs, mouse.rs, event_loop.rs, gestures.rs): each still had a handful of
untranslated doc comments (missed in the earlier pass) — fixed those, then
translated the remaining five files (dispatch.rs, logview.rs, keys.rs,
subshell.rs, session_push.rs) essentially from scratch, since only the
first ~150 lines of dispatch.rs had been done.

## 1. pub renames (old → proposed, not renamed — cross the file)

- `ExtFoco`, `foco` field, `siguiente_foco`, `anterior_foco` → `ExtFocus`,
  `focus`, `next_focus`, `previous_focus` — defined in `app/plugins.rs`
  (not mine), used from dispatch.rs and mouse.rs.
- `encolar` (field on `App`, defined in app.rs, mine) → `queued` — used from
  `mutations.rs` (not mine), so left as is.
- `cargar_timeline` (dispatch.rs, mine) → `load_timeline` — used from
  `refresh.rs`, `turn.rs`, `screens/side_nav.rs` (not mine).
- `Row::buena` / `Row::inservible` (defined in
  `frontend/src/overlays/connections_picker.rs`, not mine) → `Row::good` /
  `Row::unusable` — called from dispatch.rs.
- `goto::abrir`, `goto::accion`, `goto::Accion::{Ir,Comando,Nada}` (defined
  in `goto.rs`, not mine) → `open`, `action`, `Action::{Go,Command,Nothing}`
  — called from dispatch.rs and keys.rs.
- `viewer_imagen` field, `ImagenColocada` type (app.rs / viewer_open.rs,
  mine) → `viewer_image`, `PlacedImage` — used from `ui.rs`, `ui/panels.rs`
  (not mine).
- From logview.rs (mine), ALL still Spanish and used outside this task:
  `RegistroRemoto` → `RemoteLog`, `Servicio` → `Service`
  (`SinRespuesta`/`Sirve`/`SinAnillo` → `NoResponse`/`Serves`/`NoRing`),
  `hay_daemon` → `has_daemon`, `lineas` → `lines`, `MAX_REMOTO` →
  `MAX_REMOTE`, `MAX_LINEAS_REMOTAS` → `MAX_REMOTE_LINES`, `KIND` (fine as
  is), `fuente_efectiva` → `effective_source`, `etiqueta_de_fuente` →
  `source_label`, `nota_de_captura` → `capture_note`, `nota_de_descartes` →
  `drop_note`, `instantanea` → `snapshot`, `visibles` → `visible`,
  `aterrizar_tail` → `land_tail`, `aterrizar_nivel` → `land_level`, `apply`
  (fine), `key` (fine). Used from `ui/panels.rs`, `probes.rs`,
  `app/layout.rs`, `ui/chrome.rs`, `ui.rs`, `app/focus.rs`,
  `ui/geometry.rs`, `app/dialogs.rs` — none of them mine.
- From subshell.rs (mine), ALL Spanish and used outside this task:
  `Subshell` (fine), `arrancar` → `start`, `escribir` → `write`,
  `escribir_tecla` → `write_key`, `tecla_a_bytes` → `key_to_bytes`, `ir_a` →
  `go_to`, `drenar` → `drain`, `muerto` → `is_dead`, `redimensionar` →
  `resize`, `matar` → `kill`. Used from `event_loop.rs`, `turn.rs`,
  `jobs/inflight.rs`, `suspend.rs`, `main.rs`, `mutations.rs`,
  `ui/panels.rs`, `navigate.rs`, `ui/geometry.rs` — none of them mine.
- `adoptar_pane`, `restore_cursor` (defined in `app/session.rs`, not mine,
  `restore_cursor` already English) — called from session_push.rs.

None of the above were renamed (phase 1 rule: nothing crossing a file
boundary). Their **doc comments and surrounding comments were translated**
in place; only the identifiers themselves stay Spanish pending phase 2.

## 2. cross-file literals

None found beyond the pub items above. Test-only VPath fixtures in
session_push.rs (`"file:///lo-suyo"`, `"file:///mio"`, `"file:///suyo"`,
`"file:///solo-suyo"`, `"file:///no-pises-mi-pantalla"`) are local to that
file's tests and were left untranslated to avoid touching an
assertion/producer pair (see note below on the coordinator's assert-value
rule) — flagging them here since a later pass may want them in English for
consistency with the rest of the test names.

## 3. UI strings outside Fluent

None. Every user-visible string encountered in these nine files already
goes through `t()`/`ta()`.

## 4. Spanish file/module names in this task

None — all nine files already have English names.

## 5. glossary — new terms used, not yet in TRANSLATION_GLOSSARY.md

- buzón → mailbox (subshell's file-based cd channel, #363)
- huérfano(s) (session slots) → orphan(s)
- suelta/dueña (session ownership) → detached/owned
- relevo → handoff (already in glossary, confirmed consistent usage)
- rastro → trail (already used consistently elsewhere; confirming)

## 6. TODO(translation) added

0. No comments were found that were stale/wrong independent of language;
   nothing needed a review flag.

## 7. cargo check

- `cargo check -p norte-tui --lib`: fails, but **not from any file in this
  task**. Current failures are in `ui/pane.rs`, `ui/panels.rs`,
  `ui/geometry.rs`, `ui/modals.rs` (all T15, not mine) — half-renamed
  functions (`filas_pintadas`, `ventana_del_pane`, `estado_de_lista`,
  `celda_cortada`, `ORGANIZE_EXISTENTE` etc.) from in-progress work in that
  task. None of these errors reference app.rs, mouse.rs, event_loop.rs,
  gestures.rs, dispatch.rs, logview.rs, keys.rs, subshell.rs or
  session_push.rs.
- `cargo check -p norte-tui --lib` **did** catch one real bug in a file
  that IS mine: `gestures.rs` line 204 called `cmd.args(resto)` where a
  previous pass had renamed the binding to `rest` everywhere else. Fixed
  (one-line, `resto` → `rest`). This is the only functional fix made; every
  other change is text-only (comments, doc comments, log/panic/error
  messages, and local-variable renames within function bodies).

## Coordinator's assert-value rule

Followed: only assertion **messages** (the diagnostic string argument) were
translated. Where an assertion's expected value is itself test data I fully
control within the same test (e.g. logview.rs's
`tracing::info!("from this terminal")` paired with
`assert_eq!(texts, ["from this terminal", "from the daemon"])`), both
producer and assertion were translated together, consistently, so there is
no mismatch. Where the expected value is a VPath/path literal not
essential to translate (session_push.rs test fixtures above), it was left
untouched rather than risk it.

## Summary

Files done, comment/doc/message translation complete (verified via
`grep -P '[áéíóúñÁÉÍÓÚÑ¿¡]'` returning only intentional encoding-test data in
subshell.rs, `'ñ'`): app.rs, mouse.rs, event_loop.rs, gestures.rs,
dispatch.rs, logview.rs, keys.rs, subshell.rs, session_push.rs — all nine of
T12's files. Local variables (fully private to a function body) were
renamed to English throughout; struct/enum/fn names crossing to other files
were left in Spanish and are listed in section 1 for phase 2. One
pre-existing compile bug (unrelated file breakage aside) was fixed in
gestures.rs. `cargo check -p norte-tui --lib` is red only because of
other tasks' (T15) in-progress files; none of the nine files here appear in
its error output.
