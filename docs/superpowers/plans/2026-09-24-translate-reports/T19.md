# T19 — translate-to-english — norte-frontend/src/{overlays,pane,sync,view}/, presets/, tests/, build.rs, Cargo.toml

Status: **COMPLETE**. Every file in scope is translated. Full-crate
verification: `cargo check -p norte-frontend --all-targets` (clean), `cargo
test -p norte-frontend` via `just t norte-frontend` (nextest — 1381 passed, 0
failed), `cargo test -p norte-frontend --doc` (269 passed, 0 failed), `cargo
doc -p norte-frontend --no-deps` (clean, no broken intra-doc links), `cargo
clippy -p norte-frontend --all-targets -- -D warnings` (clean within my
scope — the one remaining finding, `task_strip.rs:406`, is a file outside
T19's ownership and untouched by me).

Scope actually covered: `overlays/*` (9 files), `pane/{mod,marks,quick,
tests,viewport}.rs`, `sync/{mod,rel,labels,plan,render,state}.rs`,
`view/{format,diskmap,treemap,columns,display,viewer}.rs`,
`presets/keymap/{orthodox,vim,cua,far,krusader,norton,total-commander}.toml`,
`presets/layout/*.toml` (already clean, no changes needed),
`tests/chord_es.rs`, `build.rs`, `Cargo.toml`. `view/columns.rs` (3838 lines)
was by far the largest piece of work in this task — roughly 490 lines
containing Spanish when I started on it, translated down to the one
intentional Unicode test fixture.

## 1. Pub identifiers kept Spanish/foreign-named (report, not renamed)

Cross-crate callers exist for all of these (confirmed via `rg -w` across the
whole `crates/` tree), so the names were left exactly as they are and only
their documentation was translated:

- `crates/norte-frontend/src/overlays/connections_picker.rs`:
  `Row::buena`, `Row::inservible` — used by `crates/norte-tui/src/dispatch.rs`.
- `crates/norte-frontend/src/view/format.rs`: `pub fn hora_utc` — used by
  `crates/norte-ui-host/src/controller/timeline.rs`, `.../logpanel.rs`, and
  `crates/norte-tui/src/ui/panels.rs`.
- `crates/norte-frontend/src/view/diskmap.rs`: the whole public surface —
  `DiskMap`, `Estado` (`Quieto`/`Midiendo`/`Hecho`/`Fallo`), and `DiskMap`'s
  methods `informe`/`elegido`/`estado`/`apuntar`/`midiendo`/`fallo`/
  `aterrizar`/`mover`/`elegir` — used by `crates/norte-tui/src/{jobs/diskmap,
  ui/panels, screens/side_nav, diskmap}.rs` and
  `crates/norte-ui-host/src/controller/diskmap.rs`. All doc comments and
  private test helpers were translated; the module doc now carries an
  explicit note listing these as a phase-2 cross-file rename candidate.
- `crates/norte-frontend/src/view/viewer.rs`: `Clase` (variants `Imagen`,
  `Otro`), `clase_por_nombre`, `hermana` — used by
  `crates/norte-tui/src/viewer_open.rs`,
  `crates/norte-ui-host/src/controller/{preview,viewer}.rs`. Also
  `pub fn senalar` on `PaneState` (defined in `pane/mod.rs`, listed below)
  is called from viewer-adjacent code in those same two crates.
- `crates/norte-frontend/src/pane/mod.rs`: `pub fn senalar(&mut self, entry:
  usize)` on `PaneState` — used by `crates/norte-ui-host/src/controller/
  {preview,viewer}.rs` and `crates/norte-tui/src/{viewer_open,app/pane}.rs`.
  I initially renamed this to `point_at` and it broke both crates; reverted
  and left the name (and all Spanish-named callers of it in `pane/tests.rs`)
  exactly as before. Its parameter was safely renamed `entrada`→`entry`
  (parameter names are never part of the cross-crate contract).

## 2. Cross-file literals kept as-is

- `crates/norte-frontend/src/pane/tests.rs`: the VPath fixture literals
  `"casa"` and `"fotos"` are kept unchanged. `"fotos"` appears in golden
  JSON fixtures (`crates/norte-proto/tests/golden/types/*.json`), and
  `"casa"` appears across dozens of unrelated files/crates; I initially
  translated both to `"home"`/`"photos"` for consistency and reverted after
  discovering this via `rg -F`.
- `crates/norte-frontend/tests/chord_es.rs`: this file specifically tests
  Spanish-language chord rendering (`set_chord_lang(Lang::Es)`), so its
  expected values — `"AvPág"`, `"RePág"`, `"Retroceso"`, `"Intro"`,
  `"Inicio"`, `"Mayús+Supr"`, `"Alt+Mayús+C"` — are genuine localized output
  the code produces and must stay Spanish. Only the module doc, the test
  function name, and the inline comments were translated.

## 3. UI strings outside Fluent (report, not translated)

None found beyond the chord_es.rs case above (which is intentional Spanish
localization output, not a stray hard-coded string bypassing Fluent). No
`t!()`-eligible literal was found hard-coded in the files I own.

## 4. Spanish file/module names

None. All files in scope already had English file and module names; only
their contents needed translation.

## 5. Glossary

No new terms beyond consistent renderings already used by other phase-1
tasks. Terms coined/reused in this task: escalera→ladder, peldaño→rung,
cede→yields, ocultar→hide, compactar→compact, reparto→split/distribute,
sobrante→leftover, huella→fingerprint, cabecera→header, saneo/enmascarar→
sanitize/mask, atestigua→attests, chord/acorde→chord (kept as-is, it's the
domain term), preset→preset (kept), gestos de panel→pane gestures.

## 6. `TODO(translation)` count

0. No comment was found to be factually wrong or requiring a
`TODO(translation)` marker; everything was translated faithfully in place.

## 7. cargo check / gate result

- `cargo check -p norte-frontend --all-targets`: clean, checked repeatedly
  during the session (~10 times) as files were completed.
- `just t norte-frontend` (nextest): **1381 passed, 0 failed, 0 skipped.**
- `cargo test -p norte-frontend --doc`: **269 passed, 0 failed** (doctests
  are not run by nextest — checked explicitly per the repo's documented
  blind spot; several doctests live in files I translated, e.g.
  `view/columns.rs::format_mode_rwx`, `view/viewer.rs::hermana`, `view/
  viewer.rs::image_dimensions`, `view/viewer.rs::clase_por_nombre`).
- `cargo doc -p norte-frontend --no-deps`: clean, no intra-doc-link errors
  (also not covered by nextest/clippy, checked explicitly). While
  translating `view/columns.rs`'s ladder-doc comment I found and fixed a
  pre-existing broken intra-doc link, `[ColumnsSettings::pone_sus_columnas]`
  (a method that had since been renamed to `has_user_columns`) — updated
  the link target so `cargo doc` stays clean.
- `just c` (workspace-wide clippy): failed, but every failure is in a file
  outside T19's ownership and none were introduced by my changes — verified
  by running `cargo clippy -p norte-frontend --all-targets -- -D warnings`
  in isolation, which fails ONLY on `crates/norte-frontend/src/task_strip.rs:
  406` (a `long`/`forms` similar-names lint, a file I never touched and
  which is outside my assigned directories). The other workspace-wide
  failures seen (`norte-vfs-archive/src/zip_cd.rs`, `norte-client/src/
  remote/mod.rs`) belong to other concurrently-running phase-1 tasks
  (T21/T22/T23 per the controller's background-agent list) and were left
  untouched, per the plan's instruction to report and stop rather than fix
  someone else's file.

## Notable fixes and corrections inside my own scope

- **Cross-crate rename breakage, caught and reverted**: renamed
  `PaneState::senalar`→`point_at` in `pane/mod.rs` without first checking
  usage outside `norte-frontend`; this broke `cargo check -p norte-tui -p
  norte-ui-host` with `no method named 'senalar' found` in two files each.
  Reverted the rename and the three call-site changes in `pane/tests.rs`;
  re-verified `cargo check -p norte-tui -p norte-ui-host --all-targets`
  clean afterward. Lesson applied for every subsequent rename in this task:
  `rg -w "<name>" -g '*.rs' crates` across the whole tree before renaming
  any `pub` item.
- **Controller's mid-task rule on assertion expected values**: reviewed all
  prior `assert_eq!`/`assert!` edits for compliance with "never translate
  the value the code produces, only the message." No violation was found
  beyond the already-caught casa/fotos case above; all other test fixture
  changes were pairs I controlled end-to-end (e.g. translating both a raw
  input byte string and its correspondingly-recomputed expected slice
  together, verified by hand — e.g. `view/viewer.rs`'s
  `the_viewer_scrolls_horizontally_with_a_cap` test, where `"corta"` became
  `"short"` and I re-derived the truncated substrings by hand rather than
  guessing).
- **Reformatting mid-edit**: the PostToolUse auto-format hook reflowed
  several regions after large `Write`/`Edit` calls (mostly in `view/
  columns.rs` and `view/viewer.rs`); subsequent `Edit` calls that targeted
  those regions were retried against the reformatted content after a
  `Read`.
