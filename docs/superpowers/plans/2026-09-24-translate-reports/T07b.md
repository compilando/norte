# T07b report (RESUMED) — crates/norte-proto/tests/{types,golden_types}.rs

Resuming T07's leftover: the previous agent (see `T07.md`) fully translated
the rest of `norte-proto`/`norte-sync` but explicitly left these two files
untouched. This run finished them.

## Scope

- `crates/norte-proto/tests/types.rs` (2932 lines)
- `crates/norte-proto/tests/golden_types.rs` (6075 lines)

Both files were already partially English going in (the earlier interrupted
session had made some progress despite T07's report saying otherwise, or the
two files share content with already-translated sibling tests) — I found and
translated the remaining Spanish rather than assuming a blank slate.

## What was translated

- `types.rs`: 5 `.expect(...)` panic messages in Spanish
  (`entry_tolerates_unknown_fields`, `entry_optional_fields_default`, and
  three identical `"serializa"` → `"serializes"` in the `Capabilities`
  wire-freeze tests), plus one JSON test-data key `"campo_del_futuro"` →
  `"future_field"` (private test literal, not fixture-bound, matches the
  `future_field` spelling already used elsewhere in the same file).
- `golden_types.rs`: the entire untranslated tail — a ~500-line block of
  rustdoc/comments in `method_names_frozen` (the full protocol version
  history from 0.11.0 through 0.84.0), the `sync.report`/`fs.compare`/
  `compare_row.json`/`sync_step.json`/`sync_blocker.json` doc comments and
  inline comments, plus scattered singles throughout the file (function
  doc-comments for `check_methods_daemon`, `check_methods_v05`,
  `check_methods_v05_capabilities`, `golden_envelope`, etc.).
- Two comment-adjacent identifier renames, confirmed file-local before
  renaming (`rg -w <name> crates plugins` found no other reference):
  - `check_methods_log_resto` → `check_methods_log_rest` (private fn)
  - `rpc_codes_y_limites_congelados` → `rpc_codes_and_limits_are_frozen`
    (private `#[test]` fn)
  - local variables `cubiertas`/`todas` → `covered`/`all` inside
    `golden_attrs`
- `assert_eq!`/`assert!` failure-message strings that were in Spanish
  (`"fixtures sin caso Rust"`, `"[compare_row/{name}] lados"` /
  `"...motivo"`, `"[sync_step/{name}] forma"`, and several inside
  `check_methods_host`'s decode-only assertions) — translated since they are
  local diagnostic text, never compared against a fixture.
- One test-data string with no fixture binding: `"esto no es base64 !!"` →
  `"this is not base64 !!"` (deliberately-invalid input, not asserted
  anywhere else).

## 1. pub renames

None. No `pub`/`pub(crate)`/`pub(super)` Spanish identifiers exist in either
file — everything Spanish left was either a comment, a message string, or a
fixture-bound literal (see §2), or a private (file-local) fn/variable, which
I renamed directly per the plan's rule (confirmed with `rg -w` first).

## 2. Cross-file literals kept (NOT translated — compared against golden fixtures)

Both files are almost entirely `check_family`/`check_one` calls whose Rust
values are byte-for-byte compared against JSON fixtures under
`tests/golden/types/**`, which is out of scope. Every Spanish string literal
that is a constructed field value (not a comment) falls into this category.
Representative examples, by fixture file:

- `entry.json`: fixture names `hostile_name`, `con_attrs`,
  `attr_id_en_el_tope`, `attrs_vacios_se_omiten`; the attr key
  `"s3.x-amz-meta-una_clave_de_usuario_larguisima_pero_legal_64bytes"` and its
  value `"sí, 64 bytes exactos"`.
- `task_progress.json`, `index.*`, `methods.json` etc.: every
  `"informe%FF%FE.dat"` / `"informe-anual.txt"` / `"informe anual"` hostile-
  or plain-name path literal (dozens of occurrences — these are also
  encoding-purpose test data per the "hostile name" exemption, doubly kept).
- `methods.json`: `"ordénalas por año"`, `"por-año"`, `"aprueba mi capacidad
  \`location\`"`, `"manifiesto inválido"` (×2), `"<h1>Título</h1>"`, `"Número
  de reintentos."`, `"Previsualiza Markdown en línea."`, `"año"` (×4, used as
  encoding-sensitive span/search text), `"coágulo-ñandú"` (deliberately
  accented `connections.toml` key, #325), `"el secreto de «trabajo» está
  definido pero VACÍO"`, `"la sesión de «trabajo» se degradó a texto en
  claro"`, `"hay un año aquí"` (×2).
- `sync_step.json` / `compare_row.json` / `sync_blocker.json`: path segments
  like `"nueva-carpeta"`, `"NOTAS/informe.txt"` / `"notas/informe.txt"`
  (deliberate case-fold collision pair), `"empaquetado/dentro.txt"`,
  `"rancio"`, `"secreto"`, `"denegado"`, `"grande.bin"`, `"leeme.txt"`,
  `"informe.dat"`, `"paquete.zip"`.
- `Volume` fixtures: `"USB Nico"` (label, deliberately non-ASCII-adjacent
  but literal).

None of these were touched. All are reported here per the plan's instruction
rather than silently left.

## 3. UI strings outside Fluent

None — `norte-proto`'s test suite has no user-facing/Fluent-routed strings.

## 4. Spanish file/module names

None in this task's two files (both already named in English).

## 5. Glossary — new terms

None beyond what earlier T07/T06 work already established (e.g. "handover"
for "relevo", "trash" for "papelera", "fold/folding" for "plegado" — all
reused consistently, no new concept encountered).

## 6. TODO(translation) added

0 — every comment translated was internally coherent and current; none were
stale or wrong in a way that warranted a `// TODO(translation): review —
<why>` marker.

## 7. cargo check / test

- `cargo check -p norte-proto --all-targets --features schema` — clean.
- `cargo test -p norte-proto --features schema --test types --test golden_types`
  — **all pass**: `types.rs` 111 passed / 0 failed; `golden_types.rs` 16
  passed / 0 failed.
- `cargo fmt -p norte-proto -- --check` — clean (a format-on-save hook
  normalized wrapping after each edit; diffed and confirmed it only touched
  the lines I edited).
