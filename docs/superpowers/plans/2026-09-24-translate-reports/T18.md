# T18 — translate-to-english — norte-frontend/src/{chrome,keymap,layout,navigation,ops}/

Status: **COMPLETE**. All 37 files across the five owned directories are
translated. Full-crate verification: `cargo test -p norte-frontend --lib`
(1380 passed), `cargo test -p norte-frontend --doc` (269 passed), `cargo doc
-p norte-frontend --no-deps` (clean, no broken intra-doc links), `cargo
clippy -p norte-frontend --all-targets -- -D warnings` (clean within my
scope — the one remaining finding, `task_strip.rs:406`, is outside T18's
ownership).

## 1. Pub/pub(crate) identifiers kept Spanish (report, not renamed)

Cross-crate callers exist for all of these, so the names were left exactly
as they are and only their documentation was translated:

- `crates/norte-frontend/src/chrome/frame.rs`: `pub const ZONA_PERMITIDA`,
  `pub fn zona_puede` (used by `norte-ui-host/src/{bridge,action}.rs`,
  `norte-ui-host/src/controller/panelplugin.rs`,
  `norte-tui/src/mouse.rs`, `norte-frontend/src/view/treemap.rs`);
  `StyledFrame::de_wire` (used by `norte-ui-host/src/controller/panelplugin.rs`,
  `norte-tui/src/panelplugin.rs`).
- `crates/norte-frontend/src/chrome/menu.rs`: `pub fn role` (used by
  `norte-ui-host/src/controller/views.rs`, `norte-ui-host/src/dto.rs`,
  `norte-tui/src/ui/chrome.rs`) — "role" happens to already be English, so
  no translation was actually being withheld, just confirmed not to rename.
- `crates/norte-frontend/src/chrome/panelbar.rs`: `pub fn cifra` (used by
  `norte-ui-host/src/controller/views.rs`, `norte-tui/src/app/layout.rs`,
  `norte-tui/src/ui/chrome.rs`) and `pub fn es_boton` (used by
  `crates/norte-frontend/src/layout/kinds.rs`, my own file but still pub).
- `crates/norte-frontend/src/layout/roles.rs`: `pub fn con_active` on
  `Roles` (used by `norte-tui/src/app.rs`,
  `norte-ui-host/src/controller/mod.rs`; candidate rename `with_active`).
- `crates/norte-frontend/src/ops/organize.rs`: `pub fn resumen` (used by
  `norte-tui/src/ui/modals.rs`, `norte-ui-host/src/controller/organize.rs`;
  candidate rename `summary`).
- `crates/norte-frontend/src/navigation/goto.rs`: a whole family of pub
  Spanish names with confirmed cross-crate/cross-module users — `SECCION_*`
  consts, `ORDEN`, `Accion`, `accion`, `parece_ruta`, `fila_ruta`,
  `fila_conexion`, `filas_de_comandos`, `filas_del_indice`, `FixedSource`
  (English already), `RutaSource`, `GotoSource::ya_filtrada` /
  `solo_con_consulta`, `TRAIDAS_POR_LISTA`, `MINIMO_PARA_EL_INDICE`,
  `TOPE_DEL_INDICE`. None renamed; all doc comments translated.

## 2. Cross-file literals kept as-is (per mid-task coordinator instruction)

- `crates/norte-frontend/src/keymap/{layer,mod}.rs`: the string literals
  `"usuario"` and `"preset"` used as `KeymapError::WrongLayerKey { layer:
  … }` values (9 occurrences total) are untouched — `crates/norte-tui/tests/keymap.rs`
  (another task's file) pattern-matches them.
- `crates/norte-frontend/src/keymap/mod.rs` / `presets.rs`: two `#[test]`
  fn names are kept Spanish because they are referenced BY NAME from a
  comment in `crates/norte-tui/src/keymap.rs` or from a doc comment inside
  my own `mod.rs` that itself is cross-referenced: `todo_planned_tiene_motivo_e_issue`,
  `ningun_preset_ata_un_acorde_que_el_terminal_no_entrega`.
- Throughout `keymap/*.rs`, essentially every `#[test]` fn name (~110
  across the module) is left in Spanish as a blanket policy: many have
  exact name-twins in `crates/norte-tui/tests/keymap.rs`, a different
  task's file, discovered by systematic `rg -w` batches. Only bodies,
  doc-comments and assert messages were translated for these.

## 3. UI strings outside Fluent (report, not translated)

- `crates/norte-frontend/src/keymap/chord.rs`, private fn `spanish_name`
  (renamed from `nombre_es`, confirmed confined): its RETURN VALUES are
  genuine Spanish chord-display labels (`"Mayús"`, `"Intro"`, `"Retroceso"`,
  `"Espacio"`, `"Más"`, `"Inicio"`, `"Fin"`, `"RePág"`, `"AvPág"`, `"Supr"`,
  plus the arrow glyphs) and were left untouched — they are real localized
  output, not code-Spanish.
- `crates/norte-frontend/src/chrome/panelbar.rs` tests: Spanish test data
  `"Sitios"`, `"Procesos"`, `"Registro"`, `"Árbol"`, `"Visor"` kept
  deliberately — the tests demonstrate genuine bilingual/accented-character
  letter-derivation behavior, not code comments.
- `crates/norte-frontend/src/chrome/statusbar.rs`: `assert_eq!(of("sort").as_deref(),
  Some("Tamaño ↓"))` kept unchanged — real `Lang::Es` Fluent-translated
  output, not a stray Spanish literal.

## 4. Spanish file/module names

None. All 37 files already had English file and module names; only their
contents needed translation.

## 5. Glossary additions

No new terms beyond what earlier phase-1 sessions already established
(vía `docs/superpowers/plans/2026-09-24-translation-glossary.md`, if present).
Consistent renderings used throughout this task: hueco→slot, reparto→
distribution/split, pestaña→tab, acoplar/dock→dock, cromo→chrome, rama→
branch, revelar→reveal, plegar→fold/collapse, desplegar→unfold/expand,
sitio→place, ancla→anchor.

## 6. `TODO(translation)` count

0. No comment was found to be factually wrong; all were faithfully
translated as-is, per the plan's rule that a stale-but-accurate comment
still gets translated verbatim.

## 7. cargo check / gate result

- `cargo check -p norte-frontend --all-targets`: clean.
- `cargo test -p norte-frontend --lib`: **1380 passed, 0 failed.**
- `cargo test -p norte-frontend --doc`: **269 passed, 0 failed** (doctests
  are not run by nextest, so this was run explicitly per the repo's
  documented blind spot).
- `cargo doc -p norte-frontend --no-deps`: clean, no intra-doc-link errors
  (also not covered by clippy/nextest, checked explicitly).
- `cargo clippy -p norte-frontend --all-targets -- -D warnings`: one
  finding fixed by me (`layout/tree.rs`: a `let three` binding too similar
  to a nearby `let tree`, renamed to `triple`); the only remaining finding
  (`task_strip.rs:406`, a `long`/`forms` name-similarity lint) is in a file
  outside T18's ownership and was left for whichever task owns it.
- During the session, `cargo clippy`/`cargo check` runs across the whole
  workspace intermittently failed in files NOT owned by T18 (`norte-client`,
  `norte-vfs-archive`, `norte-frontend/src/view/columns.rs`,
  `norte-frontend/src/task_strip.rs`, `norte-frontend/src/pane/*`,
  `norte-frontend/src/sync/mod.rs`, `norte-frontend/src/settings.rs`,
  `crates/norte-config/src/profiles.rs`, `norte-tui/src/ui/{panels,pane}.rs`)
  — all confirmed to belong to other concurrently-running phase-1 tasks and
  left untouched, per the plan's instruction to report and stop rather than
  fix someone else's file.

## Notable fix inside my own scope

`crates/norte-frontend/src/ops/compare.rs` and `keymap/rebind.rs` each had
one place where translating an `#[error(...)]`/comment elsewhere in my
scope required re-deriving a downstream English literal rather than
guessing: `keymap/rebind.rs`'s `the_classifier_and_the_loader_agree` test
asserted old Spanish substrings against `KeymapError`'s now-English
messages and was updated to the actual new text I had written
(`"ambiguous sequences"`, `"count"`, `"esc"`, `"empty"`, `"invalid key"`),
verified against the real `Display` output, not guessed.
