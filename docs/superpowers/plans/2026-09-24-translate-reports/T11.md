# T11 — `crates/norte-ui-host/tests/`, `crates/norte-ui-host/Cargo.toml` (RESUMED)

Scope per the plan: every file under `crates/norte-ui-host/tests/` (35 `.rs`
files across `tests/`, `tests/controller/`, `tests/backend_falso/`, plus the
`golden/*.json` fixtures, which are off-limits) and `crates/norte-ui-host/
Cargo.toml`. This task had already been started by a previous agent that was
cut off mid-way; my job was to scan every file and translate only what was
still Spanish.

All 35 `.rs` files are now free of comment/rustdoc/message Spanish, except
three deliberately-kept spots (see §2). `Cargo.toml` had nothing to translate.

Two systematic misses from before I resumed were caught and fixed across the
whole scope, not just the file I was working in when I found them:

- `.expect("host vivo")` → `.expect("host alive")` — 38 occurrences in
  `sync.rs`, 10 in `transferencias.rs`, 2 in `renombrar.rs`. Not caught by the
  accented-character proxy check because the string has no accents.
- `.expect("sesión")` / `.expect("catálogo")` / `.expect("semánticas")` /
  `.expect("semánticos")` → `.expect("session")` / `"catalog"` /
  `"semantics"` — 16 in `base.rs`, 1 each in `relevo.rs` and `gestos.rs`, 4 in
  `backend_falso/mod.rs`, 1 in `busqueda.rs`, 1 in `ir_a.rs`.

After fixing these, I ran a broader manual sweep of all `expect(...)` and
quoted-string literals across the scope looking for other non-accented
Spanish leftovers; nothing else turned up.

## 1. pub renames (not done — cross-file)

Field/type names that would need a cross-crate check before renaming, found
but left alone:

- `Falso.sesion`, `Falso.catalogo`, `Falso.semanticos`,
  `Falso.semanticas_pedidas` (in `backend_falso/mod.rs`) — used by every test
  file in scope via `fake.sesion.lock()` etc. Renaming the field is a
  `backend_falso`-wide change touching all 27 controller test files; I
  translated only the `.expect()` messages describing the lock, not the field
  names themselves.
- `Estado::Estados`/`EstadoPanel`-style `pub(super)` types are out of this
  task's scope (they live in `src/controller/`, owned by T09/T10).

## 2. cross-file literals (kept, verified)

- `"el servidor negoció un perfil antiguo"` (`registro.rs:1276,1296`) — an
  identical assertion exists in `crates/norte-frontend/src/chrome/banners.rs`
  (outside my scope); keeping both in sync is required, so left untouched in
  both places.
- `"PDF de ACME"` (`visor.rs:255,287`) — appears verbatim in
  `tests/golden.rs:1105` and in `golden/updates.json` / `golden/changes.json`
  as `"via PDF de ACME"`. Kept unchanged. The *other* two strings in that same
  `preview_de(...)` call, `"Informe anual"`/`"Página 1 de 12"`, are NOT
  golden-tied (verified with `rg` across `tests/`) and are asserted only
  within the same test function, so I translated them consistently to
  `"Annual report"`/`"Page 1 of 12"` (and the matching
  `l.contains("Informe anual")` assertion).
- `said.contains("caída") || said.contains("caídas")` (`gestos.rs:744`) — this
  is the actual status message text produced by application source code
  (`norte-frontend`/`norte-ui-host` src, both out of scope for this task, and
  the fake host runs with `locale: "es"`), i.e. an **expected/compared
  value**, not test prose. Left untouched per the corrected rule on never
  translating what the code under test produces.
- `"facturas del año pasado"` / `"invoices from last year"`: this one WAS
  translated (`busqueda.rs:927,957`) because it is self-authored dialog input
  text asserted only within the same test (`requested[0].0`), not a
  golden/fixture/produced value — updated consistently on both sides.
- `"sftp://servidor/..."` fixture URLs in `sync.rs` (13 occurrences): verified
  with `rg` that `"servidor"` does not appear in any other file under
  `tests/`; translated consistently to `"sftp://server/..."` across every
  occurrence (setup, host-URL argument, and the two `.contains("servidor")`
  assertions).

## 3. UI strings outside Fluent

None found — all user-visible strings in these test files are either
plugin/backend-fixture data (kept per §2) or already routed through
`norte_i18n`/pre-rendered Fluent output from `norte-frontend`.

## 4. Spanish file/module names (not renamed, reported only)

`ajustes_escritura.rs`, `ajustes_extensiones.rs`, `atributos_procesos.rs`,
`ayuda.rs`, `busqueda.rs`, `catalogo_del_host.rs`, `columnas.rs`,
`comparar.rs`, `config_cobertura.rs`, `corpus.rs`, `disposiciones.rs`,
`gestos.rs`, `ir_a.rs`, `linea_de_tiempo.rs`, `organizar.rs`,
`paneles_de_plugin.rs`, `paneles.rs`, `paridad.rs`, `registro.rs`,
`relevo.rs`, `renombrar.rs`, `revisiones.rs`, `sondas.rs`, `splash.rs`,
`sync.rs`, `teclas_paleta.rs`, `transferencias.rs`, `backend_falso/mod.rs` —
28 Spanish file names under this task's directories. `main.rs`, `base.rs`,
`visor.rs`, `dependency_boundary.rs`, `daemon_e2e.rs`, `parity.rs`,
`payload.rs`, `golden.rs` are already English or neutral. Not renamed: `mod`
declarations live in `controller/main.rs`'s `mod` list, which is shared
infrastructure I'd rather not touch file-name-wise without the coordinator
deciding a single pass for the whole `controller/` module (same reasoning as
T10 §4).

## 5. glossary additions

None beyond `TRANSLATION_GLOSSARY.md`; used existing terms (slot, listing,
pane, layout, session, catalog, semantics/semantic, request).

## 6. TODO(translation) count

**2**, both added by the prior agent before the resume and left as-is (I did
not add new ones during this session):

- `sync.rs` — a comment said the first dispatched key was `` `a` `` but the
  code below dispatches `"y"`; translated faithfully and flagged rather than
  "fixing" the mismatch.
- `transferencias.rs` — a doc comment for one test was interleaved with the
  next test's (an orphaned `/// saberlo.` line dangling before the next
  `#[tokio::test]`); translated each fragment in its original scrambled
  position and flagged it.

## 7. cargo check

`cargo check -p norte-ui-host --all-targets` — clean, 0 errors, 0 warnings.
Ran twice: once after finishing `sync.rs`, once again after the `"host
vivo"`/`"sesión"` sweep across `base.rs`, `relevo.rs`, `gestos.rs`,
`backend_falso/mod.rs`, `busqueda.rs`, `ir_a.rs`, `transferencias.rs`,
`renombrar.rs`.

## Summary

T11 is complete: all 35 test files plus `Cargo.toml` in
`crates/norte-ui-host/tests*` are translated (comments, rustdoc, log/panic/
assert *messages*, and safely-verified self-contained test data), with three
literals deliberately kept in Spanish because they are tied to golden
fixtures, a sibling crate's assertion, or the actual (untranslated,
out-of-scope) text the application under test produces; two systematic
non-accented misses (`"host vivo"` and the `sesión`/`catálogo`/`semántica*`
lock-expect messages) from before the resume were found via a broader sweep
and fixed across every file in scope, not only where first noticed; no
identifiers were renamed across file boundaries; `cargo check -p norte-ui-host
--all-targets` is clean.
