# T03 report — norte-core, "every other file" + backend/policy/rename/ui_session/volumes/sync

Scope: 45 files (`core/{benches,Cargo.toml,clippy.toml}`, `core/src/lib.rs`, and every
`core/src/*.rs` not owned by T01/T02, plus `core/src/{backend,policy,rename,ui_session,
volumes,sync}/`). All translated to English (comments, doc comments, log/error/panic
messages, test names and assertion messages), with faithful content — no rewording or
shortening.

## 1. `pub`/`pub(crate)`/`pub(super)` renames NOT made (candidates, kept as-is)

Confirmed cross-file via `rg -w <name> crates plugins` and left unchanged; only their
doc comments were translated:

- `blocking.rs`: `spawn_raiz` (`pub(crate)`) — used by `daemon/server.rs` (T04),
  `scheduler.rs` (mine), `tests/spans_en_spawn.rs` (T05).
- `archive_config.rs`: `aplicar` (`pub async fn`) — used by `norte-mcp`,
  `daemon/componer.rs` (T04), `norte-cli/src/main.rs`, `norte-cli/src/cmd/ai.rs`.
- `equipo.rs`: `Aviso` enum and all its variants (`SpoolsBarridos`,
  `SpoolsAMedias{removed,failed,dir}`, `SpoolsSinBarrer{dir,error}`, `SinIndice`,
  `IaNoDisponible`, `IaEmbeddings`, `IaInvalida`, `IaNoCargo`, `ArchivoInvalido`),
  `Equipado{avisos, ia_renombrado}`, `Ia{renombrado, embeddings, NADA, TODA}`,
  `equipar`, `con_indice` — all heavily used across `norte-cli`, `norte-tui`,
  `norte-core/daemon`.
- `anchor.rs`: `de_nodo`, `casa` (`pub fn`) — used by `engine.rs` (T01).
- `organize.rs`: `.pasos()`, `.destino()`, `.carpetas()` (`pub` methods on
  `OrganizePlan`, Spanish names) — `.pasos()` confirmed called from `engine.rs`.
  (`OrganizePlan` and `plan_hash` themselves are already-English names, unaffected.)
- `compare.rs`: `degradada` (`pub(crate)` fn) — used by `sync/mod.rs` (mine) and
  `engine.rs` (T01).
- `scheduler.rs`: `Lane::Paralelo` / `Lane::Cola` (`pub` enum variants) — used by
  `engine.rs`. `submit_en`, `mover_en_cola` (`pub` fns) — used across `norte-client`,
  `norte-ui-host`, `norte-tui`, `daemon/server.rs`, `engine.rs`.
- `index_embed.rs`: `Puntuado`, `mejores_k` (`pub(crate)`) — used by `engine.rs`.
- `backend/plugins.rs`: struct-literal fields `contexto:`/`evento:` on
  `crate::plugins::PanelCall` (the struct itself is T01's, already flagged in T01's
  file) — kept because the struct's own field names are cross-file; my local
  variables feeding them are named `context`/`event` in English.

Everything else with a `pub`/`pub(crate)` surface in these 45 files was confirmed
private-only or already English, and was left as found or renamed freely (locals,
private fns, private consts, `#[cfg(test)]` items).

## 2. Cross-file literals kept (appear in golden/doc/other-task files)

- `ui_session/disk.rs`: test fn name `las_dos_versiones_de_esquema_van_del_brazo`
  is a real, cited fn name in `crates/norte-tui/tests/session.rs` (`rg -F` confirmed)
  — kept in Spanish, not renamed.
- `sync/spool.rs`: `a_spool_holds_no_content_only_paths_and_verdicts` is cited by
  name in `docs/superpowers/plans/2026-08-11-directory-sync.md:2925` — already an
  English name, no rename needed, noted for completeness.
- `sync/spool.rs`: a doc comment cites
  `engine_sync_plan::a_zero_step_plan_whose_owner_left_is_not_retained` — confirmed
  this matches the actual (already-translated) test name in
  `crates/norte-core/tests/engine_sync_plan.rs` (T05's file); no action needed.

## 3. UI-facing strings found outside Fluent resources

None found in this file set. `norte-core` is backend/daemon logic; the few
human-facing strings present are log/error/panic text (Spanish, now translated;
these are diagnostics, not UI copy) or already routed through `norte-help`/Fluent
elsewhere, outside this scope.

## 4. Spanish file/module names (not renamed — file renames out of scope)

- `crates/norte-core/src/equipo.rs` — Spanish module name ("team/outfit"); its
  `mod equipo` declaration lives in a file outside my scope, so renaming the file
  would require touching a declaration T01/T02 may own. Flagged, not renamed.
- `crates/norte-core/benches/copy_remoto.rs` and the matching
  `[[bench]] name = "copy_remoto"` in `Cargo.toml` — Spanish bench name; renaming
  the file/bench-name would change the `just`/CI invocation surface, left as-is
  and flagged.

## 5. New glossary terms

None beyond what's already in `TRANSLATION_GLOSSARY.md`. All domain terms
encountered (spool, tombstone/`is_dead`, claim, revalidate, witness, lane, queue,
anchor, organize plan) either already had an English name on the `pub` surface or
were straightforward private renames with no new convention to record.

## 6. `TODO(translation)` markers added

0. No file in this scope needed a marker — every non-renameable cross-file
   identifier is instead listed in section 1/2 above, and no ambiguous/uncertain
   translation was left half-done.

## 7. `cargo check -p norte-core --all-targets` result

Ran once (after finishing all 45 files). First run surfaced one real bug from my
own edits: a leftover reference to a renamed local (`..igual` instead of `..same`)
in `sync/exec.rs`'s `each_error_falls_into_its_own_cause`-adjacent test — fixed
in place (renamed the whole binding `otra_clase` → `other_kind` for clarity too).

Second run: **all files in T03's scope compile clean.** The remaining failure is
outside my ownership and unrelated to translation:

```
error[E0425]: cannot find function `run_column_values_for_test` in module `norte_core::plugins`
  --> crates/norte-core/tests/columns_media_e2e.rs:113:26
```

`run_column_values_for_test` is declared in `crates/norte-core/src/plugins.rs`
(T01's file, top-level `core/src/`, explicitly excluded from T03) behind
`#[cfg(any(test, feature = "testing"))]`; the integration test invoking it lives in
`crates/norte-core/tests/`, which is outside `core/src/` entirely and not part of
any phase-1 file list I'm aware of. This is not caused by anything in my 45 files
and I did not touch it.

## Incident note (transparency)

Mid-session a transient git stash/reset event (evidently a controller-level
coordination step across the ~25 parallel phase-1 agents sharing one working
tree) briefly showed several of my already-translated files reverted to Spanish.
It resolved itself for all but my most recent in-flight edits to `sync/exec.rs`
(a few functions/doc paragraphs), which I detected via direct file reads and
redid from scratch. A final full sweep (accented-character grep plus a Spanish
stopword grep) across all 45 files, performed after that incident and again just
now, found the tree clean except for intentional non-ASCII test fixtures
(`café`/`cafétière` NFC/NFD collision fixtures in `rename/plan.rs`, `sync/mod.rs`,
`sync/exec.rs`; the hostile `/media/USB de Ñico` mount-name fixture in
`volumes/linux.rs`) — all correctly left untouched.
