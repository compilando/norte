# T21 report — `crates/norte-plugin-host` (incl. `.wit` comments) + `crates/norte-cli`

## Process deviations (disclose up front)

- None. Every edit went through `Edit`/`Write`. One `sed -i` attempt early on
  (bulk rename in `sync.rs`/`compare.rs`) was rejected by the write-blocking
  hook as expected; switched to `Edit` with `replace_all: true`.
- Found and fixed one **pre-existing bug** unrelated to my translation work
  but discovered by the final `cargo check`: `crates/norte-cli/src/cmd/ai.rs`
  called a function named `backend_with_ai()` at line 28 while the function
  itself was defined as `backend_with_ia()` at line 206 (a typo from an
  earlier rename pass in this same task, before the context-compaction that
  split this session). Fixed the call site to match. Flagging since it is a
  compile error, not a style choice.

## 1. pub renames (report only — none required action)

- No `pub`/`pub(crate)`/`pub(super)` item in either crate needed a
  cross-file-checked rename that I skipped. `norte-plugin-host` is a library
  depended on by `norte-core`, `norte-compare` and `norte-mcp`; the final
  `cargo check -p norte-plugin-host -p norte-cli --all-targets` compiles the
  whole workspace graph through those dependents and is clean, confirming no
  public API surface was broken by the (file-internal) renames made inside
  `norte-plugin-host` (documented in the prior session's partial report:
  `resumen`→`digest_bytes`, `Compilados`→`Compiled`, etc., all private).
- `norte-cli` is bin-only (`[[bin]] name = "norte"`, no `[lib]`), so nothing
  in it is consumed by another crate; all renames there needed only
  in-crate consistency, which was checked with `rg` per rename
  (`rel_marcado`→`rel_marked`, `codigo_por_escritura`→`write_error_code`,
  `codigo_de_no_se_pudo`→`code_for_could_not`, `marcado`→`masked`,
  `texto_del_aviso`→`warning_text`,
  `AvisoDeJournalPorStderr`→`JournalWarningStderr`, `Importado`→`Imported`,
  `config_dir_del_test`→`test_config_dir` (independently duplicated per test
  binary, renamed identically in `smoke.rs`, `compare_e2e.rs`, `sync_e2e.rs`
  for consistency), plus the full `help.rs` test-fn sweep and various
  `cmd/audit.rs`/`theme.rs` private-fn renames listed in the diff).

## 2. cross-file literals (kept, not translated)

- `crates/norte-plugin-host/examples-wasm/provider-mem/src/lib.rs` and
  `provider-mem-rw/src/lib.rs`: kept the fixture strings `"hola norte\n"` and
  `"vacio.txt"` unchanged — referenced by
  `crates/norte-core/tests/plugin_provider_rw_e2e.rs`, which is outside this
  task.
- `crates/norte-cli/tests/sync_e2e.rs` / `smoke.rs`: kept fixture filenames
  from `tests/fixtures/themes/` (`hijo.jsonc`, `padre.json`, `ciclo-a.json`)
  as-is in `theme_import.rs` — actual files on disk, not renamed (also
  covered under §4).
- `crates/norte-cli/tests/theme_import.rs`: the theme-name/content literals
  `"mío"` (kept, self-consistent write/read-back test data, not a golden) —
  left as arbitrary test data, same category as `nord`/`noche`-style theme
  names, none of which are UI copy.

## 3. UI strings outside Fluent (kept in Spanish, reported)

`norte-cli`'s golden/no-Fluent rule applies to a large, now-stable set of
surfaces — unchanged from the prior session's partial report, confirmed
still accurate after finishing the crate:

- **The entire clap-derived surface in `crates/norte-cli/src/main.rs`**:
  `Cli`, `Cmd`, `SyncModeArg`, `AiCmd`, `IndexCmd`, `AuditCmd`, `AuditFormat`,
  `PluginCmd`, `McpCmd`, `PolicyCmd`, `DaemonCmd`, `SymlinksArg` — every
  `///` doc comment on these becomes `--help` text via clap derive. Left
  entirely in Spanish (comments, field docs, and the identifiers, which are
  irrelevant here since clap output is driven by the doc comments/arg
  attributes, not the Rust names).
- **`crates/norte-cli/src/theme.rs`**: the `ThemeCmd` enum (and its `Import`
  variant's field docs) — same reasoning, left in Spanish. Everything else in
  the file (module doc, comments, private fn/var names, test names) was
  translated.
- Hardcoded `anyhow::bail!`/`.context()`/raw `println!`/`eprintln!` literals
  that reach a terminal without going through `norte_i18n::t`/`ta`, kept in
  Spanish throughout: `daemon.rs` (`"--daemon no está disponible en Windows
  todavía (issue #33)"`, `"resolución del socket/exe"`, `"el puente MCP
  terminó con error"`, `"el daemon terminó con error"`, `"resolución del
  socket"`, one longer diagnostic string at ~line 331), `connect.rs`
  (`"path no representable: {}"`, and the `"s" | "si" | "sí" | "y" | "yes"`
  answer-parsing match arms — user input vocabulary, not output), `index.rs`
  (`.context("query del índice")`, `.context("búsqueda semántica")`),
  `plugin.rs` (`.context("instalando")`, `.context("desinstalando")`),
  `task.rs` (hardcoded `"{entries} entradas"` progress line).
- `doctor.rs`: every `detail: format!(...)` string that becomes a
  `Finding.detail` — these are printed directly (`doctor_finding_line` in
  `entorno.rs`) or serialized under `--json`, bypassing Fluent. Two remain:
  `format!("{name}: {e} (se arrancará con «orthodox»)")` and the
  `"[ui.columns] attr que parsea pero no es un id legal del wire…"` message.
  Everything else in `doctor.rs` (comments, ~34 test fn names, helper fn
  names) was translated.

## 4. Spanish file/module names (report only, not renamed)

- `crates/norte-cli/src/cmd/entorno.rs` — module/file name is Spanish
  ("environment"); not renamed (would ripple through `mod entorno;` in
  `cmd/mod.rs` and every `crate::cmd::entorno::...` path — out of this task's
  "own only my files" scope as a rename, though the file's *contents* were
  fully translated in the prior session).
- `crates/norte-plugin-host` fixture directory names under `examples-wasm/`
  (e.g. `provider-mem`, `ftp-provider`) are already English; no Spanish
  directory names found there.

## 5. glossary — new terms (this session's portion, `norte-cli` tail end)

- hecho/done (theme import result) → `done` (local var), `Imported` (struct)
- escribir/colocar → `write_atomic`/`place` (atomic temp-file write helpers)
- leer_cadena/leer_uno → `read_chain`/`read_one`
- canonica → `canonicalize_checked`
- nombre_por_defecto → `default_name`
- cabecera → `header`
- ids_para_mostrar → `ids_for_display`
- marcador (journal format marker, `audit.rs`) → `marker`
- cabeza (journal head, `audit.rs`) → `head`
- ya_anclado → `already_anchored`
- veredicto → `verdict` (already used as a type name elsewhere; local params
  now consistent)
- config_dir_del_test → `test_config_dir`
- hijo (child process guard struct in `smoke.rs`) → `ChildGuard`
- con_roto/roto_del_daemon/roto_del_cli → `with_broken`/`daemon_broken`/
  `cli_broken`
- ollama_de_mentira → `fake_ollama`
- tiene_peligro → `has_hazard` (`theme_import.rs`)
- intercambia (mock AI rename instruction passed as a CLI arg in
  `smoke.rs`) → `swap`

## 6. TODO(translation) added

0.

## 7. cargo check / gate result

- `cargo check -p norte-plugin-host -p norte-cli --all-targets`: **clean**
  after fixing the `backend_with_ai`/`backend_with_ia` mismatch described
  above (§ Process deviations). No other errors.
- `cargo test -p norte-plugin-host --doc`: 7 doctests, all pass (`norte-cli`
  has no doctests — it is bin-only).
- `cargo doc -p norte-plugin-host -p norte-cli --no-deps`: succeeds (exit 0).
  One pre-existing warning, unrelated to translation:
  `warning: public documentation for 'PluginRuntime' links to private item
  'EPOCH_TICK'` in `crates/norte-plugin-host/src/runtime.rs:461` — a
  `rustdoc::private_intra_doc_links` warning (not the denied
  broken-intra-doc-link lint), caused by the `[`EPOCH_TICK`]` link target
  being private, not by any text I translated. Reporting per CLAUDE.md's
  "three blind spots" note, but it does not fail the build and predates this
  translation pass (the link target's visibility was not touched).
- Full crate coverage confirmed via a final sweep
  (`grep -rlP '[áéíóúñÁÉÍÓÚÑ¿¡]'` plus non-accented Spanish word-pattern
  greps) across every `.rs` file in both crates, all `.wit` files, and
  `Cargo.toml`: the only remaining Spanish is the intentionally-kept material
  listed in §2 and §3 above, plus two intentional hostile/encoding
  test-fixture bytes (`"ñ"` char array and `"á".repeat(280)` in
  `norte-plugin-host`'s `model.rs`, and the RTL/masking test data in
  `norte-cli`'s `ls.rs`/`compare_e2e.rs`/`sync_e2e.rs`/`theme_import.rs`,
  which is the point of those tests).
