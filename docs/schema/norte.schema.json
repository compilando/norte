{
  "$defs": {
    "AiProviderEntry": {
      "additionalProperties": false,
      "description": "One `[ai.providers.<name>]` entry.",
      "properties": {
        "base_url": {
          "default": null,
          "description": "Base URL (required for `openai-compat`).",
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "description": "`anthropic` | `ollama` | `openai-compat`. An invalid value is not\nrejected at parse time — same pattern as `[ui] quick_search` — the AI\ngate rejects it at use, where the diagnostic can name the provider.",
          "type": "string"
        },
        "model": {
          "description": "Model id as the provider expects it.",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "model"
      ],
      "type": "object"
    },
    "AiSection": {
      "additionalProperties": false,
      "description": "The `[ai]` section of `norte.toml` (ADR 0031). All off by default.",
      "properties": {
        "denied_prefixes": {
          "default": [],
          "description": "Prefixes whose content/names never leave the process. Wire strings;\nvalidated to `VPath` during merge ([`crate::load::load`]). Merge is\na UNION across layers, not last-wins: a deny never disappears by\nadding a layer (ADR 0035).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "default": null,
          "description": "AI enabled. `None`/`false` = the gate rejects every operation.\n`Option` (not a plain `bool`) so layer merge distinguishes \"absent\"\n(inherit the lower layer's value) from \"explicitly false\".",
          "type": [
            "boolean",
            "null"
          ]
        },
        "local_only": {
          "default": null,
          "description": "Local-only mode: reject remote providers (spec §9). `Option` for the\nsame absent-vs-false reason as `enabled` above.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "providers": {
          "additionalProperties": {
            "$ref": "#/$defs/AiProviderEntry"
          },
          "description": "Declared providers (`[ai.providers.<name>]`). By-name, later-layer-wins\nmerge: a provider redeclared in a higher layer replaces the lower\nlayer's entry for that name, other names are untouched.",
          "type": "object"
        },
        "rename_provider": {
          "default": null,
          "description": "Provider name used for AI rename. By-name, later-layer-wins merge\n(same as other `Option` scalars in this schema).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ArchiveSection": {
      "additionalProperties": false,
      "description": "The `[archive]` section of `norte.toml` (#95.2): local anti-bomb limits\nfor browsing zip/tar/tar.gz containers. Absent values keep the compiled\ndefaults. Applied at startup on the embedded engine only — a container\nthat exceeds them fails with `LimitExceeded`, never silently truncates.",
      "properties": {
        "max_decompressed_bytes": {
          "default": null,
          "description": "Decompression budget in bytes for indexing a `tar.gz` (default 64 GiB).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_entries": {
          "default": null,
          "description": "Maximum indexed entries per container (default 500000).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_nesting": {
          "description": "`[archive] max_nesting` (#56): tope de capas de archivo anidadas.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "DaemonMode": {
      "description": "Core transport. This changes transport only, not behaviour.",
      "oneOf": [
        {
          "const": "embedded",
          "description": "Core in-process (default).",
          "type": "string"
        },
        {
          "const": "daemon",
          "description": "Connect to the Unix-domain-socket daemon (Unix only; ADR 0011).",
          "type": "string"
        }
      ]
    },
    "DaemonSection": {
      "additionalProperties": false,
      "description": "The `[daemon]` section of `norte.toml` (ADR 0011).\n\nTransport mode is selected at startup and is not hot reloaded. Changing it\nrequires restarting the frontend.",
      "properties": {
        "mode": {
          "anyOf": [
            {
              "$ref": "#/$defs/DaemonMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "`embedded` for immediate startup (the default), or `daemon`."
        },
        "socket": {
          "default": null,
          "description": "Daemon socket path. When absent, use the operating-system default.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "HotlistEntry": {
      "additionalProperties": false,
      "description": "One `[[hotlist]]` entry as stored in `norte.toml`.\n\n`path` contains an unvalidated wire value such as `scheme://...`, including\nvalid remote schemes. [`crate::load::load`] validates it as a `VPath`\nwhile merging layers. One invalid entry is handled independently and does\nnot prevent the rest of `norte.toml` from loading.",
      "properties": {
        "name": {
          "description": "Name displayed in the `Ctrl+D` popup.",
          "type": "string"
        },
        "path": {
          "description": "Path in wire form, not yet validated.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "path"
      ],
      "type": "object"
    },
    "KeymapSection": {
      "additionalProperties": false,
      "description": "The `[keymap]` section of `norte.toml`.",
      "properties": {
        "preset": {
          "default": null,
          "description": "Base preset (`orthodox`, `vim`, or `cua`). Inherit when absent.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "SchemeColumnsSection": {
      "additionalProperties": false,
      "description": "One scheme's override inside `[ui.columns.scheme.<scheme>]`.",
      "properties": {
        "columns": {
          "default": null,
          "description": "Column ids for panes on this scheme (replaces the default list).",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "sort": {
          "anyOf": [
            {
              "$ref": "#/$defs/SortSection"
            },
            {
              "type": "null"
            }
          ],
          "description": "Sort for panes on this scheme."
        }
      },
      "type": "object"
    },
    "SortSection": {
      "additionalProperties": false,
      "description": "A sort choice inside `[ui.columns]`.",
      "properties": {
        "column": {
          "default": null,
          "description": "`\"name\"` | `\"size\"` | `\"mtime\"` — closed, validated at load.",
          "type": [
            "string",
            "null"
          ]
        },
        "dir": {
          "default": null,
          "description": "`\"asc\"` | `\"desc\"` — closed, validated at load.",
          "type": [
            "string",
            "null"
          ]
        },
        "dirs_first": {
          "default": null,
          "description": "Directories first (default `true`).",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "UiColumnsSection": {
      "additionalProperties": false,
      "description": "The `[ui.columns]` section (#108, columns design Layer 4). Column IDS are\nraw strings here (an open set — `attr:`/`plugin:` forms exist before\ntheir renderer does): the frontend parses them and `norte doctor`\nreports the unusable ones. The SORT vocabulary is closed and validated\nat load (same pattern as `quick_search`).",
      "properties": {
        "default": {
          "default": null,
          "description": "Column ids in paint order (`\"name\"`, `\"size\"`, `\"mtime\"`, `\"kind\"`,\n`\"attr:<id>\"`, `\"plugin:<plugin>/<column>\"`). Absent = the built-in\ndefault (`name`, `size`, `mtime`).",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "scheme": {
          "additionalProperties": {
            "$ref": "#/$defs/SchemeColumnsSection"
          },
          "description": "Per-scheme overrides, keyed by scheme (`sftp`, `s3`…). An override\nREPLACES the column list — it never merges (design decision: merging\nmakes \"why is this column here?\" unanswerable).",
          "type": [
            "object",
            "null"
          ]
        },
        "sort": {
          "anyOf": [
            {
              "$ref": "#/$defs/SortSection"
            },
            {
              "type": "null"
            }
          ],
          "description": "Global sort order. Absent = name/asc/dirs-first."
        }
      },
      "type": "object"
    },
    "UiSection": {
      "additionalProperties": false,
      "description": "The `[ui]` section of `norte.toml`.",
      "properties": {
        "columns": {
          "anyOf": [
            {
              "$ref": "#/$defs/UiColumnsSection"
            },
            {
              "type": "null"
            }
          ],
          "description": "`[ui.columns]` (#108 block 4): column selection and sort order."
        },
        "confirm_quit": {
          "default": null,
          "description": "Whether `app.quit` confirms before closing (S2): `\"auto\"` (default)\nconfirms only with pending work (active tasks in the TUI's board,\ntasks/marks in the GUI — the behavior before this setting existed,\npreserved as the default rather than a fixed point in time);\n`\"always\"` always confirms, even with nothing pending; `\"never\"`\ncloses immediately.\n\n[`crate::load::load`] rejects other values (same pattern as\n`quick_search`) so its diagnostic can include the source path.",
          "type": [
            "string",
            "null"
          ]
        },
        "font": {
          "default": null,
          "description": "UI font family for GUI chrome (GP: `docs/superpowers/specs/2026-07-23-gui-visual-plugins-design.md`).\nWhen absent, use the platform default (bundled fallback).",
          "type": [
            "string",
            "null"
          ]
        },
        "font_size": {
          "default": null,
          "description": "Base UI font size in pixels (GP spec, same design doc). Valid range is\n`[8.0, 32.0]`; [`crate::load::load`] rejects values outside that range\nso its diagnostic can include the source configuration path — it is\nNOT clamped in silence.",
          "format": "float",
          "type": [
            "number",
            "null"
          ]
        },
        "lang": {
          "default": null,
          "description": "Language (`es` or `en`). When absent, negotiate from the environment.",
          "type": [
            "string",
            "null"
          ]
        },
        "mono_font": {
          "default": null,
          "description": "Monospace font family for listings/viewer (GP spec, same design doc).\nWhen absent, use the bundled monospace font.",
          "type": [
            "string",
            "null"
          ]
        },
        "quick_search": {
          "default": null,
          "description": "Quick-search mode for `/`: `\"filter\"` narrows the listing (the default),\nwhile `\"jump\"` moves the cursor without changing the listing.\n\n[`crate::load::load`] rejects other values so its diagnostic can\ninclude the source configuration path. Invalid values never silently\nfall back.",
          "type": [
            "string",
            "null"
          ]
        },
        "reduce_motion": {
          "default": null,
          "description": "Accessibility motion override (spec §17 a11y; GUI phase G2). `true`\nforces every animated GUI effect off — CRT flicker, cursor blink, and\nany future `with_animation` use — regardless of what a theme's\n`[effects]` section declares. `None`/absent leaves motion as the\ntheme requests it; GPUI exposes no platform-level \"prefers reduced\nmotion\" hint at this revision, so the effective default when absent\nis `false` (motion allowed), not an OS query. `Option` for the same\nabsent-vs-explicit reason as `[ai] enabled`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "show_hidden": {
          "default": null,
          "description": "Whether panes show hidden entries at startup (#107): unix dot-entries,\ndecided on the raw bytes of the last path segment. Absent = `true`\n(show everything — the conservative default: nothing the provider\nlists silently disappears until the user asks). `pane.toggle-hidden`\n(Ctrl+H) flips it per pane at runtime; this key only seeds the\ninitial state.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "theme": {
          "default": null,
          "description": "Theme preset name (`default`, `catppuccin-mocha`, `gruvbox-dark`,\n`nord`, `gruvbox-light`, or `catppuccin-latte`) or a path to a custom\nTOML theme (ADR 0020). When absent, use `default`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "General configuration from `norte.toml`.",
  "properties": {
    "ai": {
      "$ref": "#/$defs/AiSection",
      "description": "AI subsystem settings (`[ai]`, ADR 0031/0035). Honored from\nSystem+User layers only — never Project (fail-closed, same carve-out\nas `[archive]`)."
    },
    "archive": {
      "$ref": "#/$defs/ArchiveSection",
      "description": "Archive-provider limits (`[archive]`, #95.2)."
    },
    "daemon": {
      "$ref": "#/$defs/DaemonSection",
      "description": "Daemon settings."
    },
    "hotlist": {
      "description": "Favourite directories shown by `Ctrl+D`.\n\nEntries accumulate across layers instead of replacing lower-layer\nvalues. The project layer is excluded while [`crate::load::load`]\nmerges the list. An absent value contributes no favourites from that\nlayer.",
      "items": {
        "$ref": "#/$defs/HotlistEntry"
      },
      "type": "array"
    },
    "keymap": {
      "$ref": "#/$defs/KeymapSection",
      "description": "Keymap settings."
    },
    "ui": {
      "$ref": "#/$defs/UiSection",
      "description": "User-interface settings."
    }
  },
  "title": "NorteToml",
  "type": "object"
}
