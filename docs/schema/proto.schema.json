{
  "$defs": {
    "AiRenameEntry": {
      "description": "Una pareja del plan de [`AI_RENAME_PLAN`]. Nombres BASE, UTF-8 garantizado:\nel engine rechaza nombres hostiles fail-loud ANTES de llamar al proveedor y\nvalida `to` como `Segment` (sin `/`, `..`, NUL, `!` ni `\\`).",
      "properties": {
        "from": {
          "description": "Nombre existente en `dir`.",
          "type": "string"
        },
        "to": {
          "description": "Nombre destino propuesto.",
          "type": "string"
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "AiRenamePlanParams": {
      "description": "Params de [`AI_RENAME_PLAN`] (M4-IA, ADR 0031).",
      "properties": {
        "dir": {
          "$ref": "#/$defs/VPath",
          "description": "Directorio cuyos basenames se envían al proveedor (tras el gate de IA)."
        },
        "instruction": {
          "description": "Instrucción del usuario.",
          "type": "string"
        }
      },
      "required": [
        "dir",
        "instruction"
      ],
      "type": "object"
    },
    "AiRenamePlanResult": {
      "description": "Resultado de [`AI_RENAME_PLAN`]: el plan REVISABLE (spec §9). Vacío = el\nmodelo no propuso cambios. El plan es el producto: aplicarlo son N\n[`FS_MOVE`] gobernados; este método jamás muta.",
      "properties": {
        "entries": {
          "description": "Parejas from→to (solo las que cambian de nombre).",
          "items": {
            "$ref": "#/$defs/AiRenameEntry"
          },
          "type": "array"
        }
      },
      "required": [
        "entries"
      ],
      "type": "object"
    },
    "AttrHint": {
      "description": "What an attribute MEANS, so a frontend can pick a default format and\nalignment without a hard-coded table of ids. Two `Uint`s render very\ndifferently depending on this.\n\n`\"unknown\"` is a RESERVED wire tag: no future protocol version may name a\nreal variant that. A component that reads a value and writes it back\n(a proxy, a cache) collapses a newer tag to `unknown` in the round trip —\nthe same accepted trade-off as [`EntryKind::Other`](crate::EntryKind::Other).\n\n```\nuse norte_proto::attrs::AttrHint;\nlet h: AttrHint = serde_json::from_str(\"\\\"pista_del_futuro\\\"\").unwrap();\nassert_eq!(h, AttrHint::Unknown);\n```",
      "oneOf": [
        {
          "const": "size",
          "description": "A byte count: right-aligned, IEC/SI formatting applies.",
          "type": "string"
        },
        {
          "const": "timestamp",
          "description": "A point in time: date formatting applies.",
          "type": "string"
        },
        {
          "const": "mode",
          "description": "A POSIX permission word: octal or `rwx` formatting applies.",
          "type": "string"
        },
        {
          "const": "identity",
          "description": "A user/group/owner identity: short text or a number.",
          "type": "string"
        },
        {
          "const": "opaque",
          "description": "No presentation advice; render as plain text.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "Hint of a newer protocol (fallback of deserialisation). A conforming\npeer NEVER emits it; a frontend that encounters it renders the value\nas if it were [`AttrHint::Opaque`].",
          "type": "string"
        }
      ]
    },
    "AttrInfo": {
      "description": "One attribute a provider offers, as advertised by `fs.capabilities`\n([`FsCapabilitiesResult::attrs`](crate::methods::FsCapabilitiesResult)).\n\nAn `AttrInfo` whose `id` fails [`is_valid_attr_id`], or that REPEATS an id\nalready advertised, is DISCARDED by [`sanitize_catalog`] — which\n[`FsCapabilitiesResult::attrs`](crate::methods::FsCapabilitiesResult) runs\nat decode, and which an embedded backend must run itself. Never a hard\nerror, mirroring the \"unknown requested id comes back absent\" rule (ADR\n0039 §5), and never a duty left to the reader. The catalog is bounded at\n[`ATTRS_MAX_ADVERTISED`] descriptors there too. The equivalent rule for\nentry keys is enforced the same way — see\n[`Entry::attrs`](crate::Entry::attrs).\n\n`label` is provider text and therefore THIRD-PARTY (a WASM provider plugin\nwrites it, an SFTP server influences it): [`sanitize_catalog`] clamps it to\n[`ATTR_LABEL_MAX`] bytes, and a frontend still masks it exactly like a\nplugin's column header before painting.\n\n```\nuse norte_proto::attrs::{AttrHint, AttrInfo, AttrType};\nlet info = AttrInfo {\n    id: \"posix.mode\".to_owned(),\n    label: \"Mode\".to_owned(),\n    ty: AttrType::Uint,\n    hint: AttrHint::Mode,\n};\nlet wire = serde_json::to_string(&info).unwrap();\nassert!(wire.contains(\"\\\"type\\\":\\\"uint\\\"\"));\n```",
      "properties": {
        "hint": {
          "$ref": "#/$defs/AttrHint",
          "description": "Presentation hint."
        },
        "id": {
          "description": "Attribute id, namespaced ([`is_valid_attr_id`]). A descriptor whose id\ndoes not satisfy that check does not survive [`sanitize_catalog`].",
          "maxLength": 64,
          "pattern": "^[a-z][a-z0-9_-]*(\\.[a-z][a-z0-9_-]*)+$",
          "type": "string"
        },
        "label": {
          "description": "Human label. THIRD-PARTY text — masked before painting, and CLAMPED to\n[`ATTR_LABEL_MAX`] bytes by [`sanitize_catalog`] at decode.\n\nThis is a FALLBACK, not the primary source of a column header: a\nfrontend should prefer a localized string keyed by the stable `id`\n(e.g. `t!(\"attr.posix.mode\")`) and fall back to this masked provider\nlabel only for ids it does not recognize. First-party attributes\nstill route their labels through `i18n/`.\n\nThe cap is in BYTES here and the schema's `maxLength` counts CODE POINTS\n(JSON Schema 2020-12 §6.3.1): a 60-character CJK label is schema-legal\nand still gets clamped to ~21 characters. See ADR 0039 §5 — the\ndivergence is accepted and pinned by a test, not a bug to \"fix\" by\ncounting characters in the code.",
          "maxLength": 64,
          "type": "string"
        },
        "type": {
          "$ref": "#/$defs/AttrType",
          "description": "Declared type of the values of this attribute — ADVISORY, and never\nauthoritative over a cell.\n\nNothing stops a `{\"type\":\"uint\"}` descriptor from being paired with a\n`{\"text\": …}` cell: both decode cleanly, and only the rule below keeps\ntwo conforming frontends from rendering the same bytes differently\n(ADR 0039 §1).\n\n- At RENDER time the [`AttrValue`]'s own tag decides. A cell whose tag\n  contradicts this field is rendered as its actual variant and is NEVER\n  coerced into the declared one.\n- This field is for column-CONFIGURATION time: it picks the formatter\n  and the sort key for a column before any value has arrived. A column\n  configured from a `Uint` descriptor that then receives `Text` cells\n  sorts them as the text they are.\n- Agreement between the two is a PRODUCER obligation, enforced by block\n  2's provider conformance suite. A consumer cannot check it: holding\n  one cell, it has no way to know whether the catalog or the provider is\n  the one that is wrong."
        }
      },
      "required": [
        "id",
        "label",
        "type",
        "hint"
      ],
      "type": "object"
    },
    "AttrType": {
      "description": "Declared type of an attribute. An unknown value (protocol N+1) degrades to\n[`AttrType::Unknown`]: an old client ignores the attribute, it never breaks.\n\n`\"unknown\"` is a RESERVED wire tag: no future protocol version may name a\nreal variant that. A component that reads a value and writes it back\n(a proxy, a cache) collapses a newer tag to `unknown` in the round trip —\nthe same accepted trade-off as [`EntryKind::Other`](crate::EntryKind::Other).\n\n```\nuse norte_proto::attrs::AttrType;\nlet t: AttrType = serde_json::from_str(\"\\\"tipo_del_futuro\\\"\").unwrap();\nassert_eq!(t, AttrType::Unknown);\n```",
      "oneOf": [
        {
          "const": "uint",
          "description": "Unsigned integer.",
          "type": "string"
        },
        {
          "const": "int",
          "description": "Signed integer.",
          "type": "string"
        },
        {
          "const": "text",
          "description": "UTF-8 text (third-party: mask before painting).",
          "type": "string"
        },
        {
          "const": "bytes",
          "description": "Raw bytes, base64 on the wire (a name that is not UTF-8). This is the\none pair whose names differ across the two surfaces: the declared type\nis `\"bytes\"`, while the values it describes are tagged `\"bytes_b64\"`\n([`AttrValue::Bytes`]) — the value tag names the ENCODING that carries\nthem, the type names what they ARE.",
          "type": "string"
        },
        {
          "const": "time_ms",
          "description": "Milliseconds since the UTC epoch; negative is valid (pre-1970).",
          "type": "string"
        },
        {
          "const": "bool",
          "description": "Boolean.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "Type of a newer protocol (fallback of deserialisation). A conforming\npeer NEVER emits it as a declared type; a client that sees it on an\n[`AttrInfo`] should not request that attribute at all, since it has\nno way to interpret the values it would get back.",
          "type": "string"
        }
      ]
    },
    "AttrValue": {
      "description": "One-key tagged attribute value; an unknown, ambiguous, ill-typed or over-cap key degrades to Unknown.",
      "maxProperties": 1,
      "minProperties": 1,
      "properties": {
        "bool": {
          "type": "boolean"
        },
        "bytes_b64": {
          "contentEncoding": "base64",
          "maxLength": 344,
          "type": "string"
        },
        "int": {
          "format": "int64",
          "type": "integer"
        },
        "text": {
          "description": "UTF-8 text. NOTE: this maxLength counts CODE POINTS (JSON Schema 2020-12), while the cap it publishes is enforced in BYTES — a 256-code-point non-ASCII value is legal here and degrades to Unknown at decode. Deliberate; see ADR 0039 §5.",
          "maxLength": 256,
          "type": "string"
        },
        "time_ms": {
          "format": "int64",
          "type": "integer"
        },
        "uint": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "unknown": {
          "type": "null"
        }
      },
      "type": "object"
    },
    "ByteRange": {
      "description": "Rango de bytes de una lectura: `offset` inicial y longitud opcional\n(`None` = hasta EOF). Lo exigen el resume de M2 (`.norte-partial` +\noffset) y el viewer (lectura parcial de archivos grandes).\n\n```\nuse norte_proto::ByteRange;\nlet r: ByteRange = serde_json::from_str(r#\"{\"offset\": 65536, \"len\": null}\"#).unwrap();\nassert_eq!(r, ByteRange { offset: 65536, len: None });\n```",
      "properties": {
        "len": {
          "description": "Cuántos bytes leer; `None` = hasta el final del archivo.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "offset": {
          "description": "Primer byte a leer (0 = principio).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "offset"
      ],
      "type": "object"
    },
    "Capabilities": {
      "description": "Capacidades declaradas por un provider.\n\n```\nuse norte_proto::{Capabilities, CapabilityFlags};\nlet c = Capabilities {\n    flags: CapabilityFlags::RENAME_ATOMIC | CapabilityFlags::CASE_SENSITIVE,\n    max_path: Some(4096),\n};\nassert!(c.flags.contains(CapabilityFlags::RENAME_ATOMIC));\n```",
      "properties": {
        "flags": {
          "$ref": "#/$defs/CapabilityFlags",
          "description": "Flags de capacidad."
        },
        "max_path": {
          "default": null,
          "description": "Longitud máxima de path nativo en bytes; `None` = sin límite conocido.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "flags"
      ],
      "type": "object"
    },
    "CapabilityFlags": {
      "description": "Capability flags as a `NAME | NAME` string (e.g. `RENAME_ATOMIC | CASE_SENSITIVE`). Unknown well-formed names are ignored for forward-compat.",
      "type": "string"
    },
    "ClientInfo": {
      "description": "Identidad de un cliente (va en [`InitializeParams`]).",
      "properties": {
        "name": {
          "description": "Nombre del frontend (`norte-tui`, `norte-cli`, un tercero…).",
          "type": "string"
        },
        "version": {
          "description": "Versión del frontend (informativa, jamás se compara).",
          "type": "string"
        }
      },
      "required": [
        "name",
        "version"
      ],
      "type": "object"
    },
    "CollisionPolicy": {
      "description": "Qué hacer cuando el destino de una copia/movimiento ya existe\n(spec §5; semánticas exactas en ADR 0005).\n\nEl engine de M1 trata [`Ask`](Self::Ask) como [`Fail`](Self::Fail): la\nresolución interactiva por archivo llega con los diálogos del TUI\n(fase 5). Un core viejo que no conozca una política nueva DEBE fallar el\nrequest, jamás adivinar.\n\n```\nuse norte_proto::CollisionPolicy;\nlet p: CollisionPolicy = serde_json::from_str(r#\"\"rename_auto\"\"#).unwrap();\nassert_eq!(p, CollisionPolicy::RenameAuto);\nassert_eq!(CollisionPolicy::default(), CollisionPolicy::Fail);\n```",
      "oneOf": [
        {
          "const": "fail",
          "description": "La colisión es error: `Conflict` y la task falla (default).",
          "type": "string"
        },
        {
          "const": "ask",
          "description": "Preguntar por archivo (TUI, fase 5). El engine M1 lo trata como\n[`Fail`](Self::Fail).",
          "type": "string"
        },
        {
          "const": "skip",
          "description": "No copiar la entrada en conflicto; la task termina `Completed`.",
          "type": "string"
        },
        {
          "const": "overwrite",
          "description": "Reemplazar el destino (remove + write, dos mutaciones en el journal;\ntipo contra tipo distinto sigue siendo `Conflict`).",
          "type": "string"
        },
        {
          "const": "rename_auto",
          "description": "Buscar nombre libre: sufijo ` (n)` antes de la última extensión,\nn = 1..=1000; agotado → `Conflict`.",
          "type": "string"
        },
        {
          "const": "newer",
          "description": "Reemplazar solo si el origen es más nuevo (`mtime`); si no, saltar.\nSin mtime comparable en cualquiera de los dos → `Conflict`.",
          "type": "string"
        }
      ]
    },
    "ConflictKind": {
      "description": "Subtipo de conflicto en el destino de una operación.\nTolerancia N/N-1 (patrón de ADR 0004; el fallback lo introduce ADR\n0005): un subtipo desconocido deserializa a [`ConflictKind::Unknown`] —\nel cliente viejo degrada a \"conflicto genérico\", no revienta.\n\n```\nuse norte_proto::ConflictKind;\nlet futuro: ConflictKind = serde_json::from_str(r#\"\"subtipo_del_futuro\"\"#).unwrap();\nassert_eq!(futuro, ConflictKind::Unknown);\n```",
      "oneOf": [
        {
          "const": "exists",
          "description": "El destino ya existe.",
          "type": "string"
        },
        {
          "const": "case_collision",
          "description": "Colisión solo-por-caja en FS case-insensitive (evaluada contra el\nFS DESTINO, no el origen).",
          "type": "string"
        },
        {
          "const": "normalization",
          "description": "Colisión solo-por-normalización Unicode: los bytes difieren pero la\nforma NFC coincide (macOS almacena NFD; issue #8, ADR 0005).",
          "type": "string"
        },
        {
          "const": "type_mismatch",
          "description": "El destino existe con otro tipo (dir donde va un archivo o viceversa).",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "Subtipo de un protocolo más nuevo (fallback de deserialización).\nEl core JAMÁS lo emite.",
          "type": "string"
        }
      ]
    },
    "ConnectionDegraded": {
      "description": "Notificación [`CONNECTION_DEGRADED`] (server→client): una sesión remota se\nestableció con seguridad degradada. Las rutas/host van REDACTADOS (rule 10):\n`host` jamás lleva userinfo.",
      "properties": {
        "detail": {
          "description": "Detalle humano opcional (presentación, jamás contrato).",
          "type": [
            "string",
            "null"
          ]
        },
        "host": {
          "description": "Host de la sesión, SIN userinfo (rule 10).",
          "type": "string"
        },
        "reason": {
          "description": "Causa, vocabulario CERRADO comparable por igualdad (como\n`PolicyDenied.rule`). Valores actuales: `\"tls-auth-rejected\"` (el servidor\nrechazó `AUTH TLS` bajo `tls=\"allow\"`; la sesión viaja en claro) y\n`\"ftp-plaintext\"` (FTP-por-plugin, ADR 0033: FTPS es deuda, la sesión es\nSIEMPRE en claro). El conjunto puede CRECER de forma aditiva: un consumidor\nque reciba un `reason` DESCONOCIDO debe degradar con gracia (mensaje\ngenérico de \"sesión degradada\" apoyándose en `detail`), jamás rechazar la\nnotif.",
          "type": "string"
        },
        "scheme": {
          "description": "Scheme de la sesión (p. ej. `\"ftp\"`).",
          "type": "string"
        }
      },
      "required": [
        "scheme",
        "host",
        "reason"
      ],
      "type": "object"
    },
    "ConnectionTrustHostKeyParams": {
      "description": "Params de [`CONNECTION_TRUST_HOST_KEY`] (flujo TOFU, ADR 0015 D). Lleva el\nfingerprint que el usuario VERIFICÓ; el core lo compara con la clave que\nvuelve a presentar el servidor al reintentar, y solo registra si coincide.",
      "properties": {
        "algo": {
          "description": "Algoritmo de la clave (p. ej. `ssh-ed25519`).",
          "type": "string"
        },
        "fingerprint": {
          "description": "Fingerprint en formato OpenSSH `SHA256:<base64>` que el usuario\nconfirmó (la misma cadena que trae el `Error::HostKeyUnknown`).",
          "type": "string"
        },
        "host": {
          "description": "Host al que se conecta (`host`; el puerto aparte).",
          "type": "string"
        },
        "port": {
          "default": null,
          "description": "Puerto (ausente = default del scheme).",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "host",
        "algo",
        "fingerprint"
      ],
      "type": "object"
    },
    "ConnectionTrustHostKeyResult": {
      "description": "Result de [`CONNECTION_TRUST_HOST_KEY`].",
      "properties": {
        "trusted": {
          "description": "`true` si la clave quedó registrada (idempotente: `true` también si ya\nestaba). `false` reservado para un futuro rechazo por política.",
          "type": "boolean"
        }
      },
      "required": [
        "trusted"
      ],
      "type": "object"
    },
    "DaemonShutdownParams": {
      "description": "Params de [`DAEMON_SHUTDOWN`].",
      "properties": {
        "graceful": {
          "default": true,
          "description": "`true` (default): terminar las tasks vivas antes de salir.\n`false`: cancelarlas primero (estado limpio garantizado igual).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "DaemonShutdownResult": {
      "description": "Result de [`DAEMON_SHUTDOWN`]: objeto vacío, reservado para extensión.",
      "type": "object"
    },
    "DecorationWire": {
      "description": "Una decoración tipo git-status de UNA entrada (elemento de\n[`PluginDecorations::decorations`], 0.27.0, G3, ADR 0037). `badge` es\ntexto del plugin — NO confiable, ≤8 chars TRAS enmascarar (tope del\nwire, ADR 0037); un frontend debe enmascarar (y truncar de nuevo) antes\nde confiar en el tope ya aplicado por el server — defensa en\nprofundidad. `role` sigue la misma validación host-side que\n[`SpanWire::role`].",
      "properties": {
        "badge": {
          "description": "Badge corto (p. ej. `\"M\"`, `\"++\"`). Ausente = sin badge para esta\nentrada de este plugin.",
          "type": [
            "string",
            "null"
          ]
        },
        "role": {
          "description": "Nombre de rol de `norte_theme::Role` para pintar el badge.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "DeleteMode": {
      "description": "Cómo borrar (ADR 0009). El default del WIRE es el seguro:\n[`Trash`](Self::Trash). El engine JAMÁS degrada solo — pedir `Trash`\nsin capability `TRASH` es `Unsupported` y el frontend decide con el\nusuario informado.\n\nSKEW: un core anterior a 0.3 IGNORA `mode` (tolerancia de structs,\nADR 0004) y borra PERMANENTE — condiciona `Trash` a la capability\n`TRASH` (que un core viejo jamás anuncia), NUNCA a tu versión.\n\n```\nuse norte_proto::DeleteMode;\nassert_eq!(DeleteMode::default(), DeleteMode::Trash);\nlet m: DeleteMode = serde_json::from_str(r#\"\"permanent\"\"#).unwrap();\nassert_eq!(m, DeleteMode::Permanent);\n```",
      "oneOf": [
        {
          "const": "trash",
          "description": "A la papelera del provider (recuperable).",
          "type": "string"
        },
        {
          "const": "permanent",
          "description": "Borrado permanente (elección EXPLÍCITA).",
          "type": "string"
        }
      ]
    },
    "Entry": {
      "description": "Metadatos de un nodo del VFS.\n\nCampos opcionales `None` = \"el provider no lo sabe\", nunca un 0 fingido.\n\n```\nuse norte_proto::attrs::AttrValue;\nuse norte_proto::{Entry, EntryKind, VPath};\nlet e = Entry {\n    path: VPath::parse(\"file:///a.txt\").unwrap(),\n    kind: EntryKind::File,\n    size: Some(42),\n    mtime_ms: None,\n    attrs: [(\"posix.mode\".to_owned(), AttrValue::Uint(0o100_644))].into(),\n};\nlet json = serde_json::to_string(&e).unwrap();\nassert_eq!(serde_json::from_str::<Entry>(&json).unwrap(), e);\n\n// Sin attrs, el wire es EXACTAMENTE el de 0.29.\nlet plain = Entry { attrs: Default::default(), ..e };\nassert!(!serde_json::to_string(&plain).unwrap().contains(\"attrs\"));\n\n// Una clave de atributo mal formada NO llega al mapa: se descarta al\n// decodificar, sin error (ver [`Entry::attrs`]).\nlet wire = r#\"{\"path\":\"file:///a.txt\",\"kind\":\"file\",\"attrs\":{\"MODE\":{\"uint\":1}}}\"#;\nlet filtrada: Entry = serde_json::from_str(wire).unwrap();\nassert!(filtrada.attrs.is_empty());\n```\n\nOJO: `PartialEq`/`Eq`/`Hash` derivados son REPRESENTACIONALES, no de\nIDENTIDAD: dos `Entry` del MISMO nodo difieren si una se pidió con\natributos y la otra no, o si se pidieron ids distintos. Para \"¿es el mismo\nnodo?\" se compara [`Entry::path`]; estas derivaciones existen para\nfixtures, tests y deduplicado de listados idénticos, no para decidir\nidentidad.",
      "properties": {
        "attrs": {
          "additionalProperties": {
            "$ref": "#/$defs/AttrValue"
          },
          "description": "Provider attributes (0.30.0, ADR 0039): typed metadata BEYOND the four\nfields above, delivered only for the ids the client requested in\n`fs.list`/`fs.stat`. Empty (the default, and the only possibility for a\n0.29 peer) is omitted from the wire entirely, so a payload without\nattributes is byte-identical to 0.29's.\n\nAn absent key has THREE possible causes and a caller cannot tell them\napart: the provider does not know the value (never a fabricated one),\nthe id the peer sent was malformed, or the id lost the byte-order cut\nof the cap below. The drop is deliberately not observable — a caller\ntreats absence as \"no value\", exactly as it does for a `None`\n[`size`](Entry::size).\n\n# Rules applied ON DECODE\n\nDeserialisation applies both of these, and NEITHER is ever an error:\none bad key costs that key, never the entry and never the page — the\nsame reason a malformed cell degrades to\n[`AttrValue::Unknown`](crate::attrs::AttrValue::Unknown).\n\n1. A key that is not a well-formed id\n   ([`is_valid_attr_id`](crate::attrs::is_valid_attr_id)) is DROPPED.\n   An attribute id becomes a configuration id and a map lookup\n   downstream, so `MODE` or `../etc/passwd` must not survive the wire.\n2. The map is bounded at\n   [`ATTRS_MAX_REQUEST`](crate::attrs::ATTRS_MAX_REQUEST) entries — a\n   client can request no more than that, so a bigger map is a buggy or\n   hostile peer. The SMALLEST ids in byte order are kept, so the SET of\n   ids that survives does not depend on the order the peer serialised\n   its keys in (JSON objects are unordered, RFC 8259 §4). A key\n   REPEATED in the same object resolves last-wins, as it would in any\n   JSON parser. At most `ATTRS_MAX_REQUEST + 1` entries ever exist at\n   once and a dropped key's value is never MATERIALISED — the tokens are\n   still walked, since JSON has to be traversed to be skipped, but\n   nothing is built from them — so a 10 000-key object does not\n   materialise a 10 000-entry map first.\n\nA non-map `attrs` IS a hard error: that is serde's decision on the\nparent, and a peer that sends one is broken rather than newer.\n\n# The filter is one-directional\n\nSerialisation is NOT filtered, and the field is public with no\nconstructor: an `Entry` built in-process with an invalid id or with\nmore than the cap emits exactly what it holds, and decoding that wire\ngives back a DIFFERENT `Entry`. That asymmetry is deliberate — a\nproducer's bug must stay visible at the boundary that validates it\ninstead of being laundered by the serialiser. A producer therefore\nvalidates before emitting; daemon-side enforcement lands in block 2 of\nthis feature.",
          "maxProperties": 16,
          "propertyNames": {
            "maxLength": 64,
            "pattern": "^[a-z][a-z0-9_-]*(\\.[a-z][a-z0-9_-]*)+$"
          },
          "type": "object"
        },
        "kind": {
          "$ref": "#/$defs/EntryKind",
          "description": "Clase del nodo."
        },
        "mtime_ms": {
          "default": null,
          "description": "Última modificación en milisegundos desde epoch UTC; `i64` porque las\nfechas pre-1970 existen en FS reales. `None` = desconocida.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "path": {
          "$ref": "#/$defs/VPath",
          "description": "Path completo del nodo (los frontends operan con él, no con el nombre)."
        },
        "size": {
          "default": null,
          "description": "Tamaño en bytes; `None` si el provider no lo conoce (p. ej. directorios).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "path",
        "kind"
      ],
      "type": "object"
    },
    "EntryKind": {
      "description": "Clase de un nodo del VFS.\n\nLos valores desconocidos (protocolo N+1) deserializan a [`EntryKind::Other`]:\nun cliente viejo degrada, no revienta.\n\n```\nuse norte_proto::EntryKind;\nlet k: EntryKind = serde_json::from_str(\"\\\"kind_del_futuro\\\"\").unwrap();\nassert_eq!(k, EntryKind::Other);\n```",
      "oneOf": [
        {
          "const": "file",
          "description": "Archivo regular.",
          "type": "string"
        },
        {
          "const": "dir",
          "description": "Directorio.",
          "type": "string"
        },
        {
          "const": "symlink",
          "description": "Symlink (M0: jamás se sigue; se preserva o `Unsupported`).",
          "type": "string"
        },
        {
          "const": "other",
          "description": "Cualquier otra cosa (device, socket, fifo, o kind de protocolo futuro).",
          "type": "string"
        }
      ]
    },
    "Error": {
      "description": "Error del protocolo norte (spec §17.7).\n\nWire: objeto tagged `{\"kind\": \"...\", …campos}`. La variante es la API:\nlos frontends hacen match por categoría y el detalle humano viaja aparte\n(campo `message` del error JSON-RPC), nunca dentro de esta taxonomía.\n\nTolerancia N/N-1 (ADR 0004): una categoría desconocida deserializa a\n[`Error::Unknown`] — el cliente viejo degrada a \"error genérico\", no\nrevienta. `#[non_exhaustive]` obliga además al brazo `_` en Rust.\n\n```\nuse norte_proto::Error;\nlet e: Error = serde_json::from_str(r#\"{\"kind\": \"not_found\"}\"#).unwrap();\nassert_eq!(e, Error::NotFound);\nlet futuro: Error = serde_json::from_str(r#\"{\"kind\": \"quota_del_futuro\"}\"#).unwrap();\nassert_eq!(futuro, Error::Unknown);\n```",
      "oneOf": [
        {
          "description": "El path no existe.",
          "properties": {
            "kind": {
              "const": "not_found",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "El provider/OS denegó el acceso.",
          "properties": {
            "kind": {
              "const": "permission_denied",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Conflicto en el destino; la operación NO escribió nada.",
          "properties": {
            "conflict": {
              "$ref": "#/$defs/ConflictKind",
              "description": "Subtipo del conflicto."
            },
            "kind": {
              "const": "conflict",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "conflict"
          ],
          "type": "object"
        },
        {
          "description": "El provider no responde (red caída, daemon remoto muerto…).",
          "properties": {
            "kind": {
              "const": "provider_unavailable",
              "type": "string"
            },
            "retryable": {
              "description": "`true` si reintentar con backoff tiene sentido.",
              "type": "boolean"
            }
          },
          "required": [
            "kind",
            "retryable"
          ],
          "type": "object"
        },
        {
          "description": "Sin espacio o cuota en el destino (ENOSPC/EDQUOT) — el fallo de copia\nmás común tras permisos; merece render propio, no un cajón genérico.",
          "properties": {
            "kind": {
              "const": "no_space",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "I/O falló a mitad de operación (EIO, reset de conexión…): el provider\nresponde, pero esta operación concreta murió.",
          "properties": {
            "kind": {
              "const": "io",
              "type": "string"
            },
            "retryable": {
              "description": "`true` si repetir la operación puede funcionar.",
              "type": "boolean"
            }
          },
          "required": [
            "kind",
            "retryable"
          ],
          "type": "object"
        },
        {
          "description": "Cancelado por el usuario o por shutdown; estado limpio garantizado.",
          "properties": {
            "kind": {
              "const": "cancelled",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "El policy engine denegó la operación (agentes/plugins, M3-3).",
          "properties": {
            "kind": {
              "const": "policy_denied",
              "type": "string"
            },
            "rule": {
              "description": "Categoría GRUESA de la causa de denegación — NO el identificador de\nla regla concreta de `policy.toml` (que no se filtra, por\nseguridad). Vocabulario cerrado, comparable por igualdad:\n`\"out-of-scope\"` (ruta/op fuera del scope del agente),\n`\"scope-expired\"` (el scope aplicable venció),\n`\"policy-rule\"` (una regla `deny` de `policy.toml` hizo match),\n`\"no-rule\"` (fail-closed: dentro del scope pero sin regla que\naplique), `\"not-approved\"` (un `ask` fue denegado o su TTL venció).",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "rule"
          ],
          "type": "object"
        },
        {
          "description": "Una transcodificación habría perdido datos y se abortó.",
          "properties": {
            "kind": {
              "const": "encoding_loss",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "La operación no está soportada por este provider (ver `Capabilities`).",
          "properties": {
            "kind": {
              "const": "unsupported",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "El `VPath` recibido no parsea o viola invariantes.",
          "properties": {
            "kind": {
              "const": "invalid_path",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Error interno del core; `panic: true` = task supervisada que reventó\n(el daemon sigue vivo, spec §17.7).",
          "properties": {
            "kind": {
              "const": "internal",
              "type": "string"
            },
            "panic": {
              "description": "`true` si el origen fue un panic capturado en una task.",
              "type": "boolean"
            }
          },
          "required": [
            "kind",
            "panic"
          ],
          "type": "object"
        },
        {
          "description": "Ciclo de symlinks detectado al recorrer con `Follow` (visited set\nde la spec §17.9; issue #31). Un cliente N-1 degrada a `Unknown`.",
          "properties": {
            "kind": {
              "const": "loop",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Contenedor/formato ROTO (0.17.0, #58): un zip/tar corrupto, truncado\no estructuralmente mentiroso. No es un fallo de I/O (reintentar no\nayuda; un fallo del provider subyacente se propaga con su propia\ncategoría, jamás como `Corrupt`); la UX honesta es «no es un\ncontenedor válido». Desde 0.23.0 (#95) exceder los topes anti-bomba\nLOCALES ya no es `Corrupt`: es [`Error::LimitExceeded`] — el\ncontenedor puede ser perfectamente válido. Un cliente N-1 degrada a\n`Unknown`.",
          "properties": {
            "kind": {
              "const": "corrupt",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "El contenedor excede un LÍMITE LOCAL anti-bomba del índice (0.23.0,\n#95, ADR 0018 D2). Distinto de [`Error::Corrupt`]: el contenedor\npuede ser VÁLIDO (un tar.gz legítimo enorme) — norte REHÚSA pagar su\ncoste con los límites vigentes, no lo declara roto. Un cliente N-1\ndegrada a `Unknown` (misma UX gruesa que el `Corrupt` de antes).",
          "properties": {
            "kind": {
              "const": "limit_exceeded",
              "type": "string"
            },
            "limit": {
              "description": "QUÉ límite se excedió — vocabulario CERRADO, comparable por\nigualdad (nunca el valor numérico, que es configuración local):\n[`Error::LIMIT_ENTRIES`] (entradas del índice — o anunciadas por\nel EOCD — por encima de `max_entries`, presupuesto de omitidas\nincluido), [`Error::LIMIT_DECOMPRESSED_BYTES`] (inflado\nacumulado por encima de `max_decompressed_bytes`) o\n[`Error::LIMIT_NESTING`] (capas de archivo anidadas por encima\nde `max_nesting`, #56/proto 0.24). Los emisores usan las\nconstantes, jamás literales sueltos (fuente única, pin en\ntests). Forward-compat: un token DESCONOCIDO (peer más nuevo)\nse trata como límite genérico — mostrar el string tal cual,\njamás fallar el parse ni adivinar.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "limit"
          ],
          "type": "object"
        },
        {
          "description": "Host key SSH DESCONOCIDA en el primer contacto (TOFU — ADR 0015 D). El\nfrontend muestra el `fingerprint` y, si el usuario confía, llama a\n`connection.trust_host_key` y reintenta. Un cliente N-1 degrada a\n`Unknown` (0.7.0, fase 6).",
          "properties": {
            "algo": {
              "description": "Algoritmo de la clave (p. ej. `ssh-ed25519`).",
              "type": "string"
            },
            "fingerprint": {
              "description": "Fingerprint en formato OpenSSH `SHA256:<base64>` — la MISMA cadena\nque core y frontend comparan y que va en `trust_host_key`.",
              "type": "string"
            },
            "host": {
              "description": "Host DESNUDO al que se conecta (sin puerto; el puerto va aparte\npara que el mapeo error→`connection.trust_host_key` sea 1:1).",
              "type": "string"
            },
            "kind": {
              "const": "host_key_unknown",
              "type": "string"
            },
            "port": {
              "default": null,
              "description": "Puerto (ausente = default del scheme).",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "kind",
            "host",
            "algo",
            "fingerprint"
          ],
          "type": "object"
        },
        {
          "description": "La host key SSH CAMBIÓ respecto a la registrada en `known_hosts`:\nposible MITM. JAMÁS se acepta en silencio (0.7.0, fase 6).",
          "properties": {
            "algo": {
              "description": "Algoritmo de la clave presentada.",
              "type": "string"
            },
            "fingerprint": {
              "description": "Fingerprint OpenSSH `SHA256:<base64>` de la clave presentada.",
              "type": "string"
            },
            "host": {
              "description": "Host DESNUDO afectado (sin puerto).",
              "type": "string"
            },
            "kind": {
              "const": "host_key_mismatch",
              "type": "string"
            },
            "port": {
              "default": null,
              "description": "Puerto (ausente = default del scheme).",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "kind",
            "host",
            "algo",
            "fingerprint"
          ],
          "type": "object"
        },
        {
          "description": "Un `cursor` de paginación de `fs.list` ya no es válido: expiró (TTL),\nfue expulsado (LRU) o murió con la conexión (ADR 0017, 0.8.0). El\ncliente reinicia el listado desde cero. Un cliente N-1 (0.7.x) jamás la\nve — no envía cursores — pero degrada a `Unknown` si la recibiera.",
          "properties": {
            "kind": {
              "const": "cursor_expired",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "El directorio cambió entre la vista previa y la ejecución de un lote de\nrenames (`fs.rename_batch`, 0.36.0): re-planificar las MISMAS parejas\nprodujo un plan distinto del `plan_hash` que el llamante aprobó. Nada se\nintentó. Accionable: re-planificar y volver a confirmar. NO reintentable\ntal cual — el humano tiene que ver el plan nuevo. Un cliente N-1\n(0.35.x) jamás la ve (no llama al método) pero degradaría a `Unknown`.",
          "properties": {
            "kind": {
              "const": "plan_stale",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "El plan de renames tiene colisiones, así que no se intentó nada\n(`fs.rename_batch`, 0.36.0). Accionable: corregir los nombres (o el\ndirectorio de origen) y re-planificar. Un cliente N-1 (0.35.x) jamás la\nve pero degradaría a `Unknown`.",
          "properties": {
            "kind": {
              "const": "plan_not_executable",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Categoría de un protocolo más nuevo (fallback de deserialización).\nEl core JAMÁS la emite; existe para que un cliente N degrade con\nelegancia ante categorías N+1.",
          "properties": {
            "kind": {
              "const": "unknown",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "FsCapabilitiesParams": {
      "description": "Params de [`FS_CAPABILITIES`].",
      "properties": {
        "path": {
          "$ref": "#/$defs/VPath",
          "description": "Un path del provider a consultar."
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "FsCapabilitiesResult": {
      "description": "Result de [`FS_CAPABILITIES`].\n\n```\nuse norte_proto::methods::FsCapabilitiesResult;\n// Un catálogo con un id mal formado NO llega al vector: se descarta al\n// decodificar, sin error (ver [`FsCapabilitiesResult::attrs`]).\nlet wire = r#\"{\n    \"capabilities\": {\"flags\": \"\", \"max_path\": null},\n    \"attrs\": [{\"id\": \"MODE\", \"label\": \"Mode\", \"type\": \"uint\", \"hint\": \"mode\"}]\n}\"#;\nlet caps: FsCapabilitiesResult = serde_json::from_str(wire).unwrap();\nassert!(caps.attrs.is_empty());\n```",
      "properties": {
        "attrs": {
          "description": "Provider attributes this provider offers (0.30.0, ADR 0039): the\ndiscovery half of [`Entry::attrs`](crate::Entry::attrs). Empty (the\ndefault, and the only possibility for a 0.29 peer) is omitted from the\nwire entirely, so a response from a provider that publishes none is\nbyte-identical to 0.29's.\n\n[`AttrInfo::label`](crate::attrs::AttrInfo::label) is THIRD-PARTY text\n— a frontend masks and clamps it exactly as it does a plugin's column\nheader. The ORDER is the provider's and it is meaningful: a column\npicker paints the catalog in it, so it is never sorted here.\n\n# Rules applied ON DECODE\n\nThis is RECEIVED data, so the same shape of filter that guards\n[`Entry::attrs`](crate::Entry::attrs) guards it: decoding runs\n[`sanitize_catalog`](crate::attrs::sanitize_catalog), whose rules are\ndocumented there in full and NONE of which is ever an error —\n\n1. an [`AttrInfo`](crate::attrs::AttrInfo) whose `id` is not\n   well-formed ([`is_valid_attr_id`](crate::attrs::is_valid_attr_id))\n   is DROPPED. An advertised id becomes a requested id, a configuration\n   id and a map lookup downstream, so `MODE` or `../etc/passwd` must\n   not survive the wire — and a caller must not have to remember to\n   check;\n2. a REPEATED id is dropped, FIRST WINS — the opposite of\n   [`Entry::attrs`](crate::Entry::attrs)'s last-wins, because this is\n   an ordered list the provider ranked (where \"first\" means something)\n   and that is an unordered JSON object (where it does not). Keeping\n   both would let two consumers render the same bytes differently, one\n   folding into a map and one using `find()`;\n3. an over-long `label` is CLAMPED to\n   [`ATTR_LABEL_MAX`](crate::attrs::ATTR_LABEL_MAX) bytes on a char\n   boundary; the descriptor survives, since the id is what a client\n   acts on;\n4. the vector is bounded at\n   [`ATTRS_MAX_ADVERTISED`](crate::attrs::ATTRS_MAX_ADVERTISED)\n   descriptors, keeping the FIRST in wire order — rules 1 and 2 run\n   before a slot is taken, so rejects and duplicates never starve a\n   legitimate later attribute. A fat catalog is a buggy provider, not a\n   broken peer, so it truncates rather than failing the call: the same\n   spirit as an unknown requested id coming back absent.\n\nDecoding additionally stops EXAMINING elements past\n[`ATTRS_MAX_CATALOG_SCAN`](crate::attrs::ATTRS_MAX_CATALOG_SCAN) and\ndrains the rest unmaterialised, so a catalog padded with rejects costs\nbounded work rather than unbounded work for an empty result.\n\nThose rules are NOT the deserialiser's alone: the field is an\n[`AttrCatalog`](crate::attrs::AttrCatalog), whose only constructor runs\n`sanitize_catalog` and whose contents are private. That matters because\nan EMBEDDED backend — the default TUI/CLI configuration, no daemon in\nbetween — never crosses the deserialisation boundary, so in block 2 a\ncatalog from a WASM provider plugin (untrusted by the threat model)\nwould otherwise reach a frontend unfiltered whenever someone forgot the\ncall. The wire shape is unchanged: a plain array of `AttrInfo`.\n\nA descriptor that is not a well-formed `AttrInfo` at all (a missing\n`label`, `attrs` that is not a list) IS a hard error: that is serde's\ndecision, and a peer that sends one is broken rather than newer — an\nunknown `type` or `hint` from a NEWER peer already degrades to its\n`Unknown` variant instead.\n\n# Unlike `Entry::attrs`, this cannot carry a producer's bug\n\n[`Entry::attrs`](crate::Entry::attrs) is a public map with no\nconstructor, so an entry built in-process with an invalid id emits it\nand decodes back different — deliberately, so a producer's bug stays\nvisible at the boundary that validates it. A catalog has no such hole:\nit cannot be built dirty in the first place, so serialisation has\nnothing to launder. The asymmetry is on purpose — an advertised id\nbecomes a REQUESTED id, a configuration id and a map lookup downstream,\nwhich is a longer blast radius than one entry's cell.",
          "items": {
            "$ref": "#/$defs/AttrInfo"
          },
          "maxItems": 64,
          "type": "array"
        },
        "capabilities": {
          "$ref": "#/$defs/Capabilities",
          "description": "Capabilities declaradas por el provider."
        }
      },
      "required": [
        "capabilities"
      ],
      "type": "object"
    },
    "FsCopyParams": {
      "description": "Params de [`FS_COPY`].",
      "properties": {
        "from": {
          "$ref": "#/$defs/VPath",
          "description": "Origen (archivo o directorio)."
        },
        "on_collision": {
          "$ref": "#/$defs/CollisionPolicy",
          "default": "fail",
          "description": "Qué hacer si el destino existe. `#[serde(default)]`: un cliente N-1\nque no lo envía obtiene `Fail` (el comportamiento de siempre)."
        },
        "resume": {
          "$ref": "#/$defs/ResumePolicy",
          "default": "off",
          "description": "Reanudación (ADR 0012); default `Off` = contrato de M1."
        },
        "symlinks": {
          "$ref": "#/$defs/SymlinkPolicy",
          "default": "preserve",
          "description": "Qué hacer con los symlinks del origen (default `Preserve`)."
        },
        "to": {
          "$ref": "#/$defs/VPath",
          "description": "Destino EXACTO (con `RenameAuto` el core deriva el nombre libre;\ncon el resto de políticas jamás inventa nombres)."
        },
        "verify": {
          "$ref": "#/$defs/VerifyPolicy",
          "default": "length",
          "description": "Verificación del parcial al reanudar; default `Length`."
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "FsDeleteParams": {
      "description": "Params de [`FS_DELETE`].",
      "properties": {
        "mode": {
          "$ref": "#/$defs/DeleteMode",
          "default": "trash",
          "description": "Papelera o permanente. `#[serde(default)]` = Trash: el default del\nwire es el SEGURO (ADR 0009)."
        },
        "path": {
          "$ref": "#/$defs/VPath",
          "description": "Nodo a borrar (recursivo si es dir)."
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "FsListParams": {
      "description": "Params de [`FS_LIST`] (paginación por cursor desde 0.8.0, ADR 0017).",
      "properties": {
        "attrs": {
          "description": "Ids of the provider attributes to deliver with each entry (0.30.0,\nADR 0039). Empty (the default, and the only possibility for a 0.29\nclient) = none: nothing is delivered unrequested. The contract is at\nmost [`ATTRS_MAX_REQUEST`](crate::attrs::ATTRS_MAX_REQUEST) ids, each\nwell-formed ([`is_valid_attr_id`](crate::attrs::is_valid_attr_id)), and\nviolating either is `-32602` — a rule BLOCK 2 wires up: a 0.30.0 daemon\nships no producer and ignores the requested ids entirely, so today the\nanswer to any request is the same empty set of attributes. An id the\nprovider does not offer is NOT an error either way: it comes back\nabsent, so a client with a stale catalog degrades instead of failing.\n\n# Bounded at decode, but NOT validated — on purpose\n\nThis is a REQUEST: data this peer is SENDING, not data it received. So\nwhatever the wire carries lands in the vector VERBATIM — malformed ids\nand over-cap length included. Silently dropping a bad id here would\nturn \"the client asked for `../etc/passwd`\" into \"the client asked for\nnothing\", hiding a caller's bug and making the daemon-side validation\nuntestable. The asymmetry with the two RECEIVE-side fields —\n[`Entry::attrs`](crate::Entry::attrs) and\n[`FsCapabilitiesResult::attrs`], which both filter at decode — is\ndeliberate: a bad cell or a bad advertised descriptor costs itself,\nwhile a bad request is the daemon's `-32602` to raise.\n\nThe one thing decoding does impose is a MEMORY bound, which is not\nvalidation: only the first `ATTRS_MAX_REQUEST + 1` elements are kept\nand the rest is drained unmaterialised, because a 16 MiB frame of\n`[\"a\",\"a\",…]` would otherwise allocate ~15× its own size in `String`\nheaders before any daemon check could run. The `+ 1` is what keeps\nover-cap OBSERVABLE (`attrs.len() > ATTRS_MAX_REQUEST`) instead of\ntrimming a violation into legality.",
          "items": {
            "maxLength": 64,
            "pattern": "^[a-z][a-z0-9_-]*(\\.[a-z][a-z0-9_-]*)+$",
            "type": "string"
          },
          "maxItems": 16,
          "type": "array"
        },
        "cursor": {
          "default": null,
          "description": "Cursor OPACO de la página siguiente (el `next_cursor` de la respuesta\nprevia). `None` = abre un listado nuevo. Jamás se parsea; desconocido\no expirado → [`Error::CursorExpired`](crate::Error::CursorExpired).",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "default": null,
          "description": "Máximo de entradas de ESTA página. `None` (o ausente) = sin límite\n(drena el resto). Recortado a [`FS_LIST_MAX_PAGE`]; `Some(0)` es error\n(`-32602`, evita páginas vacías en bucle). Un cliente 0.7 no lo envía.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "path": {
          "$ref": "#/$defs/VPath",
          "description": "Directorio a listar. OBLIGATORIO también al continuar (valida que el\n`cursor` corresponde a ESTE listado)."
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "FsListResult": {
      "description": "Result de [`FS_LIST`].\n\nCláusula de compatibilidad (ADR 0004/0017): sin `cursor` NI `limit`, el\ncore DEVUELVE el listado COMPLETO con `next_cursor: null` — un cliente\n0.7 (N-1) recibe exactamente lo de antes, jamás un truncado en silencio.",
      "properties": {
        "entries": {
          "description": "Entradas de ESTA página (orden: el del provider, sin garantía).",
          "items": {
            "$ref": "#/$defs/Entry"
          },
          "type": "array"
        },
        "next_cursor": {
          "default": null,
          "description": "Cursor de la página siguiente, o `None` si el listado terminó. Un\ncliente 0.7 lo ignora (campo desconocido para él).",
          "type": [
            "string",
            "null"
          ]
        },
        "skipped": {
          "description": "Entradas del CONTENEDOR omitidas de TODO su índice (#93, desde\n0.22.0): nombres hostiles/límites anti-bomba de un provider archive\n(ADR 0018 C2). Es un total POR CONTENEDOR, no por página ni por\ndirectorio (las omitidas no tienen ruta representable donde\natribuirse): cada página del listado repite el mismo valor. Ausente\n(`None`) = no aplica o desconocido; los clientes solo deben\nseñalizarlo cuando es `Some(n)` con `n > 0`.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "entries"
      ],
      "type": "object"
    },
    "FsMkdirParams": {
      "description": "Params de [`FS_MKDIR`] (#104).",
      "properties": {
        "path": {
          "$ref": "#/$defs/VPath",
          "description": "Directorio a crear, COMPLETO (el último segmento es el nombre nuevo).\nEl padre debe existir; no hay `-p`."
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "FsMoveParams": {
      "description": "Params de [`FS_MOVE`].",
      "properties": {
        "from": {
          "$ref": "#/$defs/VPath",
          "description": "Origen."
        },
        "on_collision": {
          "$ref": "#/$defs/CollisionPolicy",
          "default": "fail",
          "description": "Qué hacer si el destino existe (ver [`FsCopyParams::on_collision`])."
        },
        "resume": {
          "$ref": "#/$defs/ResumePolicy",
          "default": "off",
          "description": "Reanudación del camino copy+delete (ADR 0012); default `Off`."
        },
        "symlinks": {
          "$ref": "#/$defs/SymlinkPolicy",
          "default": "preserve",
          "description": "Qué hacer con los symlinks (solo aplica al camino copy+delete; el\nrename same-provider mueve el link tal cual)."
        },
        "to": {
          "$ref": "#/$defs/VPath",
          "description": "Destino exacto (ver [`FsCopyParams::to`])."
        },
        "verify": {
          "$ref": "#/$defs/VerifyPolicy",
          "default": "length",
          "description": "Verificación del parcial al reanudar; default `Length`."
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "FsReadParams": {
      "description": "Params de [`FS_READ`].\n\n```\nuse norte_proto::methods::FsReadParams;\nuse norte_proto::VPath;\nlet p = FsReadParams { path: VPath::parse(\"file:///x\").unwrap(), range: None };\n// El emisor canónico escribe `range: null` explícito (ADR 0004).\nassert!(serde_json::to_string(&p).unwrap().contains(r#\"\"range\":null\"#));\n```",
      "properties": {
        "path": {
          "$ref": "#/$defs/VPath",
          "description": "Archivo a leer."
        },
        "range": {
          "anyOf": [
            {
              "$ref": "#/$defs/ByteRange"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Tramo pedido; ausente/`null` = desde 0, tope del server."
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "FsReadResult": {
      "description": "Result de [`FS_READ`].\n\n```\nuse norte_proto::methods::FsReadResult;\nlet r: FsReadResult = serde_json::from_str(r#\"{\"content_b64\":\"aGk=\",\"eof\":true}\"#).unwrap();\nassert!(r.eof);\n```",
      "properties": {
        "content_b64": {
          "description": "Bytes del tramo, en base64 estándar (los bytes de un archivo no\nson texto: JSON no puede llevarlos crudos).",
          "type": "string"
        },
        "eof": {
          "description": "`true` si el tramo termina EN el fin del archivo.",
          "type": "boolean"
        }
      },
      "required": [
        "content_b64",
        "eof"
      ],
      "type": "object"
    },
    "FsRenameBatchParams": {
      "description": "Params de [`FS_RENAME_BATCH`] (0.36.0).",
      "properties": {
        "dir": {
          "$ref": "#/$defs/VPath",
          "description": "El directorio en el que viven TODAS las parejas."
        },
        "pairs": {
          "description": "Los renames pedidos — la MISMA intención que produjo `plan_hash`. Tope\n[`FS_RENAME_BATCH_MAX_PAIRS`], igual que en el plan.",
          "items": {
            "$ref": "#/$defs/RenamePair"
          },
          "maxItems": 4096,
          "type": "array"
        },
        "plan_hash": {
          "$ref": "#/$defs/PlanHash",
          "description": "El hash del plan que el humano aprobó. Que la FORMA sea válida lo\ngarantiza [`PlanHash`] al deserializar; que el CONTENIDO case es lo que\ncomprueba el core, y no casar es\n[`Error::PlanStale`](crate::Error::PlanStale)."
        }
      },
      "required": [
        "dir",
        "pairs",
        "plan_hash"
      ],
      "type": "object"
    },
    "FsRenameBatchPlanParams": {
      "description": "Params de [`FS_RENAME_BATCH_PLAN`] (0.36.0).",
      "properties": {
        "dir": {
          "$ref": "#/$defs/VPath",
          "description": "El directorio en el que viven TODAS las parejas."
        },
        "pairs": {
          "description": "Los renames pedidos. Más de [`FS_RENAME_BATCH_MAX_PAIRS`] es error de\nparams (`-32602`), no un recorte.",
          "items": {
            "$ref": "#/$defs/RenamePair"
          },
          "maxItems": 4096,
          "type": "array"
        }
      },
      "required": [
        "dir",
        "pairs"
      ],
      "type": "object"
    },
    "FsRenameBatchPlanResult": {
      "description": "Result de [`FS_RENAME_BATCH_PLAN`] (0.36.0).\n\n**Un plan que no se puede ejecutar no lleva pasos** — el core no ordena a\nmedias un plan que no va a ejecutar (decisión 4 del diseño). El invariante\nNORMATIVO, con su dirección exacta, está enunciado UNA vez, en `executable`.\n\n```\nuse norte_proto::methods::{FsRenameBatchPlanResult, PlanHash};\nlet r = FsRenameBatchPlanResult {\n    steps: vec![],\n    collisions: vec![],\n    executable: true,\n    plan_hash: PlanHash::parse(&\"0\".repeat(64)).expect(\"hex\"),\n};\nlet json = serde_json::to_value(&r).expect(\"json\");\n// `collisions` vacío es una lista vacía, jamás una clave ausente.\nassert_eq!(json[\"collisions\"], serde_json::json!([]));\n// El hash viaja como el string desnudo, sin envoltorio del newtype.\nassert_eq!(json[\"plan_hash\"], serde_json::json!(\"0\".repeat(64)));\n```",
      "properties": {
        "collisions": {
          "description": "Todo lo que detiene el plan; vacío cuando `executable`. Como mucho UNA\nentrada por pareja, así que su techo es exactamente\n[`FS_RENAME_BATCH_MAX_PAIRS`] — no el de `steps`, que es mayor porque\nlos temporales añaden pasos sin añadir parejas.\n\nEs la EXPLICACIÓN, no el veredicto: quien decide si se puede ejecutar es\n`executable`.",
          "items": {
            "$ref": "#/$defs/RenameCollision"
          },
          "type": "array"
        },
        "executable": {
          "description": "`true` cuando el plan se puede ejecutar tal cual. Campo NORMATIVO: el\nfrontend deshabilita el confirmar con `!executable`, y no deduce nada de\n`collisions`.\n\nEs derivable de `collisions.is_empty()` HOY, y aun así manda este campo:\nun veredicto futuro podría parar un plan sin nombre ofensor que listar,\ny un cliente que dedujera de la lista lo ejecutaría.\n\nINVARIANTE (el core lo mantiene, un cliente puede asumirlo):\n`executable == false` ⟹ `steps` vacío, y `collisions` no vacío ⟹\n`executable == false`. Nótese la DIRECCIÓN: lo que vacía `steps` es\n`!executable`, no la presencia de veredictos — así el invariante sigue\nen pie para ese veredicto futuro sin nombre que listar, y un cliente que\nignorase este campo y ejecutase `steps` a ciegas no tendría, en ningún\ncaso, nada que ejecutar.",
          "type": "boolean"
        },
        "plan_hash": {
          "$ref": "#/$defs/PlanHash",
          "description": "sha256 en hex minúscula sobre las CONCLUSIONES del plan (directorio,\nparejas ordenadas, pasos resultantes, veredictos, banderas de caja y\nnormalización). Devuélvelo en [`FsRenameBatchParams`]: el core\nre-planifica y compara. Es una cadena hex a propósito — legible en un\nlog, sin ambigüedad de base64, sin bytes en el wire.\n\nSu tipo YA impone la forma ([`PLAN_HASH_LEN`] caracteres hex minúscula):\nuna cadena de otra forma es un error de PARAMS que muere en la\ndeserialización, no [`Error::PlanStale`](crate::Error::PlanStale) — «no\ncasa» y «el directorio cambió» son cosas distintas, y contestar lo\nsegundo a quien mandó basura le miente sobre el estado del mundo."
        },
        "steps": {
          "description": "Pasos en ORDEN de ejecución, temporales incluidos. Acotado por\n[`FS_RENAME_BATCH_MAX_PAIRS`]: como mucho una pareja no nula más un\ntemporal por ciclo, y un ciclo consume al menos dos parejas — el techo\nduro es `pairs * 3 / 2`. Sin constante propia: lo acota el tope de\nparejas.\n\nVACÍO siempre que `executable` sea `false` (ver el invariante en ese\ncampo).",
          "items": {
            "$ref": "#/$defs/RenameStep"
          },
          "type": "array"
        }
      },
      "required": [
        "steps",
        "collisions",
        "executable",
        "plan_hash"
      ],
      "type": "object"
    },
    "FsRenameBatchReportParams": {
      "description": "Params de [`FS_RENAME_BATCH_REPORT`] (0.36.0).",
      "properties": {
        "task_id": {
          "$ref": "#/$defs/TaskId",
          "description": "Task del lote cuyo informe se pide (la de [`FsTaskResult::task_id`]\nque devolvió [`FS_RENAME_BATCH`])."
        }
      },
      "required": [
        "task_id"
      ],
      "type": "object"
    },
    "FsRenameBatchReportResult": {
      "description": "Result de [`FS_RENAME_BATCH_REPORT`] (0.36.0): qué hizo el lote.\n\nUna corrida limpia deja `applied` == el número de pasos del plan y todo lo\ndemás en cero/ausente. **Cualquier otra forma es el ejecutor diciendo la\nverdad sobre un directorio que no pudo dejar como lo encontró** — léelo\naunque la Task haya fallado, y sobre todo cuando diga `cancelled`.\n\n```\nuse norte_proto::methods::FsRenameBatchReportResult;\nlet r = FsRenameBatchReportResult {\n    applied: 3,\n    rolled_back: 0,\n    failed_pair: None,\n    stuck: None,\n    uncertain: None,\n    compensations_lost: 0,\n};\nlet json = serde_json::to_value(&r).expect(\"json\");\n// Lo ausente se OMITE: un lote limpio no manda tres `null`.\nassert_eq!(json.get(\"stuck\"), None);\nassert_eq!(json[\"applied\"], serde_json::json!(3));\n```",
      "properties": {
        "applied": {
          "description": "Pasos aplicados Y journalizados.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "compensations_lost": {
          "default": 0,
          "description": "Reversas que se APLICARON pero cuya entrada compensatoria no se pudo\nescribir. El árbol volvió; el journal sigue diciendo que el rename\ndirecto está en vigor, así que un `policy.undo_session` posterior\nllegará a esa entrada y se BLOQUEARÁ ahí. Un valor distinto de cero es\nel único aviso de eso.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "failed_pair": {
          "description": "La pareja pedida cuyo paso falló, si falló alguno.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "rolled_back": {
          "description": "Pasos que el rollback consiguió devolver.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "stuck": {
          "anyOf": [
            {
              "$ref": "#/$defs/RenameStuckStep"
            },
            {
              "type": "null"
            }
          ],
          "description": "El rollback se rehusó AQUÍ y paró: el directorio está medio\nrenombrado."
        },
        "uncertain": {
          "anyOf": [
            {
              "$ref": "#/$defs/RenameStuckStep"
            },
            {
              "type": "null"
            }
          ],
          "description": "Un paso cuyo provider reportó fallo y que después no se pudo SONDEAR,\nasí que si surtió efecto o no es desconocido. No se asumió ninguna de\nlas dos cosas: `to` es dónde mirar."
        }
      },
      "required": [
        "applied",
        "rolled_back"
      ],
      "type": "object"
    },
    "FsSearchParams": {
      "description": "Params de [`FS_SEARCH`].",
      "properties": {
        "case_sensitive": {
          "default": false,
          "description": "Sensible a mayúsculas (default false; el matching de nombre es\nsobre el lossy en NFC — misma disciplina que el quick search).",
          "type": "boolean"
        },
        "content": {
          "description": "Texto literal a buscar en el CONTENIDO (multi-encoding: la aguja se\ntranscodifica, el pajar jamás se decodifica entero).",
          "type": [
            "string",
            "null"
          ]
        },
        "content_regex": {
          "description": "Regex sobre el contenido (solo ficheros que el detector dé como\ntexto). Excluyente con `content`.",
          "type": [
            "string",
            "null"
          ]
        },
        "max_hits": {
          "description": "Tope de hits: alcanzado, la Task completa (`Completed`, no `Failed`).\nNo hay flag `truncated` en el wire — el cliente infiere la\ntruncación comparando el total de hits recibidos contra `max_hits`\n(== implica truncada).",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "name_glob": {
          "description": "Glob sobre el NOMBRE (último segmento), p.ej. `*.rs`.",
          "type": [
            "string",
            "null"
          ]
        },
        "name_regex": {
          "description": "Regex sobre el nombre. Excluyente con `name_glob`.",
          "type": [
            "string",
            "null"
          ]
        },
        "root": {
          "$ref": "#/$defs/VPath",
          "description": "Raíz del walk (subtree entero)."
        }
      },
      "required": [
        "root"
      ],
      "type": "object"
    },
    "FsStatParams": {
      "description": "Params de [`FS_STAT`].",
      "properties": {
        "attrs": {
          "description": "Ids of the provider attributes to deliver with the entry (0.30.0,\nADR 0039). Empty (the default, and the only possibility for a 0.29\nclient) = none: nothing is delivered unrequested. The contract is at\nmost [`ATTRS_MAX_REQUEST`](crate::attrs::ATTRS_MAX_REQUEST) ids, each\nwell-formed ([`is_valid_attr_id`](crate::attrs::is_valid_attr_id)), and\nviolating either is `-32602` — a rule BLOCK 2 wires up: a 0.30.0 daemon\nignores the requested ids entirely. An id the provider does not offer\nis NOT an error either way: it comes back absent.\n\n# Bounded at decode, but NOT validated — on purpose\n\nSame as [`FsListParams::attrs`], for the same reasons: a malformed id\nsurvives decoding and is the daemon's `-32602` to raise, instead of\nbeing laundered into \"asked for nothing\"; only the RECEIVE-side fields\n— [`Entry::attrs`](crate::Entry::attrs) and\n[`FsCapabilitiesResult::attrs`] — filter. Decoding keeps the first\n`ATTRS_MAX_REQUEST + 1` elements and drains the rest, which is a memory\nbound (a 16 MiB frame of tiny ids would otherwise allocate ~15× its own\nsize) that leaves over-cap observable.",
          "items": {
            "maxLength": 64,
            "pattern": "^[a-z][a-z0-9_-]*(\\.[a-z][a-z0-9_-]*)+$",
            "type": "string"
          },
          "maxItems": 16,
          "type": "array"
        },
        "path": {
          "$ref": "#/$defs/VPath",
          "description": "Nodo a consultar."
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "FsStatResult": {
      "description": "Result de [`FS_STAT`].",
      "properties": {
        "entry": {
          "$ref": "#/$defs/Entry",
          "description": "Metadatos del nodo."
        }
      },
      "required": [
        "entry"
      ],
      "type": "object"
    },
    "FsTaskResult": {
      "description": "Result de [`FS_COPY`], [`FS_MOVE`], [`FS_DELETE`] y [`FS_MKDIR`]: la Task\ncreada. El progreso llega por [`TASK_PROGRESS`].",
      "properties": {
        "task_id": {
          "$ref": "#/$defs/TaskId",
          "description": "Id de la Task encolada."
        }
      },
      "required": [
        "task_id"
      ],
      "type": "object"
    },
    "GrantScopeParams": {
      "description": "Params de [`POLICY_GRANT_SCOPE`] (un humano concede una petición pendiente).",
      "properties": {
        "request_id": {
          "description": "Id devuelto por `policy.request_scope`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "request_id"
      ],
      "type": "object"
    },
    "GrantScopeResult": {
      "description": "Result de [`POLICY_GRANT_SCOPE`].",
      "type": "object"
    },
    "IndexBuildParams": {
      "description": "Params de [`INDEX_BUILD`] (M4): raíz a indexar.",
      "properties": {
        "root": {
          "$ref": "#/$defs/VPath",
          "description": "Raíz del subárbol a (re)indexar."
        }
      },
      "required": [
        "root"
      ],
      "type": "object"
    },
    "IndexBuildResult": {
      "description": "Resultado de [`INDEX_BUILD`] al completar la Task (M4).",
      "properties": {
        "indexed": {
          "description": "Entradas indexadas (insertadas o actualizadas).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "removed": {
          "description": "Filas barridas (paths que ya no existían).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "indexed",
        "removed"
      ],
      "type": "object"
    },
    "IndexEmbedParams": {
      "description": "Params de [`INDEX_EMBED`] (0.33.0, M4-IA-2).",
      "properties": {
        "root": {
          "$ref": "#/$defs/VPath",
          "description": "Root YA indexado con `index.build` (misma clave exacta)."
        }
      },
      "required": [
        "root"
      ],
      "type": "object"
    },
    "IndexHit": {
      "description": "Un hit de [`INDEX_QUERY`] (M4). `path` en bytes crudos vía [`VPath`].",
      "properties": {
        "kind": {
          "$ref": "#/$defs/EntryKind",
          "description": "Tipo de entrada."
        },
        "mtime_ms": {
          "description": "mtime en ms desde epoch (`None` si desconocido).",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "path": {
          "$ref": "#/$defs/VPath",
          "description": "Path completo del resultado."
        },
        "size": {
          "description": "Tamaño (`None` para dirs).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "path",
        "kind"
      ],
      "type": "object"
    },
    "IndexQueryParams": {
      "description": "Params de [`INDEX_QUERY`] (M4).",
      "properties": {
        "limit": {
          "description": "Tope de resultados.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "root": {
          "$ref": "#/$defs/VPath",
          "description": "Raíz cuyo índice se consulta."
        },
        "text": {
          "description": "Texto libre del usuario (se sanea a una query FTS5 en el core).",
          "type": "string"
        }
      },
      "required": [
        "root",
        "text",
        "limit"
      ],
      "type": "object"
    },
    "IndexQueryResult": {
      "description": "Resultado de [`INDEX_QUERY`] (M4): hits ordenados por relevancia.",
      "properties": {
        "hits": {
          "description": "Hits (bm25, más relevante primero).",
          "items": {
            "$ref": "#/$defs/IndexHit"
          },
          "type": "array"
        }
      },
      "required": [
        "hits"
      ],
      "type": "object"
    },
    "IndexSearchSemanticParams": {
      "description": "Params de [`INDEX_SEARCH_SEMANTIC`] (0.33.0, M4-IA-2).",
      "properties": {
        "k": {
          "description": "Máximo de hits; el server recorta a [`INDEX_SEMANTIC_MAX_K`].",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "query": {
          "description": "Consulta en lenguaje natural (sale hacia el proveedor de IA).",
          "type": "string"
        },
        "root": {
          "anyOf": [
            {
              "$ref": "#/$defs/VPath"
            },
            {
              "type": "null"
            }
          ],
          "description": "Root a consultar; ausente ⇒ todos los roots del índice."
        }
      },
      "required": [
        "query",
        "k"
      ],
      "type": "object"
    },
    "IndexSearchSemanticResult": {
      "description": "Result de [`INDEX_SEARCH_SEMANTIC`] (0.33.0), mejor primero.",
      "properties": {
        "hits": {
          "description": "Hits ordenados por score descendente.",
          "items": {
            "$ref": "#/$defs/SemanticHit"
          },
          "type": "array"
        }
      },
      "required": [
        "hits"
      ],
      "type": "object"
    },
    "InitializeParams": {
      "description": "Params de [`INITIALIZE`].",
      "properties": {
        "agent_session": {
          "description": "Si presente, la conexión actúa como SESIÓN DE AGENTE con este id: sus\nmutaciones se evalúan por el policy engine (M3-3). Ausente = frontend\nhumano (`User`, sin sandbox). El servidor liga el actor a la conexión;\nun cliente no puede declararse `User` por otra vía.\n\nEl id se valida server-side fail-closed: 1..=64 chars de\n`[A-Za-z0-9._-]`, si no `INVALID_PARAMS` — viaja a journal, logs y\nmodales de aprobación de los frontends, jamás debe ser un vector de\ninyección (controles/bidi) elegido por el agente.",
          "type": [
            "string",
            "null"
          ]
        },
        "client_info": {
          "$ref": "#/$defs/ClientInfo",
          "description": "Quién se conecta."
        },
        "encodings": {
          "default": [],
          "description": "Encodings que el cliente sabe hablar, por preferencia. Vacío o\nausente = `[\"json\"]` implícito (el único de M2, decisión 4 del\nkickoff: negociado-pero-solo-JSON).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "protocol_version": {
          "description": "Versión del protocolo del cliente; incompatible = error y cierre.",
          "type": "string"
        }
      },
      "required": [
        "client_info",
        "protocol_version"
      ],
      "type": "object"
    },
    "InitializeResult": {
      "description": "Result de [`INITIALIZE`].",
      "properties": {
        "encodings": {
          "description": "Encodings aceptados (hoy siempre `[\"json\"]`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "protocol_version": {
          "description": "Versión del protocolo del core.",
          "type": "string"
        },
        "server_info": {
          "$ref": "#/$defs/ServerInfo",
          "description": "Quién responde."
        }
      },
      "required": [
        "server_info",
        "protocol_version",
        "encodings"
      ],
      "type": "object"
    },
    "MatchInfo": {
      "description": "Contexto de UN match de contenido.",
      "properties": {
        "line": {
          "description": "Línea (1-based) del primer match, si se computó.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "preview": {
          "description": "La línea del match decodificada lossy y saneada EN ORIGEN\n(controles/bidi/invisibles enmascarados a `U+FFFD`, luego recorte a un\ntope fijo de chars). El consumidor puede pintarla directa —jamás llegan\nANSI ni overrides bidi crudos por wire— aunque el TUI sigue pasándola\npor `detail_for_bar` como cinturón.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "PendingApproval": {
      "description": "Una aprobación pendiente (elemento de [`PolicyPendingResult`]).",
      "properties": {
        "approval_id": {
          "description": "Id para responder con `policy.decide`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "op": {
          "description": "Op-kind.",
          "type": "string"
        },
        "paths": {
          "description": "Rutas implicadas (wire, redactadas). SOLO display: jamás se reparsan a\nuna operación — la op real va ligada server-side por `approval_id`.\n\nPuede ser un PREFIJO: ver `paths_total`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "paths_total": {
          "default": 0,
          "description": "Cuántas rutas cubre la decisión (0.36.0). Mismo contrato que\n[`PolicyApprovalRequired::paths_total`], incluido `0` = desconocido.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "session": {
          "description": "Sesión de agente que pidió la op (si aplica).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "approval_id",
        "op",
        "paths"
      ],
      "type": "object"
    },
    "PlanHash": {
      "description": "sha256 of a rename plan's conclusions, as exactly 64 LOWERCASE hex digits. Uppercase is rejected: two spellings of one hash must not compare differently. A string of any other shape is a params error, never `plan_stale`.",
      "maxLength": 64,
      "minLength": 64,
      "pattern": "^[0-9a-f]{64}$",
      "type": "string"
    },
    "PluginColumnInfo": {
      "description": "Una columna que un plugin `columns` contribuye (elemento de\n[`PluginInfo::columns`], 0.28.0, G3c, ADR 0037): discovery — con QUÉ\n`column_id` llamar a [`PLUGIN_COLUMN_VALUES`] y QUÉ cabecera pintar, sin\nque el frontend tenga que adivinar por `category == \"columns\"`. `header`\nes texto del plugin — NO confiable, un frontend debe enmascararlo antes\nde renderizarlo (mismo trato que [`PluginCommandInfo::title`]).\n\n```\nuse norte_proto::methods::PluginColumnInfo;\nlet c: PluginColumnInfo =\n    serde_json::from_str(r#\"{\"id\":\"git-status\",\"header\":\"Git\"}\"#).unwrap();\nassert_eq!(c.id, \"git-status\");\nassert_eq!(c.header, \"Git\");\n```",
      "properties": {
        "header": {
          "description": "Cabecera legible para mostrar. Texto del plugin — NO confiable.",
          "type": "string"
        },
        "id": {
          "description": "Id de la columna (el mismo `column_id` que espera\n[`PLUGIN_COLUMN_VALUES`]).",
          "type": "string"
        }
      },
      "required": [
        "id",
        "header"
      ],
      "type": "object"
    },
    "PluginColumnValuesParams": {
      "description": "Params de [`PLUGIN_COLUMN_VALUES`]: el id de columna declarado por el\nplugin `columns` en su manifiesto, más las rutas visibles a valorar.",
      "properties": {
        "column_id": {
          "description": "Id de la columna (declarado por el plugin; identifica QUÉ columna\nentre varias que un mismo plugin `columns` podría exponer).",
          "type": "string"
        },
        "paths": {
          "description": "Rutas a valorar, en el orden en que se listan.",
          "items": {
            "$ref": "#/$defs/VPath"
          },
          "type": "array"
        },
        "plugin_id": {
          "description": "QUÉ plugin sirve la columna (0.35.0, #120). Ausente = el host resuelve\npor `column_id` a secas, que es lo que hacía antes y sigue haciendo\npara un cliente 0.34.\n\nEl campo existe porque `column_id` NO identifica al plugin y el host\nresolvía a-la-primera-que-casa: dos plugins consentidos que declaren el\nmismo id bare —`status` es el ejemplo obvio— hacían que una columna\nconfigurada como `plugin:a/status` pintara los valores de `b` sin que\nnada lo dijera. El frontend SIEMPRE sabe cuál configuró el usuario (el\nid de configuración lleva el plugin dentro), así que lo que faltaba era\nsitio en el wire para decirlo.\n\nUn host que lo reciba DEBE servir ese plugin o ninguno: si el plugin\nnombrado no está aprobado, activado, o no declara `column_id`, la\nrespuesta son celdas ausentes — jamás las de otro plugin. Caer al\nprimero que case sería reintroducir el fallo con un campo más.\n\n`skip_serializing_if`: una petición sin este campo es byte a byte la de\n0.34, así que la ventana N-1 no ve forma nueva.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "column_id",
        "paths"
      ],
      "type": "object"
    },
    "PluginColumnValuesResult": {
      "description": "Result de [`PLUGIN_COLUMN_VALUES`]: `values` es POSICIONAL 1:1 con\n`PluginColumnValuesParams::paths` — el valor `i` es la celda de la ruta\n`i`. Cada celda es `Option<String>` (no `String`) por la MISMA razón que\n[`DecorationWire::badge`]: una columna que no aplica a esa entrada (p.\nej. \"duración\" sobre un archivo que no es media) necesita distinguirse\nde un valor real que resulta ser la cadena vacía — `None` = sin celda\npara esta entrada de esta columna, jamás se omite del vector posicional\n(protocol-guardian, ADR 0037). Texto del plugin — NO confiable, un\nfrontend debe enmascararlo antes de renderizarlo.",
      "properties": {
        "values": {
          "description": "Valores de celda, uno por elemento de `paths` en el mismo orden;\n`None` = la columna no aplica a esa entrada.",
          "items": {
            "type": [
              "string",
              "null"
            ]
          },
          "type": "array"
        }
      },
      "required": [
        "values"
      ],
      "type": "object"
    },
    "PluginCommandInfo": {
      "description": "Un comando de plugin invocable (elemento de [`PluginInfo::commands`],\nP1): espejo minimal, solo-lectura, de la contribución `command` del\nmanifiesto (`CommandContrib` en `norte-plugin-host`) — lo que un\nfrontend necesita para listar el comando (paleta, gestor de\nextensiones), no para ejecutarlo. `title` es texto suministrado por el\nplugin: NO CONFIABLE, un frontend debe enmascararlo antes de\nrenderizarlo (mismo trato que `PluginInfo::name`).\n\n```\nuse norte_proto::methods::PluginCommandInfo;\nlet c: PluginCommandInfo =\n    serde_json::from_str(r#\"{\"id\":\"greet\",\"title\":\"Greet\"}\"#).unwrap();\nassert_eq!(c.id, \"greet\");\nassert_eq!(c.title, \"Greet\");\n```",
      "properties": {
        "id": {
          "description": "Id del comando dentro del plugin (estable; se pasa junto al id del\nplugin a [`PLUGIN_RUN_COMMAND`]).",
          "type": "string"
        },
        "title": {
          "description": "Título legible para mostrar. Texto del plugin — NO confiable.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "title"
      ],
      "type": "object"
    },
    "PluginConfigKeyWire": {
      "description": "Una clave `[config.<key>]` del esquema de un plugin, esquema + valor\nEFECTIVO juntos (elemento de [`PluginGetConfigResult::keys`], 0.28.0,\nG3c, ADR 0037): mismo criterio \"schema+value together\" que evita una\nsegunda ida y vuelta al wire para pintar la UI de ajustes. `kind` es\ntexto CERRADO (`\"string\"|\"bool\"|\"int\"|\"enum\"` — los cuatro únicos que\n`norte_plugin_host::ConfigKeySpec` declara); un frontend que ve un valor\ndesconocido (peer más nuevo) debe tratarlo como no-editable, jamás\nreventar. `default`/`value` viajan como `String` SIEMPRE (la MISMA\ncodificación canónica que `norte_plugin_host::resolve_settings`: `bool`\n→ `\"true\"`/`\"false\"`, `int` → decimal), coherente con\n[`PluginSetConfigParams::value`], que también es `String`.\n\n```\nuse norte_proto::methods::PluginConfigKeyWire;\nlet k: PluginConfigKeyWire = serde_json::from_str(\n    r#\"{\"key\":\"greeting\",\"kind\":\"string\",\"default\":\"hola\",\"value\":\"hola\"}\"#,\n)\n.unwrap();\nassert_eq!(k.key, \"greeting\");\nassert_eq!(k.kind, \"string\");\nassert!(k.min.is_none());\nassert!(k.values.is_empty());\n```",
      "properties": {
        "default": {
          "description": "Valor por defecto del esquema, codificado como string canónico.",
          "type": "string"
        },
        "description": {
          "description": "Descripción cosmética del manifiesto. Texto del plugin — NO\nconfiable, un frontend debe enmascararla antes de renderizarla\n(mismo trato que `PluginInfo::description`).",
          "type": [
            "string",
            "null"
          ]
        },
        "key": {
          "description": "Nombre de la clave (charset `[a-z0-9-]{1,32}` del manifiesto, seguro\nde mostrar tal cual — mismo criterio que\n`norte_plugin_host::is_valid_config_key`).",
          "type": "string"
        },
        "kind": {
          "description": "Tipo declarado: `\"string\"`, `\"bool\"`, `\"int\"` o `\"enum\"`.",
          "type": "string"
        },
        "max": {
          "description": "Cota superior inclusive (solo `kind == \"int\"`). Ausente = sin cota.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "min": {
          "description": "Cota inferior inclusive (solo `kind == \"int\"`). Ausente = sin cota.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "value": {
          "description": "Valor EFECTIVO actual (defaults de esquema + `config.toml` ya\nsuperpuesto), codificado como string canónico — la MISMA\ncodificación que `default`.",
          "type": "string"
        },
        "values": {
          "default": [],
          "description": "Valores permitidos (solo `kind == \"enum\"`); vacío para el resto de\ntipos — SIEMPRE presente (mismo criterio aditivo que\n`PluginInfo::commands`), nunca omitido.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "key",
        "kind",
        "default",
        "value"
      ],
      "type": "object"
    },
    "PluginDecorateParams": {
      "description": "Params de [`PLUGIN_DECORATE`]: las entradas VISIBLES de la página actual\n(batched — el frontend no pide decoraciones entrada por entrada).",
      "properties": {
        "paths": {
          "description": "Rutas a decorar, en el orden en que se listan.",
          "items": {
            "$ref": "#/$defs/VPath"
          },
          "type": "array"
        }
      },
      "required": [
        "paths"
      ],
      "type": "object"
    },
    "PluginDecorateResult": {
      "description": "Result de [`PLUGIN_DECORATE`]: las decoraciones de cada plugin\n`decorator` aprobado y activado que respondió.",
      "properties": {
        "plugins": {
          "description": "Un elemento por plugin `decorator` que decoró esta página.",
          "items": {
            "$ref": "#/$defs/PluginDecorations"
          },
          "type": "array"
        }
      },
      "required": [
        "plugins"
      ],
      "type": "object"
    },
    "PluginDecorations": {
      "description": "Las decoraciones de UN plugin `decorator` (elemento de\n[`PluginDecorateResult::plugins`]): `decorations` es POSICIONAL 1:1 con\n`PluginDecorateParams::paths` — el elemento `i` decora la ruta `i`, jamás\nuna clave por path (barato en el wire, y un nombre hostil no puede\ncolisionar con otro como clave). Un plugin muerto o que falló\nsimplemente no aparece en `plugins` (sin decoraciones de ESE plugin; el\nresto de la página se pinta igual — mismo contrato de fallback que\n[`PLUGIN_PREVIEW`]).",
      "properties": {
        "decorations": {
          "description": "Decoraciones, UNA por elemento de `paths` en el mismo orden (una\nentrada sin decoración de este plugin lleva\n`DecorationWire{badge:None,role:None}`, nunca se omite — el índice es\nel único enlace con la ruta).",
          "items": {
            "$ref": "#/$defs/DecorationWire"
          },
          "type": "array"
        },
        "plugin_id": {
          "description": "Id del plugin `decorator` que produjo estas decoraciones.",
          "type": "string"
        }
      },
      "required": [
        "plugin_id",
        "decorations"
      ],
      "type": "object"
    },
    "PluginGetConfigParams": {
      "description": "Params de [`PLUGIN_GET_CONFIG`] (0.28.0, G3c).",
      "properties": {
        "id": {
          "description": "Id del plugin cuyo esquema `[config]` se consulta.",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "PluginGetConfigResult": {
      "description": "Result de [`PLUGIN_GET_CONFIG`]: el esquema completo + valores\nefectivos, EN ORDEN DE CLAVE del manifiesto (mismo criterio que\n`PluginInfo::commands`: orden de manifiesto, no reordenado). `id`\ndesconocido responde `keys: []` — nunca un error (mismo criterio\nindulgente que `PLUGIN_LIST` con un catálogo vacío).",
      "properties": {
        "keys": {
          "description": "Una entrada por clave `[config.<key>]` declarada.",
          "items": {
            "$ref": "#/$defs/PluginConfigKeyWire"
          },
          "type": "array"
        }
      },
      "required": [
        "keys"
      ],
      "type": "object"
    },
    "PluginHelpParams": {
      "description": "Params de [`PLUGIN_HELP`].\n\n```\nuse norte_proto::methods::PluginHelpParams;\nlet p: PluginHelpParams = serde_json::from_str(r#\"{\"id\":\"acme.ftp\"}\"#).unwrap();\nassert_eq!(p.id, \"acme.ftp\");\n```",
      "properties": {
        "id": {
          "description": "Id del plugin cuyo `help.md` se pide. Es una CLAVE DE BÚSQUEDA\ncontra el catálogo: el host la resuelve contra los plugins que\ndescubrió y jamás la compone en una ruta de fichero.",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "PluginHelpResult": {
      "description": "Result de [`PLUGIN_HELP`]: el `help.md` acotado y qué se perdió al\nacotarlo.\n\n```\nuse norte_proto::methods::PluginHelpResult;\nlet r: PluginHelpResult =\n    serde_json::from_str(r#\"{\"markdown\":\"body\",\"truncated\":true,\"lossy\":false}\"#)\n        .unwrap();\nassert!(r.truncated && !r.lossy);\n```",
      "properties": {
        "lossy": {
          "default": false,
          "description": "Algún byte no decodificó bajo ninguna lectura y salió `U+FFFD`.\nViaja por la misma razón que `truncated`.",
          "type": "boolean"
        },
        "markdown": {
          "default": "",
          "description": "El `help.md` del plugin, ya acotado y ya UTF-8 VÁLIDO (el host\ndecodifica y sustituye lo irrecuperable). Sin `help.md` legible:\ncadena vacía, nunca un error — la ayuda es cosmética. Por eso el\ncampo AUSENTE también se acepta y se lee como esa misma página vacía\n(`#[serde(default)]`): un peer que expresa «no hay página» omitiéndolo\nno puede recibir un fallo de deserialización por decir justo lo que el\ncontrato ya permite decir. En emisión NO se omite nunca (sin\n`skip_serializing_if`), así que ausente y vacío solo se distinguen de\nENTRADA, y ahí significan lo mismo.\n\nEl tope es [`PLUGIN_HELP_MAX_BYTES`] y acota ESTE TEXTO, no solo los\nbytes del fichero: un byte puede decodificar a tres (windows-1252 `0x80`\n→ `U+20AC`), así que una fuente que cabía justa daría el triple del tope\nsi solo se acotara la fuente. El host corta las dos veces y `truncated`\ncubre ambos cortes, de modo que un receptor puede dimensionar por esa\nconstante y los dos lados ven la MISMA página.\n\nNO está enmascarado: lleva verbatim los peligros de terminal que el\nplugin escribiera (ESC, controles C0, anulaciones bidi). Se parsea con\n`norte_help::parse_untrusted`, que enmascara al construir el modelo;\nnunca se pinta ni se loguea en crudo.",
          "type": "string"
        },
        "truncated": {
          "default": false,
          "description": "El fichero superaba el tope y se cortó. Viaja porque el receptor NO\npuede deducirlo: el texto le llega ya corto, así que su propio parseo\nsaldría limpio y la insignia — la mitigación entera frente a un\n`help.md` hostil — se apagaría en silencio.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "PluginInfo": {
      "description": "Un plugin descubierto (elemento de [`PluginListResult::plugins`], M4-P3).",
      "properties": {
        "approved": {
          "description": "`true` si un humano ya aprobó sus capabilities.",
          "type": "boolean"
        },
        "capabilities": {
          "description": "Capabilities que el plugin solicita (p. ej. `fs-read`). Un humano las\naprueba con [`PLUGIN_SET_APPROVAL`] antes de que surtan efecto.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "category": {
          "description": "Categoría (`previewer`, `indexer`…): qué papel juega en el core.",
          "type": "string"
        },
        "columns": {
          "default": [],
          "description": "Columnas que el plugin contribuye (0.28.0, G3c); vacío si no\ncontribuye ninguna. Un peer N-1 que construye su propio `PluginInfo`\nno emite este campo — se toma por su default (`vec![]`) al\ndeserializar aquí (mismo criterio aditivo que `commands` en 0.26.0).",
          "items": {
            "$ref": "#/$defs/PluginColumnInfo"
          },
          "type": "array"
        },
        "commands": {
          "default": [],
          "description": "Comandos que el plugin expone (P1); vacío si no contribuye ninguno.\nUn peer N-1 que construye su propio `PluginInfo` no emite este\ncampo — se toma por su default (`vec![]`) al deserializar aquí.",
          "items": {
            "$ref": "#/$defs/PluginCommandInfo"
          },
          "type": "array"
        },
        "description": {
          "description": "Descripción cosmética declarada en el manifiesto (P1); ausente =\n`None`. NO forma parte del digest de aprobación (editarla no\nreinvalida capabilities ya aprobadas) y es texto del plugin — NO\nconfiable, un frontend debe enmascararla antes de renderizarla.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "description": "`true` si un humano lo tiene activado.",
          "type": "boolean"
        },
        "has_help": {
          "description": "`true` si el plugin trae un `help.md` SERVIBLE junto a su `plugin.toml`\n(H3e, 0.34.0). Es DISCOVERY barato: decide si el nodo del plugin\naparece en la barra de temas de la ayuda, y evita que\n[`PLUGIN_HELP_MAX_BYTES`] por plugin viajen en cada `plugin.list` — el\ncontenido se pide aparte con [`PLUGIN_HELP`], bajo demanda.\n\nEl host DEBE calcularlo con la MISMA guarda que aplica al servir\n[`PLUGIN_HELP`], no con una comprobación de existencia más laxa. Si no,\nel par (`has_help: true`, `markdown: \"\"`) le dice a quien llama «esa ruta\nexiste y es un fichero regular» sobre un fichero que el host se niega a\nservir — un oráculo de rutas montado con dos métodos abiertos, ninguno\ngateado por policy.\n\nQué puede concluir un RECEPTOR, que es lo que importa aquí:\n\n- `true` NO promete una página con contenido. El host no lee el fichero\n  ni lo parsea, así que un `help.md` vacío o ilegible sale `true` y se\n  degrada al pedirlo (markdown vacío) — la dirección correcta, porque la\n  ayuda es cosmética y jamás tumba un plugin.\n- `false` NO significa «no hay fichero». Significa «no hay página que yo\n  vaya a servir»: puede no existir, o existir y no pasar la guarda (un\n  symlink que sale del directorio del plugin). Las dos son\n  indistinguibles a propósito, y distinguirlas es justo lo que reabriría\n  el oráculo. Quien quiera el diagnóstico lo obtiene de `norte doctor`,\n  en local, no del wire.\n\n`skip_serializing_if` sobre `false`: un plugin sin ayuda produce un\npayload IDÉNTICO byte a byte al de 0.33 (mismo criterio aditivo\nfuerte que los `attrs` de 0.30). Un peer N-1 que construye su propio\n`PluginInfo` no lo emite y aquí se toma por `false`.",
          "type": "boolean"
        },
        "id": {
          "description": "Id estable del plugin (namespace inverso, p. ej. `org.norte.demo`).",
          "type": "string"
        },
        "name": {
          "description": "Nombre legible para mostrar.",
          "type": "string"
        },
        "publisher": {
          "description": "Publicador declarado en el manifiesto.",
          "type": "string"
        },
        "version": {
          "description": "Versión del plugin (informativa).",
          "type": "string"
        }
      },
      "required": [
        "id",
        "name",
        "publisher",
        "version",
        "category",
        "capabilities",
        "approved",
        "enabled"
      ],
      "type": "object"
    },
    "PluginListParams": {
      "description": "Params de [`PLUGIN_LIST`]: objeto vacío, reservado para extensión\n(filtros por categoría/estado llegarán aquí como campos opcionales).",
      "type": "object"
    },
    "PluginListResult": {
      "description": "Result de [`PLUGIN_LIST`]: el catálogo descubierto y los fallos de carga.",
      "properties": {
        "errors": {
          "description": "Directorios que fallaron al cargar (mejor esfuerzo; ver\n[`PluginLoadError`]).",
          "items": {
            "$ref": "#/$defs/PluginLoadError"
          },
          "type": "array"
        },
        "plugins": {
          "description": "Plugins descubiertos y cargados (con su estado aprobado/activo).",
          "items": {
            "$ref": "#/$defs/PluginInfo"
          },
          "type": "array"
        }
      },
      "required": [
        "plugins",
        "errors"
      ],
      "type": "object"
    },
    "PluginLoadError": {
      "description": "Un directorio de plugin que NO se pudo cargar (elemento de\n[`PluginListResult::errors`], M4-P3): se reporta para diagnóstico, sin\ntumbar el resto del catálogo.",
      "properties": {
        "dir": {
          "description": "Directorio del plugin que falló (display; puede llevar bytes lossy).",
          "type": "string"
        },
        "reason": {
          "description": "Motivo legible del fallo (manifiesto inválido, versión no soportada…).",
          "type": "string"
        }
      },
      "required": [
        "dir",
        "reason"
      ],
      "type": "object"
    },
    "PluginPreview": {
      "description": "La preview producida por un plugin previewer: los tres campos van JUNTOS\n(all-or-nothing). Ver [`PluginPreviewResult`].",
      "properties": {
        "lossy": {
          "default": false,
          "description": "La decodificación host-side del fichero fue LOSSY (0.29.0, #101): el\ncore detectó texto en un encoding no-UTF8 y algún byte no era válido,\nasí que los `�` de la salida vienen de la decodificación, no del\nfichero. El frontend lo señala junto al indicador «via …» (el viewer\ncrudo ya marca su propio `had_errors`; esto le da al modo preview la\nmisma honestidad). Aditivo sobre 0.28.x: `#[serde(default)]` = un\ncliente/daemon N-1 que no lo emite se lee como `false` (sin aviso,\ndirección segura). SIEMPRE presente al serializar (mismo criterio\n«aditivo siempre presente» que `PluginInfo::commands`).",
          "type": "boolean"
        },
        "output": {
          "description": "Salida (texto) de la preview.",
          "type": "string"
        },
        "plugin_id": {
          "description": "Id del plugin previewer que produjo la salida.",
          "type": "string"
        },
        "plugin_name": {
          "description": "Nombre legible del plugin previewer (para el indicador «via …»).",
          "type": "string"
        }
      },
      "required": [
        "plugin_id",
        "plugin_name",
        "output"
      ],
      "type": "object"
    },
    "PluginPreviewParams": {
      "description": "Params de [`PLUGIN_PREVIEW`] (M4-P5).",
      "properties": {
        "path": {
          "$ref": "#/$defs/VPath",
          "description": "Ruta del archivo a previsualizar (el core lee sus bytes)."
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "PluginPreviewResult": {
      "description": "Result de [`PLUGIN_PREVIEW`] (M4-P5): la preview del primer previewer que\naplica, o NADA. El `flatten` sobre un `Option` hace que el wire sea\n`{plugin_id,plugin_name,output}` (aplicó) o `{}` (ninguno); el TIPO Rust hace\nINCONSTRUIBLE un estado parcial (los tres campos van juntos en\n[`PluginPreview`]), y un objeto parcial del wire colapsa a `None` (sin\npreview, seguro) — jamás un `plugin_id` sin `output` (protocol-guardian\nM4-P5). `None` = ningún previewer maneja el mimetype; el frontend cae a la\nvista cruda.",
      "properties": {
        "lossy": {
          "default": false,
          "description": "La decodificación host-side del fichero fue LOSSY (0.29.0, #101): el\ncore detectó texto en un encoding no-UTF8 y algún byte no era válido,\nasí que los `�` de la salida vienen de la decodificación, no del\nfichero. El frontend lo señala junto al indicador «via …» (el viewer\ncrudo ya marca su propio `had_errors`; esto le da al modo preview la\nmisma honestidad). Aditivo sobre 0.28.x: `#[serde(default)]` = un\ncliente/daemon N-1 que no lo emite se lee como `false` (sin aviso,\ndirección segura). SIEMPRE presente al serializar (mismo criterio\n«aditivo siempre presente» que `PluginInfo::commands`).",
          "type": "boolean"
        },
        "output": {
          "description": "Salida (texto) de la preview.",
          "type": "string"
        },
        "plugin_id": {
          "description": "Id del plugin previewer que produjo la salida.",
          "type": "string"
        },
        "plugin_name": {
          "description": "Nombre legible del plugin previewer (para el indicador «via …»).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "PluginPreviewStyled": {
      "description": "La preview CON ESTILO producida por un plugin previewer (0.27.0, G3, ADR\n0037): gemelo de [`PluginPreview`] con `lines` de [`SpanWire`] en vez de\nun `output: String` plano. Topes del wire (ADR 0037): ≤10 000 líneas,\n≤64 spans por línea, texto de span ≤4 KiB, payload total ≤4 MiB (mismo\ntope de retorno del runtime que ya usan [`PLUGIN_PREVIEW`]/\n[`PLUGIN_RUN_COMMAND`]) — el server los aplica antes de enviar; un\ncliente los re-valida y cae a [`PLUGIN_PREVIEW`] si se violan.",
      "properties": {
        "lines": {
          "description": "Líneas de la preview; cada línea es una lista de spans en orden.",
          "items": {
            "items": {
              "$ref": "#/$defs/SpanWire"
            },
            "type": "array"
          },
          "type": "array"
        },
        "lossy": {
          "default": false,
          "description": "La decodificación host-side del fichero fue LOSSY (0.29.0, #101):\nidéntico a [`PluginPreview::lossy`] — el previewer con estilo recibe el\nMISMO texto ya decodificado por el core, así que hereda el mismo aviso.",
          "type": "boolean"
        },
        "plugin_id": {
          "description": "Id del plugin previewer que produjo la salida.",
          "type": "string"
        },
        "plugin_name": {
          "description": "Nombre legible del plugin previewer (para el indicador «via …»).",
          "type": "string"
        }
      },
      "required": [
        "plugin_id",
        "plugin_name",
        "lines"
      ],
      "type": "object"
    },
    "PluginPreviewStyledParams": {
      "description": "Params de [`PLUGIN_PREVIEW_STYLED`]: idéntico a [`PluginPreviewParams`]\n(mismo archivo, misma resolución de previewer — solo cambia la forma del\nresult).",
      "properties": {
        "path": {
          "$ref": "#/$defs/VPath",
          "description": "Ruta del archivo a previsualizar (el core lee sus bytes)."
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "PluginPreviewStyledResult": {
      "description": "Result de [`PLUGIN_PREVIEW_STYLED`]: la preview con estilo del primer\npreviewer que aplica, o NADA. Mismo patrón `flatten`-sobre-`Option`\nall-or-nothing que [`PluginPreviewResult`] (ver su rustdoc): el wire es\n`{plugin_id,plugin_name,lines}` (aplicó) o `{}` (ninguno), jamás un\nestado parcial.",
      "properties": {
        "lines": {
          "description": "Líneas de la preview; cada línea es una lista de spans en orden.",
          "items": {
            "items": {
              "$ref": "#/$defs/SpanWire"
            },
            "type": "array"
          },
          "type": "array"
        },
        "lossy": {
          "default": false,
          "description": "La decodificación host-side del fichero fue LOSSY (0.29.0, #101):\nidéntico a [`PluginPreview::lossy`] — el previewer con estilo recibe el\nMISMO texto ya decodificado por el core, así que hereda el mismo aviso.",
          "type": "boolean"
        },
        "plugin_id": {
          "description": "Id del plugin previewer que produjo la salida.",
          "type": "string"
        },
        "plugin_name": {
          "description": "Nombre legible del plugin previewer (para el indicador «via …»).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "PluginRunCommandParams": {
      "description": "Params de [`PLUGIN_RUN_COMMAND`] (M4-P4).",
      "properties": {
        "arg": {
          "default": "",
          "description": "Argumento del comando. Ausente = `\"\"` (el default del wire): un cliente\nque no lo envía ejecuta el comando sin argumento.",
          "type": "string"
        },
        "command": {
          "description": "Nombre del comando a ejecutar (declarado por el plugin).",
          "type": "string"
        },
        "id": {
          "description": "Id del plugin que expone el comando.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "command"
      ],
      "type": "object"
    },
    "PluginRunCommandResult": {
      "description": "Result de [`PLUGIN_RUN_COMMAND`]: la salida del comando del plugin.",
      "properties": {
        "output": {
          "description": "Salida (string) que devuelve el comando del plugin.",
          "type": "string"
        }
      },
      "required": [
        "output"
      ],
      "type": "object"
    },
    "PluginSetApprovalParams": {
      "description": "Params de [`PLUGIN_SET_APPROVAL`].",
      "properties": {
        "approved": {
          "description": "`true` = aprobar las capabilities, `false` = revocar.",
          "type": "boolean"
        },
        "id": {
          "description": "Id del plugin a (des)aprobar.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "approved"
      ],
      "type": "object"
    },
    "PluginSetApprovalResult": {
      "description": "Result de [`PLUGIN_SET_APPROVAL`]: objeto vacío, reservado para extensión.",
      "type": "object"
    },
    "PluginSetConfigParams": {
      "description": "Params de [`PLUGIN_SET_CONFIG`] (0.28.0, G3c): `value` es SIEMPRE\n`String` (la codificación canónica descrita en\n[`PluginConfigKeyWire::value`]) — el daemon la valida contra el ESQUEMA\nde `key` antes de persistir; nunca se persiste sin validar (spec S2).",
      "properties": {
        "id": {
          "description": "Id del plugin cuyo ajuste se cambia.",
          "type": "string"
        },
        "key": {
          "description": "Clave `[config.<key>]` a fijar.",
          "type": "string"
        },
        "value": {
          "description": "Valor nuevo, codificado como string canónico (ver\n[`PluginConfigKeyWire::value`]).",
          "type": "string"
        }
      },
      "required": [
        "id",
        "key",
        "value"
      ],
      "type": "object"
    },
    "PluginSetConfigResult": {
      "description": "Result de [`PLUGIN_SET_CONFIG`]: objeto vacío, reservado para extensión.",
      "type": "object"
    },
    "PluginSetEnabledParams": {
      "description": "Params de [`PLUGIN_SET_ENABLED`].",
      "properties": {
        "enabled": {
          "description": "`true` = activar, `false` = desactivar.",
          "type": "boolean"
        },
        "id": {
          "description": "Id del plugin a activar/desactivar.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "enabled"
      ],
      "type": "object"
    },
    "PluginSetEnabledResult": {
      "description": "Result de [`PLUGIN_SET_ENABLED`]: objeto vacío, reservado para extensión.",
      "type": "object"
    },
    "PolicyApprovalRequired": {
      "description": "Notificación [`POLICY_APPROVAL_REQUIRED`] (server→client): una op `ask`\nespera decisión. Las rutas van REDACTADAS si llevan userinfo (regla 10).",
      "properties": {
        "approval_id": {
          "description": "Id para responder con `policy.decide`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "op": {
          "description": "Op-kind (`copy|move|delete|mkdir`).",
          "type": "string"
        },
        "paths": {
          "description": "Rutas implicadas (wire, redactadas). SOLO display: jamás se reparsan a\nuna operación — la op real va ligada server-side por `approval_id`.\n\nPuede ser un PREFIJO de las rutas que la decisión cubre: ver\n`paths_total`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "paths_total": {
          "default": 0,
          "description": "Cuántas rutas cubre de verdad la decisión (0.36.0). `0` = DESCONOCIDO\n(un server N-1 no lo mandaba), y entonces vale `paths.len()`.\n\nNORMATIVO para un frontend: si es mayor que `paths.len()`, la lista está\nRECORTADA y hay que decírselo al humano. Un lote de renames\n([`FS_RENAME_BATCH`]) gatea dos rutas por paso y puede traer miles; el\nserver recorta lo que difunde —la notificación va a cada conexión humana\ny se retiene durante el TTL—, pero la DECISIÓN se toma sobre todas. Un\nhumano que aprueba 32 rutas de aspecto inocente sin saber que había ocho\nmil no está consintiendo lo que cree.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "session": {
          "description": "Sesión de agente que pidió la op (si aplica).",
          "type": [
            "string",
            "null"
          ]
        },
        "ttl_ms": {
          "description": "TTL de la aprobación en milisegundos. `0` = DESCONOCIDO (p. ej. una\npendiente reconstruida del resync de `policy.pending`, que no\ntransporta el TTL restante): el frontend no pinta cuenta atrás.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "approval_id",
        "op",
        "paths",
        "ttl_ms"
      ],
      "type": "object"
    },
    "PolicyDecideParams": {
      "description": "Params de [`POLICY_DECIDE`] (un humano aprueba/deniega).",
      "properties": {
        "approval_id": {
          "description": "Id de la aprobación pendiente.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "approve": {
          "description": "`true` = aprobar, `false` = denegar.",
          "type": "boolean"
        }
      },
      "required": [
        "approval_id",
        "approve"
      ],
      "type": "object"
    },
    "PolicyDecideResult": {
      "description": "Result de [`POLICY_DECIDE`].",
      "type": "object"
    },
    "PolicyPendingResult": {
      "description": "Result de [`POLICY_PENDING`] (resync de aprobaciones pendientes).",
      "properties": {
        "pending": {
          "description": "Aprobaciones pendientes.",
          "items": {
            "$ref": "#/$defs/PendingApproval"
          },
          "type": "array"
        }
      },
      "required": [
        "pending"
      ],
      "type": "object"
    },
    "PolicyUndoReportParams": {
      "description": "Params de [`POLICY_UNDO_REPORT`].",
      "properties": {
        "task_id": {
          "$ref": "#/$defs/TaskId",
          "description": "Task de undo cuyo informe se pide (la de\n[`PolicyUndoSessionResult::task_id`])."
        }
      },
      "required": [
        "task_id"
      ],
      "type": "object"
    },
    "PolicyUndoReportResult": {
      "description": "Result de [`POLICY_UNDO_REPORT`]: el informe del undo. Todo cero y sin\n`blocked` = no había nada que deshacer SOLO si la Task terminó\n`Completed`: una Task `Failed`/`Cancelled` deja contadores PARCIALES con\n`blocked` ausente (el motivo vive en su `task.progress` terminal) — el\nestado de la Task se consulta aparte, este result no lo lleva.",
      "properties": {
        "batch_stuck": {
          "anyOf": [
            {
              "$ref": "#/$defs/RenameStuckStep"
            },
            {
              "type": "null"
            }
          ],
          "description": "**Deshacer un LOTE de renames (0.36.0) se quedó a medias**: el ejecutor\nno pudo devolver un paso de undo que ya había aplicado, así que el\ndirectorio NO volvió a como estaba.\n\nEs una categoría propia y no un `blocked`: `blocked` dice «paré aquí y\nel árbol está consistente», y esto dice justo lo contrario. Cuando está\npresente la Task termina `Failed` — un undo que dijera `Completed`\nprometería un árbol restaurado que no lo está."
        },
        "blocked": {
          "anyOf": [
            {
              "$ref": "#/$defs/UndoBlocked"
            },
            {
              "type": "null"
            }
          ],
          "description": "Primer paso donde el LIFO paró (estricto), si lo hubo. El undo va de\nla entrada MÁS NUEVA hacia atrás: lo posterior al bloqueo en el\njournal ya se deshizo; lo ANTERIOR a él en el journal (seq menor)\nquedó SIN deshacer."
        },
        "compensations_lost": {
          "default": 0,
          "description": "Reversas del undo de un lote que se APLICARON pero cuya compensación no\nse pudo escribir (0.36.0). Cada una deja una entrada que sigue\npareciendo pendiente aunque su efecto ya volvió: un undo posterior la\nencontrará y se bloqueará ahí. Es la única señal de eso.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "skipped_created_no_trash": {
          "description": "Reversas de `Created` saltadas porque el provider no tiene papelera\n(#65): el nodo SIGUE en el destino — deshacerlo habría sido un borrado\npermanente y el undo jamás destruye de forma irrecuperable.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "skipped_irreversible": {
          "description": "Entradas `Irreversible` saltadas (no hay nada que pisar).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "undone": {
          "description": "Entradas revertidas con éxito.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "undone",
        "skipped_irreversible",
        "skipped_created_no_trash"
      ],
      "type": "object"
    },
    "PolicyUndoSessionParams": {
      "description": "Params de [`POLICY_UNDO_SESSION`].",
      "properties": {
        "session": {
          "description": "Sesión de agente cuyas mutaciones se deshacen (mismo formato que\n`agent_session` del initialize: `[A-Za-z0-9._-]`, 1..=64).",
          "type": "string"
        }
      },
      "required": [
        "session"
      ],
      "type": "object"
    },
    "PolicyUndoSessionResult": {
      "description": "Result de [`POLICY_UNDO_SESSION`]: el undo corre como Task (progreso por\n`task.progress`, cancelable con `task.cancel`).",
      "properties": {
        "task_id": {
          "$ref": "#/$defs/TaskId",
          "description": "Task del undo."
        }
      },
      "required": [
        "task_id"
      ],
      "type": "object"
    },
    "RenameCollision": {
      "description": "Un nombre rechazado más su veredicto, DIRECCIONABLE a la pareja que lo\nprovocó.\n\n```\nuse norte_proto::Segment;\nuse norte_proto::methods::{RenameCollision, RenameCollisionKind};\nlet c = RenameCollision {\n    pair_index: 1,\n    name: Segment::new(b\"ep01.mkv\".to_vec()).expect(\"segment\"),\n    kind: RenameCollisionKind::Internal,\n};\nassert_eq!(serde_json::to_string(&c).expect(\"json\"),\n           r#\"{\"pair_index\":1,\"name\":\"ep01.mkv\",\"kind\":\"internal\"}\"#);\n```",
      "properties": {
        "kind": {
          "$ref": "#/$defs/RenameCollisionKind",
          "description": "El veredicto."
        },
        "name": {
          "$ref": "#/$defs/Segment",
          "description": "El nombre ofensor. QUÉ nombre depende de `kind`:\n\n- `Internal` — el DESTINO que esta pareja no va a conseguir;\n- `External` — el fichero que ESTORBA, escrito como lo escribe el\n  directorio, que no tiene por qué ser como lo escribió la petición: un\n  gemelo NFD tapa un destino NFC y lo que el humano necesita ver es el\n  gemelo, no un eco de lo que ya tecleó;\n- `AbsentSource` / `AmbiguousSource` — el ORIGEN, tal cual lo mandó\n  quien pidió, porque ese es el texto que tiene que corregir.\n\nEl que no depende de nada es `pair_index`."
        },
        "pair_index": {
          "description": "Índice, dentro de `pairs` de la petición, de la pareja que este\nveredicto RECHAZA. Para `Internal` es la pareja POSTERIOR en el orden de\n`pairs` (la primera se señala sola si también resulta rechazada). No hay\npareja «ganadora» que un frontend pueda pintar como aceptada: un plan con\nveredictos no ejecuta NADA.\n\nEstá definido para toda clase, incluidas las futuras, y ese es su\nmotivo: `name` cambia de significado con `kind` (ver ahí abajo), así que\nbajo [`RenameCollisionKind::Unknown`] un cliente no sabría qué está\nmirando. Con el índice siempre puede señalar la fila culpable, aunque no\nentienda el veredicto — que es justo lo que el fallback promete.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "pair_index",
        "name",
        "kind"
      ],
      "type": "object"
    },
    "RenameCollisionKind": {
      "description": "Por qué un plan no se puede ejecutar. Vocabulario CERRADO: el core jamás\ninventa una clase.\n\nTolerancia N/N-1 (ADR 0004/0005, mismo patrón que\n[`ConflictKind`](crate::ConflictKind)): una clase desconocida deserializa a\n[`RenameCollisionKind::Unknown`] en vez de reventar el parse del plan\nentero. Importa aunque el vocabulario nazca cerrado: un cliente 0.36 negocia\ncon un daemon 0.37, y un veredicto nuevo allí no puede dejarlo sin plan que\npintar — degrada a «rechazado, motivo que no entiendo».\n\n```\nuse norte_proto::methods::RenameCollisionKind;\nassert_eq!(\n    serde_json::to_string(&RenameCollisionKind::AbsentSource).expect(\"json\"),\n    r#\"\"absent_source\"\"#\n);\nlet futuro: RenameCollisionKind =\n    serde_json::from_str(r#\"\"clase_del_futuro\"\"#).expect(\"json\");\nassert_eq!(futuro, RenameCollisionKind::Unknown);\n```",
      "oneOf": [
        {
          "const": "internal",
          "description": "Una pareja ANTERIOR del mismo lote hace imposible esta: le tomó el\ndestino, o le tomó el ORIGEN. Ambas cosas se miden con la equivalencia\nde colisiones del directorio (NFC, y plegado de caja donde la caja se\npliega), no byte a byte, así que dos parejas que nombran ficheros\ndistintos que ese directorio no distingue también caen aquí.",
          "type": "string"
        },
        {
          "const": "external",
          "description": "El destino ya existe y NINGUNA pareja del lote lo va a quitar de en\nmedio. Que exista una pareja cuyo origen sea ese nombre no basta: si esa\npareja es nula, o su origen es un fichero GEMELO del que estorba (`café`\nen NFC frente a `café` en NFD), el fichero sigue ahí.",
          "type": "string"
        },
        {
          "const": "absent_source",
          "description": "El origen de la pareja no está en el directorio (el plan se construyó\ncontra un listado rancio).",
          "type": "string"
        },
        {
          "const": "ambiguous_source",
          "description": "El origen no coincide EXACTAMENTE con ningún nombre del directorio y se\npliega sobre DOS O MÁS a la vez — dos ficheros que las reglas de ese\ndirectorio no distinguen, típicamente `café` en NFC y en NFD conviviendo\nen un ext4, o `Foo` y `foo` en un listado que contradice sus propias\ncapacidades.\n\nEl origen NO falta: sobra. El core no adivina cuál de los dos se pedía,\nporque acertar la mitad de las veces es peor que no hacer nada. Lo que\nresuelve ESTE veredicto es escribir el nombre BYTE A BYTE como lo\ndevuelve `fs.list`: con la ortografía exacta hay coincidencia exacta y\nel origen queda identificado.\n\nOjo, no es una llave maestra para el directorio gemelo. Renombrar los\nDOS gemelos en un mismo lote sigue sin poder ser, porque las colisiones\nse miden plegadas y el segundo se lleva un [`Self::Internal`]. Hay que\nmandarlos en lotes distintos.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "Clase de un protocolo más nuevo (fallback de deserialización).\nEl core JAMÁS la emite.",
          "type": "string"
        }
      ]
    },
    "RenamePair": {
      "description": "Un rename PEDIDO dentro de un directorio: nombres base, no rutas.\n\nA diferencia de [`AiRenameEntry`] (donde el proveedor de IA garantiza UTF-8\ny el engine rechaza lo demás fail-loud), aquí los nombres son [`Segment`]:\nun lote de renames es exactamente donde un nombre no-UTF8 tiene que\nsobrevivir byte a byte (regla dura 1).\n\n```\nuse norte_proto::{Segment, methods::RenamePair};\n// Un nombre que NO es UTF-8 a la izquierda: el wire lo escapa y los bytes\n// vuelven intactos, que es justo lo que un `String` no podría prometer.\nlet p = RenamePair {\n    from: Segment::new(b\"caf\\xff.txt\".to_vec()).expect(\"segment\"),\n    to: Segment::new(b\"cafe.txt\".to_vec()).expect(\"segment\"),\n};\nlet json = serde_json::to_string(&p).expect(\"json\");\nassert_eq!(json, r#\"{\"from\":\"caf%FF.txt\",\"to\":\"cafe.txt\"}\"#);\nlet back: RenamePair = serde_json::from_str(&json).expect(\"json\");\nassert_eq!(back.from.as_bytes(), b\"caf\\xff.txt\");\n```",
      "properties": {
        "from": {
          "$ref": "#/$defs/Segment",
          "description": "Nombre existente en el directorio."
        },
        "to": {
          "$ref": "#/$defs/Segment",
          "description": "Nombre propuesto."
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "RenameStep": {
      "description": "Un paso del plan ORDENADO. `temp` marca un paso que es MAQUINARIA del\nplanificador (romper un ciclo), no algo que el usuario haya pedido.\n\nUna permutación `a→b, b→a` son TRES pasos, y el temporal aparece en dos:\n`a → .norte-rename-XXXXXXXX-0` (`temp`), `b → a`, y\n`.norte-rename-XXXXXXXX-0 → b` (`temp`). Sin el tercero el fichero que\nempezó como `a` se queda aparcado bajo el nombre de máquina.\n\nUn paso NO dice de qué pareja viene, y es deliberado: los pasos son\nMAQUINARIA opaca. Un frontend pinta las parejas del usuario — que ya tiene —\nmás los veredictos, que sí llevan `pair_index`; el ORDEN es asunto del core\n(ver [`FS_RENAME_BATCH_PLAN`]), y un temporal parte una pareja en dos pasos\njusto porque lo es. Si algún día un frontend demuestra que necesita el\nmapeo, añadir el campo es un cambio ADITIVO — la forma correcta para una\nnecesidad que todavía no se ha enseñado.\n\n```\nuse norte_proto::{Segment, methods::RenameStep};\nlet s = RenameStep {\n    from: Segment::new(b\"caf\\xff.txt\".to_vec()).expect(\"segment\"),\n    to: Segment::new(b\"cafe.txt\".to_vec()).expect(\"segment\"),\n    temp: false,\n};\nassert_eq!(serde_json::to_string(&s).expect(\"json\"),\n           r#\"{\"from\":\"caf%FF.txt\",\"to\":\"cafe.txt\",\"temp\":false}\"#);\n```",
      "properties": {
        "from": {
          "$ref": "#/$defs/Segment",
          "description": "Nombre antes de este paso."
        },
        "temp": {
          "description": "`true` si ALGUNO de los dos lados de este paso es un nombre temporal\npropiedad del planificador — propiedad del PASO, no de `to`: el paso que\nsaca al fichero del temporal lo lleva en `from` y es maquinaria igual.\nUn frontend jamás lo presenta como propuesta del usuario.",
          "type": "boolean"
        },
        "to": {
          "$ref": "#/$defs/Segment",
          "description": "Nombre después de este paso."
        }
      },
      "required": [
        "from",
        "to",
        "temp"
      ],
      "type": "object"
    },
    "RenameStuckStep": {
      "description": "Un paso de un lote que se quedó APLICADO y que el core no pudo devolver a\nsu sitio (0.36.0). Dicho de otro modo: el directorio está medio renombrado\ny esto dice dónde mirar.\n\nLos dos lados van como [`VPath`] absoluto y no como [`Segment`]: quien lee\nesto está buscando un fichero, y darle el nombre suelto le obligaría a\nrecomponer el directorio de la petición para poder ir a por él.\n\n```\nuse norte_proto::{VPath, methods::RenameStuckStep};\nlet s = RenameStuckStep {\n    from: VPath::parse(\"file:///fotos/a\").expect(\"path\"),\n    to: VPath::parse(\"file:///fotos/b\").expect(\"path\"),\n    pair_index: 0,\n    error: norte_proto::Error::Io { retryable: false },\n    journalled: true,\n    still_applied: 1,\n};\nlet json = serde_json::to_value(&s).expect(\"json\");\nassert_eq!(json[\"to\"], serde_json::json!(\"file:///fotos/b\"));\nassert_eq!(json[\"journalled\"], serde_json::json!(true));\n```",
      "properties": {
        "error": {
          "$ref": "#/$defs/Error",
          "description": "Por qué se rehusó la reversa, o por qué el destino del paso es\ndesconocido (taxonomía de errores del protocolo)."
        },
        "from": {
          "$ref": "#/$defs/VPath",
          "description": "El nombre que el fichero tenía ANTES del lote: donde no se pudo\ndevolver."
        },
        "journalled": {
          "description": "¿Hay entrada de journal detrás de este paso?\n\nDecide QUIÉN tiene que limpiar. `true`: la entrada describe el rename,\nasí que un `policy.undo_session` posterior puede rematarlo cuando el\nobstáculo desaparezca. `false`: el rename surtió efecto pero su entrada\nnunca aterrizó (regla dura 4 — el journal no sabe que ocurrió), así que\nningún undo lo encontrará y solo un humano puede deshacerlo.",
          "type": "boolean"
        },
        "pair_index": {
          "description": "Índice, dentro de `pairs` de la petición, de la pareja de la que\ndesciende el paso. Un temporal parte una pareja en dos pasos, así que\ndos atascos distintos pueden citar la misma pareja.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "still_applied": {
          "description": "Cuántos pasos de ese lote siguen aplicados, este incluido.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "to": {
          "$ref": "#/$defs/VPath",
          "description": "El nombre que el fichero lleva AHORA. Es el que hay que buscar."
        }
      },
      "required": [
        "from",
        "to",
        "pair_index",
        "error",
        "journalled",
        "still_applied"
      ],
      "type": "object"
    },
    "RequestId": {
      "anyOf": [
        {
          "description": "Contador del emisor canónico.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        {
          "description": "Aceptado por tolerancia con otros clientes JSON-RPC.",
          "type": "string"
        }
      ],
      "description": "Id de una request. El emisor canónico escribe números (contador u64);\nse aceptan strings por tolerancia (JSON-RPC 2.0 los permite).\n\n```\nuse norte_proto::wire::RequestId;\nlet n: RequestId = serde_json::from_str(\"7\").unwrap();\nassert_eq!(n, RequestId::Num(7));\nlet s: RequestId = serde_json::from_str(r#\"\"abc\"\"#).unwrap();\nassert_eq!(s, RequestId::Str(\"abc\".into()));\n```"
    },
    "RequestScopeParams": {
      "description": "Params de [`POLICY_REQUEST_SCOPE`] (M3-3b): un agente pide un scope.",
      "properties": {
        "ops": {
          "description": "Op-kinds solicitados (`copy|move|delete|mkdir`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "roots": {
          "description": "Raíces solicitadas (contención por subtree-prefix).",
          "items": {
            "$ref": "#/$defs/VPath"
          },
          "type": "array"
        },
        "session": {
          "description": "Sesión de agente que pide (debe coincidir con la de la conexión).",
          "type": "string"
        },
        "ttl_ms": {
          "description": "TTL solicitado en milisegundos.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "session",
        "roots",
        "ops",
        "ttl_ms"
      ],
      "type": "object"
    },
    "RequestScopeResult": {
      "description": "Result de [`POLICY_REQUEST_SCOPE`].",
      "properties": {
        "request_id": {
          "description": "Id de la petición, para que un humano la conceda con `policy.grant_scope`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "request_id"
      ],
      "type": "object"
    },
    "ResumePolicy": {
      "description": "Reanudación de una transferencia interrumpida (ADR 0012, spec §5).\n\n```\nuse norte_proto::ResumePolicy;\nassert_eq!(ResumePolicy::default(), ResumePolicy::Off);\nlet p: ResumePolicy = serde_json::from_str(r#\"\"on\"\"#).unwrap();\nassert_eq!(p, ResumePolicy::On);\n```",
      "oneOf": [
        {
          "const": "off",
          "description": "Sin reanudación: cancelar/fallar deja el destino LIMPIO (contrato\nde M1). Default — cero sorpresas para quien no lo pide.",
          "type": "string"
        },
        {
          "const": "on",
          "description": "Reanudar: cancelar o un fallo transitorio CONSERVA el\n`.norte-partial`; la próxima copia del mismo `src→dst` continúa\ndesde donde iba (`already` del provider, ADR 0012).",
          "type": "string"
        }
      ]
    },
    "RpcCancelParams": {
      "description": "Params de [`RPC_CANCEL`] (#72): el id de la request en vuelo a cancelar.\n\nEl `id` es el mismo tipo que [`crate::wire::RequestId`] — número (emisor\ncanónico) o string (tolerancia JSON-RPC). No se valida contra un mapa aquí\n(es una notificación best-effort): el daemon lo coteja con sus requests en\nvuelo y un id sin correspondencia es un no-op.",
      "properties": {
        "id": {
          "$ref": "#/$defs/RequestId",
          "description": "Id JSON-RPC de la request a cancelar."
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "SearchHits": {
      "description": "Un lote de resultados de [`SEARCH_HITS`]. `matches` alineado 1:1 con\n`entries` cuando la búsqueda es de contenido (None si es solo nombre).",
      "properties": {
        "entries": {
          "description": "Entradas que casan.",
          "items": {
            "$ref": "#/$defs/Entry"
          },
          "type": "array"
        },
        "matches": {
          "description": "Contexto del match de contenido, alineado con `entries`.",
          "items": {
            "$ref": "#/$defs/MatchInfo"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "task_id": {
          "$ref": "#/$defs/TaskId",
          "description": "Task dueña (correlación con `fs.search` → `task_id`)."
        }
      },
      "required": [
        "task_id",
        "entries"
      ],
      "type": "object"
    },
    "Segment": {
      "description": "One path component in its percent-encoded wire form (ADR 0001). Never assume UTF-8: the decoded bytes are the name. A literal `%` is always `%25`, and C0/DEL control bytes are always escaped, even inside otherwise-valid UTF-8. The decoded bytes must be non-empty, must not contain `/` or NUL, and must not be `.` or `..`.",
      "minLength": 1,
      "type": "string"
    },
    "SemanticHit": {
      "description": "Un hit semántico: path + similitud coseno. Sin `Eq` (a diferencia de sus\nhermanos de `index.*`): `score` es `f64`.",
      "properties": {
        "path": {
          "$ref": "#/$defs/VPath",
          "description": "Path del fichero (wire encoding)."
        },
        "score": {
          "description": "Similitud coseno en `[-1, 1]` (mayor = más afín).\nSiempre finito: el server jamás emite NaN/Infinity (cinturón en el\nengine — un no-finito serializaría como null y envenenaría la\nrespuesta).",
          "format": "double",
          "type": "number"
        }
      },
      "required": [
        "path",
        "score"
      ],
      "type": "object"
    },
    "ServerInfo": {
      "description": "Identidad del servidor (va en [`InitializeResult`]).",
      "properties": {
        "name": {
          "description": "Nombre del servidor (`norte-core`).",
          "type": "string"
        },
        "version": {
          "description": "Versión del binario del daemon (informativa).",
          "type": "string"
        }
      },
      "required": [
        "name",
        "version"
      ],
      "type": "object"
    },
    "SpanWire": {
      "description": "Un span de texto con estilo opcional (elemento de una línea de\n[`PluginPreviewStyled::lines`], 0.27.0, G3, ADR 0037): el HOST pinta, el\nplugin solo describe. `text` es texto del plugin — NO CONFIABLE, un\nfrontend debe enmascararlo antes de renderizarlo (mismo trato que\n`PluginInfo::name`/`title`). `role` referencia un nombre de\n`norte_theme::Role` — el HOST lo valida contra el conjunto CERRADO al\nproducirlo (un nombre desconocido nunca sale al wire como texto libre,\ncolapsa a `None` antes de serializar); un cliente remoto que reciba de un\ndaemon en el que no confía plenamente un `role` que no reconoce debe\ntratarlo igual, como `None`. `fg` es un fallback de color RGB crudo para\nspans sin rol (p. ej. la paleta fija de un highlighter); cuando AMBOS\nestán presentes, `role` gana — el tema del usuario tiene precedencia\nsobre un color fijo del plugin.\n\n```\nuse norte_proto::methods::SpanWire;\nlet s: SpanWire = serde_json::from_str(r#\"{\"text\":\"fn\"}\"#).unwrap();\nassert_eq!(s.text, \"fn\");\nassert_eq!(s.role, None);\nassert_eq!(s.fg, None);\n```",
      "properties": {
        "fg": {
          "description": "Color RGB crudo de respaldo cuando no hay `role` (un byte por canal).",
          "items": {
            "format": "uint8",
            "maximum": 255,
            "minimum": 0,
            "type": "integer"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": [
            "array",
            "null"
          ]
        },
        "role": {
          "description": "Nombre de rol de `norte_theme::Role` (validado host-side; un nombre\ndesconocido nunca llega hasta aquí como `Some`, ver el rustdoc del\ntipo).",
          "type": [
            "string",
            "null"
          ]
        },
        "text": {
          "description": "Texto del span. Texto del plugin — NO confiable.",
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "SymlinkPolicy": {
      "description": "Qué hacer con los symlinks al copiar (spec §17.9; ADR 0005).\n\nEn M1, [`Follow`](Self::Follow) sobre un symlink a DIRECTORIO devuelve\n`Unsupported` (seguir dirs exige detección de ciclos — M2).\n\n```\nuse norte_proto::SymlinkPolicy;\nlet p: SymlinkPolicy = serde_json::from_str(r#\"\"preserve\"\"#).unwrap();\nassert_eq!(p, SymlinkPolicy::Preserve);\nassert_eq!(SymlinkPolicy::default(), SymlinkPolicy::Preserve);\n```",
      "oneOf": [
        {
          "const": "follow",
          "description": "Copiar el CONTENIDO apuntado. Symlink a dir: `Unsupported` en M1.",
          "type": "string"
        },
        {
          "const": "preserve",
          "description": "Recrear el symlink en el destino, bytes del target intactos\n(default: lo que hace `cp -a`).",
          "type": "string"
        },
        {
          "const": "skip",
          "description": "No copiar symlinks (contados como saltados).",
          "type": "string"
        }
      ]
    },
    "TaskCancelParams": {
      "description": "Params de [`TASK_CANCEL`].",
      "properties": {
        "task_id": {
          "$ref": "#/$defs/TaskId",
          "description": "Task a cancelar. Cancelar una Task terminal o inexistente no es error:\nla respuesta llega igual y el estado real viaja por [`TASK_PROGRESS`]."
        }
      },
      "required": [
        "task_id"
      ],
      "type": "object"
    },
    "TaskCancelResult": {
      "description": "Result de [`TASK_CANCEL`]: objeto vacío, reservado para extensión.",
      "type": "object"
    },
    "TaskId": {
      "description": "Identificador de una Task, único por proceso core.\n\nWire: número JSON transparente.\n\n```\nuse norte_proto::TaskId;\nlet id = TaskId::new(42);\nassert_eq!(serde_json::to_string(&id).unwrap(), \"42\");\n```",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "TaskKind": {
      "description": "Clase de operación que ejecuta una Task (M0: las tres mutaciones del VFS).\n\n`#[non_exhaustive]` (#126): antes de esto, cada variante nueva rompía la\nAPI de Rust para quien hiciera match exhaustivo fuera de este crate — por\neso `Mkdir`, `Embed` y `RenameBatch` tocaron los dos frontends en su propio\ncommit. Es una propiedad SOLO de la API de Rust: invisible en JSON, no\nmueve el wire, no toca `#[serde(other)]` ni pide bump de versión de\nprotocolo. El coste es simétrico al beneficio: un `match` externo ahora\nnecesita un brazo `_`, así que el compilador deja de señalar dónde un kind\nnuevo necesita etiqueta — cada `_` debe hacer lo mismo que ya hace el\nbrazo de [`TaskKind::Unknown`] en ese mismo match, no inventar un\ncomportamiento nuevo.",
      "oneOf": [
        {
          "const": "copy",
          "description": "Copia (posiblemente recursiva, posiblemente cross-provider).",
          "type": "string"
        },
        {
          "const": "move",
          "description": "Movimiento (rename atómico o copy+delete).",
          "type": "string"
        },
        {
          "const": "delete",
          "description": "Borrado (recursivo post-order).",
          "type": "string"
        },
        {
          "const": "undo",
          "description": "Undo de sesión: deshace mutaciones previas en LIFO (M3-2).\n\nOJO (compat): esta variante entra en 0.10.0. Un cliente 0.9.x (N-1) NO la\nconoce y su parse de `TaskKind` FALLA al recibirla — el `serde(other)` de\nabajo protege a ESTE proto (0.10+) frente a kinds de 0.11+, no\nretroactivamente al 0.9. En M3-2 el undo no se expone por RPC (no llega a\nclientes), así que la rotura es latente; M3-4 debe gatear la emisión de\nkinds nuevos por versión negociada (o asumir descarte silencioso en\nbroadcast y proteger el resync de `task.list`).",
          "type": "string"
        },
        {
          "const": "search",
          "description": "Búsqueda viva por nombre y/o contenido bajo un subtree (`fs.search`,\nM4 live search). Lectura pura (regla 4 no aplica): sin journal.\n\nOJO (compat): esta variante entra en 0.18.0. A diferencia del borde\n0.9→0.10 de [`TaskKind::Undo`] (donde el `serde(other)` de abajo\nTODAVÍA no existía), un cliente 0.17.x (N-1) YA tiene ese fallback\n(desde 0.10) — recibirla la degrada a [`TaskKind::Unknown`] sin\nfallar el parse. Sin gating de emisión necesario para este borde.",
          "type": "string"
        },
        {
          "const": "mkdir",
          "description": "Creación de un directorio (`fs.mkdir`, #104). Mutación: pasa por el\njournal como `Created` con su undo (regla 4). Entra en 0.31.0; un\ncliente N-1 (0.30.x) la degrada a [`TaskKind::Unknown`] vía el\n`serde(other)`, mismo caso que `Search`/`Index`.",
          "type": "string"
        },
        {
          "const": "index",
          "description": "Construcción/actualización del índice de búsqueda de un subtree\n(`index.build`, M4). Entra en 0.25.0; un cliente N-1 (0.24.x) la degrada a\n[`TaskKind::Unknown`] vía el `serde(other)`.",
          "type": "string"
        },
        {
          "const": "embed",
          "description": "`index.embed` (0.33.0): generación de embeddings del índice semántico.\nUn cliente N-1 (0.32.x) la degrada a [`TaskKind::Unknown`] por su\n`serde(other)`.",
          "type": "string"
        },
        {
          "const": "rename_batch",
          "description": "Un lote de renames dentro de UN directorio ejecutado como UNA\ntransacción con UNA unidad deshacible del journal (`fs.rename_batch`,\n0.36.0). El progreso es `i/n` PASOS, no bytes. Un cliente N-1 (0.35.x)\nla degrada a [`TaskKind::Unknown`] por el `serde(other)` de abajo, igual\nque `Search`/`Index`/`Embed`.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "Clase desconocida: un daemon N+1 (0.11+) envió un kind que ESTE proto no\nconoce → se acepta como genérica en vez de fallar el parse (forward-compat\ndesde 0.10, como [`TaskState::Unknown`]). No cubre el borde hacia atrás\n0.9→0.10 (ver `Undo`); SÍ cubre 0.17→0.18 (ver `Search`, ya nacida\ndentro de la ventana de este fallback).\n\n`TaskKind::Index` (0.25.0, `index.build`) es el mismo caso que `Search`:\nun cliente 0.24.x lo degrada aquí sin fallar.",
          "type": "string"
        }
      ]
    },
    "TaskListParams": {
      "description": "Params de [`TASK_LIST`]: objeto vacío, reservado para extensión\n(filtros por estado/kind llegarán aquí como campos opcionales).",
      "type": "object"
    },
    "TaskListResult": {
      "description": "Result de [`TASK_LIST`].\n\n```\nuse norte_proto::methods::TaskListResult;\nlet r: TaskListResult = serde_json::from_str(r#\"{\"tasks\":[]}\"#).unwrap();\nassert!(r.tasks.is_empty());\n```",
      "properties": {
        "tasks": {
          "description": "Snapshots de las tasks vivas + los desenlaces recientes retenidos\npor el server (mejor esfuerzo; ver [`TASK_LIST`]). Puede repetir\n`task_id` — el receptor deduplica.",
          "items": {
            "$ref": "#/$defs/TaskProgress"
          },
          "type": "array"
        }
      },
      "required": [
        "tasks"
      ],
      "type": "object"
    },
    "TaskProgress": {
      "description": "Snapshot de progreso de una Task (payload de la notificación `task.progress`).\n\nTotales `None` = aún desconocidos (walk en curso), jamás un 0 fingido.\nEl emisor coalesce; el último snapshot de una Task siempre lleva estado\nterminal y totales finales.\n\n```\nuse norte_proto::{TaskId, TaskKind, TaskProgress, TaskState};\nlet p = TaskProgress {\n    task_id: TaskId::new(7),\n    kind: TaskKind::Copy,\n    state: TaskState::Running,\n    bytes_done: 512,\n    bytes_total: Some(4096),\n    entries_done: 0,\n    entries_total: Some(2),\n    current: None,\n};\nlet json = serde_json::to_string(&p).unwrap();\nassert_eq!(serde_json::from_str::<TaskProgress>(&json).unwrap(), p);\n```",
      "properties": {
        "bytes_done": {
          "description": "Bytes ya procesados.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "bytes_total": {
          "default": null,
          "description": "Bytes totales estimados; `None` mientras el walk no termina.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "current": {
          "anyOf": [
            {
              "$ref": "#/$defs/VPath"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Entrada en curso (para pintar \"copiando X…\"); puede faltar."
        },
        "entries_done": {
          "description": "Entradas (archivos/dirs) ya procesadas.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "entries_total": {
          "default": null,
          "description": "Entradas totales estimadas; `None` mientras el walk no termina.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "kind": {
          "$ref": "#/$defs/TaskKind",
          "description": "Clase de operación (un frontend pinta \"copiando…\" sin estado propio)."
        },
        "state": {
          "$ref": "#/$defs/TaskState",
          "description": "Estado en el momento del snapshot."
        },
        "task_id": {
          "$ref": "#/$defs/TaskId",
          "description": "Task a la que pertenece el snapshot."
        }
      },
      "required": [
        "task_id",
        "kind",
        "state",
        "bytes_done",
        "entries_done"
      ],
      "type": "object"
    },
    "TaskState": {
      "description": "Estado del ciclo de vida de una Task.\n\nWire: objeto tagged `{\"kind\": \"...\", …}` — mismo convenio que [`Error`].\nTolerancia N/N-1 (ADR 0004): un estado desconocido deserializa a\n[`TaskState::Unknown`], que se trata como NO terminal (conservador:\nel cliente sigue escuchando `task.progress` hasta un estado que entienda).\n\n```\nuse norte_proto::TaskState;\nlet s: TaskState = serde_json::from_str(r#\"{\"kind\": \"running\"}\"#).unwrap();\nassert_eq!(s, TaskState::Running);\nassert!(!s.is_terminal());\n```",
      "oneOf": [
        {
          "description": "Encolada, aún sin hueco en el scheduler.",
          "properties": {
            "kind": {
              "const": "pending",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Ejecutándose.",
          "properties": {
            "kind": {
              "const": "running",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Pausada (spec §11: `task.pause`). M0 no la emite; queda reservada ya\npara que estrenarla no rompa a clientes N-1.",
          "properties": {
            "kind": {
              "const": "paused",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Terminó bien.",
          "properties": {
            "kind": {
              "const": "completed",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Cancelada limpiamente (destino limpio o `.norte-partial`, spec §5).",
          "properties": {
            "kind": {
              "const": "cancelled",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Falló; el error dice por qué (un panic capturado llega como\n[`Error::Internal`] con `panic: true`).",
          "properties": {
            "error": {
              "$ref": "#/$defs/Error",
              "description": "Causa del fallo."
            },
            "kind": {
              "const": "failed",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "error"
          ],
          "type": "object"
        },
        {
          "description": "Estado de un protocolo más nuevo (fallback de deserialización).\nEl core JAMÁS lo emite.",
          "properties": {
            "kind": {
              "const": "unknown",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "UndoBlocked": {
      "description": "Un bloqueo del undo: dónde y por qué (elemento de\n[`PolicyUndoReportResult::blocked`]).",
      "properties": {
        "error": {
          "$ref": "#/$defs/Error",
          "description": "Motivo (taxonomía de errores del protocolo; drift/conflicto típicos)."
        },
        "seq": {
          "description": "`seq` de la entrada del journal cuya reversa bloqueó. Referencia\nOPACA para el cliente: solo cobra sentido contra el journal/audit del\nserver (M3-5) — sirve para citarla, no para interpretarla.",
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "seq",
        "error"
      ],
      "type": "object"
    },
    "VPath": {
      "description": "VPath wire string: `scheme://authority/segments`. Filenames are bytes; non-UTF-8 segments use the percent-encoded wire form.",
      "type": "string"
    },
    "VerifyPolicy": {
      "description": "Cómo verificar el `.norte-partial` antes de reanudar sobre él\n(ADR 0012). Solo aplica con [`ResumePolicy::On`].\n\n```\nuse norte_proto::VerifyPolicy;\nassert_eq!(VerifyPolicy::default(), VerifyPolicy::Length);\nlet v: VerifyPolicy = serde_json::from_str(r#\"\"hash\"\"#).unwrap();\nassert_eq!(v, VerifyPolicy::Hash);\n```",
      "oneOf": [
        {
          "const": "length",
          "description": "Solo longitud: si el parcial es más largo que el origen se descarta\ny se empieza de cero. Barato (default).",
          "type": "string"
        },
        {
          "const": "hash",
          "description": "Además, el hash de `origen[..already]` debe coincidir con el del\nparcial; si no, se descarta. Relee `already` bytes de ambos lados.",
          "type": "string"
        }
      ]
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Aggregate root: one field per top-level wire type. `schema_for!` emits each\nas a property and pulls every nested type into `$defs`, so the single\ndocument covers the whole protocol surface. Field values are never read.",
  "properties": {
    "ai_rename_entry": {
      "$ref": "#/$defs/AiRenameEntry"
    },
    "ai_rename_plan_params": {
      "$ref": "#/$defs/AiRenamePlanParams"
    },
    "ai_rename_plan_result": {
      "$ref": "#/$defs/AiRenamePlanResult"
    },
    "attr_hint": {
      "$ref": "#/$defs/AttrHint"
    },
    "attr_info": {
      "$ref": "#/$defs/AttrInfo"
    },
    "attr_type": {
      "$ref": "#/$defs/AttrType"
    },
    "attr_value": {
      "$ref": "#/$defs/AttrValue"
    },
    "byte_range": {
      "$ref": "#/$defs/ByteRange"
    },
    "capabilities": {
      "$ref": "#/$defs/Capabilities"
    },
    "capability_flags": {
      "$ref": "#/$defs/CapabilityFlags"
    },
    "client_info": {
      "$ref": "#/$defs/ClientInfo"
    },
    "collision_policy": {
      "$ref": "#/$defs/CollisionPolicy"
    },
    "conflict_kind": {
      "$ref": "#/$defs/ConflictKind"
    },
    "connection_degraded": {
      "$ref": "#/$defs/ConnectionDegraded"
    },
    "connection_trust_host_key_params": {
      "$ref": "#/$defs/ConnectionTrustHostKeyParams"
    },
    "connection_trust_host_key_result": {
      "$ref": "#/$defs/ConnectionTrustHostKeyResult"
    },
    "daemon_shutdown_params": {
      "$ref": "#/$defs/DaemonShutdownParams"
    },
    "daemon_shutdown_result": {
      "$ref": "#/$defs/DaemonShutdownResult"
    },
    "decoration_wire": {
      "$ref": "#/$defs/DecorationWire"
    },
    "delete_mode": {
      "$ref": "#/$defs/DeleteMode"
    },
    "entry": {
      "$ref": "#/$defs/Entry"
    },
    "entry_kind": {
      "$ref": "#/$defs/EntryKind"
    },
    "error": {
      "$ref": "#/$defs/Error"
    },
    "fs_capabilities_params": {
      "$ref": "#/$defs/FsCapabilitiesParams"
    },
    "fs_capabilities_result": {
      "$ref": "#/$defs/FsCapabilitiesResult"
    },
    "fs_copy_params": {
      "$ref": "#/$defs/FsCopyParams"
    },
    "fs_delete_params": {
      "$ref": "#/$defs/FsDeleteParams"
    },
    "fs_list_params": {
      "$ref": "#/$defs/FsListParams"
    },
    "fs_list_result": {
      "$ref": "#/$defs/FsListResult"
    },
    "fs_mkdir_params": {
      "$ref": "#/$defs/FsMkdirParams"
    },
    "fs_move_params": {
      "$ref": "#/$defs/FsMoveParams"
    },
    "fs_read_params": {
      "$ref": "#/$defs/FsReadParams"
    },
    "fs_read_result": {
      "$ref": "#/$defs/FsReadResult"
    },
    "fs_rename_batch_params": {
      "$ref": "#/$defs/FsRenameBatchParams"
    },
    "fs_rename_batch_plan_params": {
      "$ref": "#/$defs/FsRenameBatchPlanParams"
    },
    "fs_rename_batch_plan_result": {
      "$ref": "#/$defs/FsRenameBatchPlanResult"
    },
    "fs_rename_batch_report_params": {
      "$ref": "#/$defs/FsRenameBatchReportParams"
    },
    "fs_rename_batch_report_result": {
      "$ref": "#/$defs/FsRenameBatchReportResult"
    },
    "fs_search_params": {
      "$ref": "#/$defs/FsSearchParams"
    },
    "fs_stat_params": {
      "$ref": "#/$defs/FsStatParams"
    },
    "fs_stat_result": {
      "$ref": "#/$defs/FsStatResult"
    },
    "fs_task_result": {
      "$ref": "#/$defs/FsTaskResult"
    },
    "grant_scope_params": {
      "$ref": "#/$defs/GrantScopeParams"
    },
    "grant_scope_result": {
      "$ref": "#/$defs/GrantScopeResult"
    },
    "index_build_params": {
      "$ref": "#/$defs/IndexBuildParams"
    },
    "index_build_result": {
      "$ref": "#/$defs/IndexBuildResult"
    },
    "index_embed_params": {
      "$ref": "#/$defs/IndexEmbedParams"
    },
    "index_hit": {
      "$ref": "#/$defs/IndexHit"
    },
    "index_query_params": {
      "$ref": "#/$defs/IndexQueryParams"
    },
    "index_query_result": {
      "$ref": "#/$defs/IndexQueryResult"
    },
    "index_search_semantic_params": {
      "$ref": "#/$defs/IndexSearchSemanticParams"
    },
    "index_search_semantic_result": {
      "$ref": "#/$defs/IndexSearchSemanticResult"
    },
    "initialize_params": {
      "$ref": "#/$defs/InitializeParams"
    },
    "initialize_result": {
      "$ref": "#/$defs/InitializeResult"
    },
    "match_info": {
      "$ref": "#/$defs/MatchInfo"
    },
    "pending_approval": {
      "$ref": "#/$defs/PendingApproval"
    },
    "plan_hash": {
      "$ref": "#/$defs/PlanHash"
    },
    "plugin_column_info": {
      "$ref": "#/$defs/PluginColumnInfo"
    },
    "plugin_column_values_params": {
      "$ref": "#/$defs/PluginColumnValuesParams"
    },
    "plugin_column_values_result": {
      "$ref": "#/$defs/PluginColumnValuesResult"
    },
    "plugin_command_info": {
      "$ref": "#/$defs/PluginCommandInfo"
    },
    "plugin_config_key_wire": {
      "$ref": "#/$defs/PluginConfigKeyWire"
    },
    "plugin_decorate_params": {
      "$ref": "#/$defs/PluginDecorateParams"
    },
    "plugin_decorate_result": {
      "$ref": "#/$defs/PluginDecorateResult"
    },
    "plugin_decorations": {
      "$ref": "#/$defs/PluginDecorations"
    },
    "plugin_get_config_params": {
      "$ref": "#/$defs/PluginGetConfigParams"
    },
    "plugin_get_config_result": {
      "$ref": "#/$defs/PluginGetConfigResult"
    },
    "plugin_help_params": {
      "$ref": "#/$defs/PluginHelpParams"
    },
    "plugin_help_result": {
      "$ref": "#/$defs/PluginHelpResult"
    },
    "plugin_info": {
      "$ref": "#/$defs/PluginInfo"
    },
    "plugin_list_params": {
      "$ref": "#/$defs/PluginListParams"
    },
    "plugin_list_result": {
      "$ref": "#/$defs/PluginListResult"
    },
    "plugin_load_error": {
      "$ref": "#/$defs/PluginLoadError"
    },
    "plugin_preview": {
      "$ref": "#/$defs/PluginPreview"
    },
    "plugin_preview_params": {
      "$ref": "#/$defs/PluginPreviewParams"
    },
    "plugin_preview_result": {
      "$ref": "#/$defs/PluginPreviewResult"
    },
    "plugin_preview_styled": {
      "$ref": "#/$defs/PluginPreviewStyled"
    },
    "plugin_preview_styled_params": {
      "$ref": "#/$defs/PluginPreviewStyledParams"
    },
    "plugin_preview_styled_result": {
      "$ref": "#/$defs/PluginPreviewStyledResult"
    },
    "plugin_run_command_params": {
      "$ref": "#/$defs/PluginRunCommandParams"
    },
    "plugin_run_command_result": {
      "$ref": "#/$defs/PluginRunCommandResult"
    },
    "plugin_set_approval_params": {
      "$ref": "#/$defs/PluginSetApprovalParams"
    },
    "plugin_set_approval_result": {
      "$ref": "#/$defs/PluginSetApprovalResult"
    },
    "plugin_set_config_params": {
      "$ref": "#/$defs/PluginSetConfigParams"
    },
    "plugin_set_config_result": {
      "$ref": "#/$defs/PluginSetConfigResult"
    },
    "plugin_set_enabled_params": {
      "$ref": "#/$defs/PluginSetEnabledParams"
    },
    "plugin_set_enabled_result": {
      "$ref": "#/$defs/PluginSetEnabledResult"
    },
    "policy_approval_required": {
      "$ref": "#/$defs/PolicyApprovalRequired"
    },
    "policy_decide_params": {
      "$ref": "#/$defs/PolicyDecideParams"
    },
    "policy_decide_result": {
      "$ref": "#/$defs/PolicyDecideResult"
    },
    "policy_pending_result": {
      "$ref": "#/$defs/PolicyPendingResult"
    },
    "policy_undo_report_params": {
      "$ref": "#/$defs/PolicyUndoReportParams"
    },
    "policy_undo_report_result": {
      "$ref": "#/$defs/PolicyUndoReportResult"
    },
    "policy_undo_session_params": {
      "$ref": "#/$defs/PolicyUndoSessionParams"
    },
    "policy_undo_session_result": {
      "$ref": "#/$defs/PolicyUndoSessionResult"
    },
    "request_scope_params": {
      "$ref": "#/$defs/RequestScopeParams"
    },
    "request_scope_result": {
      "$ref": "#/$defs/RequestScopeResult"
    },
    "resume_policy": {
      "$ref": "#/$defs/ResumePolicy"
    },
    "rpc_cancel_params": {
      "$ref": "#/$defs/RpcCancelParams"
    },
    "search_hits": {
      "$ref": "#/$defs/SearchHits"
    },
    "segment": {
      "$ref": "#/$defs/Segment"
    },
    "semantic_hit": {
      "$ref": "#/$defs/SemanticHit"
    },
    "server_info": {
      "$ref": "#/$defs/ServerInfo"
    },
    "span_wire": {
      "$ref": "#/$defs/SpanWire"
    },
    "symlink_policy": {
      "$ref": "#/$defs/SymlinkPolicy"
    },
    "task_cancel_params": {
      "$ref": "#/$defs/TaskCancelParams"
    },
    "task_cancel_result": {
      "$ref": "#/$defs/TaskCancelResult"
    },
    "task_id": {
      "$ref": "#/$defs/TaskId"
    },
    "task_kind": {
      "$ref": "#/$defs/TaskKind"
    },
    "task_list_params": {
      "$ref": "#/$defs/TaskListParams"
    },
    "task_list_result": {
      "$ref": "#/$defs/TaskListResult"
    },
    "task_progress": {
      "$ref": "#/$defs/TaskProgress"
    },
    "task_state": {
      "$ref": "#/$defs/TaskState"
    },
    "undo_blocked": {
      "$ref": "#/$defs/UndoBlocked"
    },
    "v_path": {
      "$ref": "#/$defs/VPath"
    },
    "verify_policy": {
      "$ref": "#/$defs/VerifyPolicy"
    }
  },
  "required": [
    "ai_rename_entry",
    "ai_rename_plan_params",
    "ai_rename_plan_result",
    "attr_hint",
    "attr_info",
    "attr_type",
    "attr_value",
    "byte_range",
    "capabilities",
    "capability_flags",
    "client_info",
    "collision_policy",
    "conflict_kind",
    "connection_degraded",
    "connection_trust_host_key_params",
    "connection_trust_host_key_result",
    "daemon_shutdown_params",
    "daemon_shutdown_result",
    "decoration_wire",
    "delete_mode",
    "entry",
    "entry_kind",
    "error",
    "fs_capabilities_params",
    "fs_capabilities_result",
    "fs_copy_params",
    "fs_delete_params",
    "fs_list_params",
    "fs_list_result",
    "fs_mkdir_params",
    "fs_move_params",
    "fs_read_params",
    "fs_read_result",
    "fs_rename_batch_params",
    "fs_rename_batch_plan_params",
    "fs_rename_batch_plan_result",
    "fs_rename_batch_report_params",
    "fs_rename_batch_report_result",
    "fs_search_params",
    "fs_stat_params",
    "fs_stat_result",
    "fs_task_result",
    "grant_scope_params",
    "grant_scope_result",
    "index_build_params",
    "index_build_result",
    "index_embed_params",
    "index_hit",
    "index_query_params",
    "index_query_result",
    "index_search_semantic_params",
    "index_search_semantic_result",
    "initialize_params",
    "initialize_result",
    "match_info",
    "pending_approval",
    "plan_hash",
    "plugin_column_info",
    "plugin_column_values_params",
    "plugin_column_values_result",
    "plugin_command_info",
    "plugin_config_key_wire",
    "plugin_decorate_params",
    "plugin_decorate_result",
    "plugin_decorations",
    "plugin_get_config_params",
    "plugin_get_config_result",
    "plugin_help_params",
    "plugin_help_result",
    "plugin_info",
    "plugin_list_params",
    "plugin_list_result",
    "plugin_load_error",
    "plugin_preview",
    "plugin_preview_params",
    "plugin_preview_result",
    "plugin_preview_styled",
    "plugin_preview_styled_params",
    "plugin_preview_styled_result",
    "plugin_run_command_params",
    "plugin_run_command_result",
    "plugin_set_approval_params",
    "plugin_set_approval_result",
    "plugin_set_config_params",
    "plugin_set_config_result",
    "plugin_set_enabled_params",
    "plugin_set_enabled_result",
    "policy_approval_required",
    "policy_decide_params",
    "policy_decide_result",
    "policy_pending_result",
    "policy_undo_report_params",
    "policy_undo_report_result",
    "policy_undo_session_params",
    "policy_undo_session_result",
    "request_scope_params",
    "request_scope_result",
    "resume_policy",
    "rpc_cancel_params",
    "search_hits",
    "segment",
    "semantic_hit",
    "server_info",
    "span_wire",
    "symlink_policy",
    "task_cancel_params",
    "task_cancel_result",
    "task_id",
    "task_kind",
    "task_list_params",
    "task_list_result",
    "task_progress",
    "task_state",
    "undo_blocked",
    "v_path",
    "verify_policy"
  ],
  "title": "ProtocolSchema",
  "type": "object"
}
