[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
#   shell      → curl -sSf .../norte-installer.sh | sh   (linux/macOS)
#   powershell → irm .../norte-installer.ps1 | iex        (windows)
installers = ["shell", "powershell"]
# Target platforms to build apps for (Rust target-triple syntax).
# gnu Linux (x64/arm64) cubre las distros modernas; macOS intel+arm; windows x64.
# musl estático (cualquier distro vieja) queda como futuro: aws-lc-rs (russh/
# rustls) sobre musl exige toolchain cruzado y es frágil — ver ADR 0021.
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Un `cargo build --package` POR aplicación, no un `--workspace` para todas.
#
# No es una preferencia de estilo: las features de cargo se unifican POR
# invocación, así que construir el workspace entero de una vez mete las
# features de cualquier miembro dentro de los binarios que publicamos —fue
# exactamente lo que pasó con el `serde_json/preserve_order` de la GUI GPUI,
# que cambiaba el orden de claves del JSON publicado—. Un paquete por
# invocación es lo que garantiza que se distribuye lo que el gate probó.
precise-builds = true
