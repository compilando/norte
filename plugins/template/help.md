+++
id = "org.example.template"
title = "Template"
+++
This page ships with the plugin and appears in norte's help, in the
extensions group, marked as written by a plugin.

The `hello` command greets whoever is named in its argument, with the word
set in the `greeting` setting. The previewer shows the first line of a text
file, and says how many bytes the host handed it.
